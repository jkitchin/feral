* DONE D.1 FactorWorkspace — pool scratch across factor calls
  - Context: dev/research/sparse-tail-perf-2026-04-19.md §6-§9,
    dev/plans/factor-workspace.md.
  - Landed 2026-04-19 (session 04). Sparse factor/MUMPS geomean
    0.48 -> 0.46; VESUVIO reallocs 2053 -> 13 (-99%).
  - Commits: 9c0419b (plan), b1016cc + f102d56 + dedb3f3
    (rollout + Solver wiring).
  - Exited at 0.46 (short of ≤0.40 ex-ante target) per the plan's
    exit clause — remaining tail was structural, not alloc churn.

* DONE D.3 dense fast-path for small-and-dense matrices
  - Context: dev/research/sparse-tail-d3-d4-2026-04-19.md,
    dev/plans/sparse-tail-d3.md.
  - Landed 2026-04-19 (session 04). Sparse factor/MUMPS geomean
    0.46 -> 0.37 (-20%); max ratio 128.34 -> 80.22 (-37%).
    Ex-ante target ≤0.44 met with 0.07 margin.
  - Commits: ef4a626 (research), 71f5692 (plan),
    7c9e07d (RED), 32dd65a (GREEN), 70f077e (stage 1/2),
    e0db169 (stage 3).

* TODO follow-up: D.4 tiny-n fast path (n <= 16)
  - Context: dev/research/sparse-tail-d3-d4-2026-04-19.md §3.
  - D.3's `dense_fast_factor` synthesis path is a prerequisite and
    is now landed — D.4 reuses the same SparseFactors shape with a
    tighter gate (`N_TINY <= 16`) and a stack-buffer densify.
  - Target: PFIT4_2178 (n=6), GAUSS1_0002 (n=8), HATFLDH_2515
    (n=11) and similar in the post-D.3 top-10.
  - Needs its own plan before code. Start at
    dev/plans/sparse-tail-d4.md.

* TODO follow-up: small-n `compute_scaling` fast path
  - Context: dev/results/lever-d3/stage1-stage2-2026-04-19.md §1
    (phase breakdown). `compute_scaling` is 82% of dense-path wall
    time on TRO3X3_0013 (34 of 41 us at n=69).
  - Idea: when `should_use_dense_fast_path` fires, bypass MC64's
    Hungarian matching and use `infnorm::compute_infnorm` directly
    against the dense buffer we're about to create. MC64's win
    over InfNorm is marginal on small-dense matrices and the
    matching overhead dominates.
  - Estimated win: TRO3X3_0013 41 us -> ~20 us (halving).

* TODO follow-up: pool `CscMatrix::to_dense` buffer via FactorWorkspace
  - Context: dev/plans/sparse-tail-d3.md §"Interaction with
    FactorWorkspace" (explicitly listed as a follow-up there).
  - 9 us per call on TRO3X3_0013 (22% of dense-path wall time).
    Widen FactorWorkspace with a `dense_buf: Vec<f64>` field;
    `dense_fast_factor` takes it via `std::mem::take` + clear +
    resize, then hands it back.

* TODO follow-up: diagnose post-D.3 top-10 worst ratios
  - Context: dev/results/lever-d3/stage3-corpus-2026-04-19.md.
    New top-10 is led by HS85_0022 (n=68, 80x) and DECONVC_0105
    (n=52, 55x). 9/10 are in the D.3 gate's size range.
  - Question: are these in-gate and the dense path pathological,
    or out-of-gate (density < 0.25) and the multifrontal path is
    slow? Decides whether the lever is "widen the gate" or
    "attack the remaining multifrontal overhead".


* TODO follow-up: evaluate Mongoose (SuiteSparse edge separator + AMD hybrid)
  - User request 2026-04-18-03.
  - SuiteSparse Mongoose is an edge-separator-driven hybrid ordering that
    couples a ND-style outer separator recursion with AMD on the leaves.
    Could give another data point alongside feral-amd/metis/scotch for
    the large-KKT bakeoff.
  - Research note completed 2026-04-18-06: dev/research/ordering-mongoose.md.
    Findings:
    + License GPL-3.0-only (blocks source reuse; clean-room from paper
      is fine per FERAL protocol).
    + Paper: Davis/Hager/Kolodziej/Yeralan 2020, ACM TOMS 46(1) Art 7,
      DOI 10.1145/3337792.
    + Published Mongoose is edge-partitioning ONLY; the ND-outer +
      AMD-inner pipeline described here is the paper's future-work
      section, NOT shipped code.
    + Recommendation (b): cherry-pick HEMSR coarsening + 2-phase hybrid
      refinement into feral-metis / feral-kahip behind feature flags.
      A dedicated feral-mongoose crate would be ~70% duplicated plumbing.
  - Open question: has a vertex-separator extension shipped since 2020?
    Quick check of SuiteSparse dev branch before scheduling any plan.
  - Next step if pursued: dev/plans/feral-metis-hemsr-coarsening.md
    (or similar) — NOT a dedicated crate plan.

* TODO performance: blocked BK + SIMD micro-kernel for src/dense/factor.rs
  - Context: dev/sessions/2026-04-18-08.md "Next Session Should #1";
    dev/journal/2026-04-18-08.org VESUVIO entry.
  - Pure performance issue, NOT correctness. VESUVIO matrices factor
    correctly under both AMD and MetisND (inertia matches oracle,
    residuals pass). They just take 84x longer than MUMPS.
  - Bottleneck: scalar rank-1 BK updates in src/dense/factor.rs
    on the dense root frontal (~2059x959 for VESUVIO). Measured
    ~8 GFLOP/s vs MUMPS DGETRF on Accelerate ~400 GFLOP/s, exact
    50x kernel gap explains the 84x factor ratio.
  - Both AMD and MetisND produce the same ~67%-of-n root frontal
    (single dense linking column with 1026 nnz). Not an ordering
    fix; needs blocked BK + SIMD.
  - Existing prerequisite plans:
    + dev/plans/phase-2.4.1-blocked-ldlt.md
    + dev/plans/phase-2.4.2-simd-schur-kernel.md
  - Estimated: 4-6 sessions to first measurable win. Risk:
    BK pivoting interleaves with the trailing update, so naive
    blocking is unsafe; SIMD on stable Rust means hand-written
    core::arch with runtime dispatch.
  - Corpus geomean factor/MUMPS already 0.42 (we win on average);
    this addresses the long tail (VESUVIO/CRESC arrow-KKTs only).
  - Defer until: a downstream consumer or benchmark target makes
    the VESUVIO tail a blocker, OR the current substantial-feature
    queue is empty.

* TODO follow-up: MSS1_0009 MC64-pathology triage (defer until after integration)
  - Context: dev/research/lever-c-policy-diff-2026-04-19.md.
  - Single non-jiggle non-excluded regression in Policy 3 (adaptive
    scaling): MSS1_0009 (n=163), baseline rel_res 6.31e-12 →
    Policy 3 rel_res 9.96e-7. All four oracles agree on inertia
    (90, 73, 0); verdict: definitive; agreement: strong.
  - Likely "matched-but-bad" MC64 of the POLAK6 family but on a
    larger, more interesting matrix.
  - NOT a deep bug. Iterative refinement in a real outer solve
    loop (Ipopt, our own solve_sparse_refined) almost certainly
    recovers 9.96e-7 to acceptable. The bench just doesn't refine.
  - Defer until: (a) we have integration with an outer-iteration
    consumer that exercises refinement end-to-end, OR (b) we ship
    the lever-C scaling-default flip and MSS1_0009 actually
    surfaces in a downstream consumer.
  - When picked up: clone src/bin/polak6_diag.rs against
    data/matrices/kkt/MSS1/MSS1_0009.mtx; check raw |diag| range,
    scaled |diag| range under InfNorm vs MC64, and per-column
    max(|off|/|diag|).
  - Decision deferred: lever-C scaling-default flip (Auto vs
    InfNorm). Current: keep InfNorm. Revisit alongside the
    triage above.

* TODO follow-up: adaptive ordering dispatcher
  - User request 2026-04-18-03 (session priority-list item 5).
  - Observation from dev/research/ordering-bakeoff-2026-04-18.md:
    AMD wins on small matrices (parity corpus), SCOTCH or METIS win
    on large (≥10k) KKT arrow structures. No single default is best.
  - Idea: pick ordering based on cheap pre-analysis (n, nnz, average
    row degree, maybe density of the arrow "apex" column). Budget: a
    function-call-time decision, not a full factor-and-measure.
  - Precondition: the post-FM-rebalance, post-AMD-retirement bakeoff
    numbers are stable. Both are in place as of 2026-04-18-03.
