#+TITLE: FERAL Journal — 2026-04-12 Session 01

* 08:15 :orientation:bench:
  Resumed Phase 1b. Fixed assemble-context.sh BSD-find incompatibility
  (find -printf is GNU-only, replaced with ls -1 | sort | tail -1).
  Confirmed cargo test was hanging at tests/dense_ldlt.rs:497
  test_bench_harness_output, which spawns =cargo run --bin bench= as a
  subprocess and now blocks for 13+ minutes on the 153k matrix corpus.
  Test was a Session-1 skeleton check; deleted with user approval.
  Two atomic commits: 87b9d1d, 79aa8c5.

* 08:35 :bench:phase-1b-exit:
  Re-ran cargo run --release --bin bench. Numbers:
  - Dense: 99.2% inertia (1221 miss), 99.6% residual (662 miss),
    worst residual 8.97e-1 (POLAK6_0021)
  - Sparse: 98.6% inertia (2154 miss), 99.0% residual (1525 miss),
    worst residual *2.61e21* (MGH10S_0000)

  Phase 1b exit criterion (FERAL-PROJECT-SPEC.md §1712) requires 100%
  inertia + solution. Neither path meets it. The sparse worst residual
  of 2.61e21 is *not* ill-conditioning — refinement cannot fix garbage
  output from a wrong factorization. Ordered triage of MGH10S before
  any other Phase 1b work.

* 08:50 :triage:mgh10s:
  Built examples/triage_mgh10s.rs to load MGH10S_0000 (51x51 KKT) and
  run dense + sparse paths side by side. Output:
  - Dense: inertia (35, 16, 0) MATCH, residual 2.014e-16
  - Sparse: inertia (50, 1, 0) MISMATCH, residual 2.613e21

  Dense handles the matrix cleanly. The sparse path computes inertia
  finding 15 extra positives and missing 15 negatives. Conclusion: this
  is a structural factorization bug in the sparse path, not numerical.
  Commit 895d4f3.

* 09:10 :diagnosis:supernodes:
  Read src/symbolic/{mod.rs, supernode.rs} and src/numeric/factorize.rs.

  MGH10S has bordered structure: AMD perm is identity. Etree has roots
  {0,1,2, 35..50}; constraint row 35+i has children {3+i, 19+i} (each
  variable column couples to one constraint). The two children of any
  constraint subtree are *non-adjacent* in column index — separated by
  15 columns.

  find_supernodes amalgamates child-into-parent under size-based merging
  (all ncol < nemin=32). The merge tracks first_col = min(child, parent)
  in supernode.rs:175. After merging, "supernode 35" reports first_col=3,
  ncol=3 but its actual column SET is {3, 19, 35} — non-contiguous.

  Every downstream consumer treats first_col..first_col+ncol as the
  eliminated set:
  - supernode.rs:200: =row_indices = (first_col..first_col+nrow).collect()=
  - factorize.rs:130: =for (local_j, &gj) in row_indices[..ncol].iter()=
  - factorize.rs:253: =for j in first_col..first_col+ncol=

  So the frontal *meant* to eliminate {3, 19, 35} actually eliminates
  {3, 4, 5}, and the negative constraint diagonals at cols 35..50 are
  mostly never reached. By coincidence the snode lengths still sum to
  51 across the matrix, so column-coverage *looks* correct.

* 09:25 :root-cause:postorder:
  Found the orphan: src/ordering/postorder.rs exists with a working
  =postorder()= function and 5 tests, but is *never called* from
  symbolic_factorize. The original Phase 1b plan
  (dev/plans/phase-1b-sparse-solver.md:96) explicitly called for
  "Composing AMD perm with postorder perm gives the final ordering"
  but the implementation regressed against the plan.

  Without postorder, AMD-permuted etrees on KKT matrices with bordered
  structure have non-contiguous parent-child column relationships.
  Supernode amalgamation requires a postordered tree as a precondition;
  CHOLMOD's cholmod_analyze_p does
    Perm = AMD(A); Parent = etree(A(Perm,Perm)); Post = postorder(Parent);
    Perm = Perm[Post]; rebuild Parent and ColCount on the composed perm.

  Wrote dev/research/postorder-pipeline.md (~190 lines, with worked
  MGH10S example, CHOLMOD pseudocode, SSIDS reference, correctness
  invariants) and dev/plans/postorder-fix.md (12 steps, three
  regression tests, acceptance criteria). Commit 429507f.

* 09:55 :test:reproducer:
  Wrote tests/sparse_postorder.rs. First attempt: 4x4 single-constraint
  bordered KKT — passed on main, doesn't trigger the bug (only one
  supernode, trivially contiguous).

  Built smaller two-constraint reproducer:
  - 6x6 with 2 vars + 2 slacks + 2 constraints
  - Each constraint couples one var + one slack
  - Etree has 2 disjoint subtrees, both amalgamated
  - Expected (4, 2, 0); main gives (5, 1, 0)

  Same shape failure as MGH10S (one extra positive, one missing
  negative) at minimum size. Test red as expected.

* 10:05 :fix:postorder:
  Implemented CHOLMOD-style composition in src/symbolic/mod.rs::symbolic_factorize:
  1. AMD perm
  2. Build etree on AMD-permuted pattern (intermediate)
  3. Postorder the etree
  4. Compose: =perm[k] = amd_perm[post[k]]=
  5. Re-permute on the composed perm and rebuild etree
  6. Column counts on final pattern + etree
  7. Supernode detection on the postordered etree

  Change is ~15 lines (lines 66-104), one new import. The struct field
  =perm= is now the composed permutation rather than just AMD perm.

* 10:15 :verify:bench:
  Ran cargo test, cargo clippy, triage, bench:

  - cargo test: 111/111 pass (107 existing + 4 new sparse_postorder).
    No regressions.
  - cargo clippy -- -D warnings: clean
  - cargo test --test sparse_postorder mgh10s -- --ignored: PASS
  - cargo run --release --example triage_mgh10s:
      sparse inertia (35, 16, 0) MATCH, residual 1.106e-16
      (was MISMATCH (50, 1, 0), residual 2.613e21)
  - cargo run --release --bin bench (153k matrices):
      | Metric                | Before     | After      |
      |-----------------------+------------+------------|
      | Sparse inertia match  | 98.6%      | *99.3%*    |
      | Sparse residual pass  | 99.0%      | *99.7%*    |
      | Sparse worst residual | 2.61e21    | *3.14e-4*  |

  Sparse now slightly *exceeds* dense (99.3 vs 99.2 inertia, 99.7 vs
  99.6 residual). The remaining ~0.3% gap is constrained by the dense
  BK kernel on ill-conditioned matrices, not the sparse pipeline.
  Worst sparse residual dropped 25 orders of magnitude — now in
  iterative-refinement territory.

  Acceptance criteria from dev/plans/postorder-fix.md all met.

* 11:30 :refinement:bench:
  Implemented solve_sparse_refined mirroring the dense version. Switched
  bench to use solve_refined and solve_sparse_refined per spec §1709
  (Phase 1b solve convention). First attempt was the naive "always
  accept dx" loop matching the existing dense implementation. Bench
  showed dense worst residual went from 8.97e-1 to *3.04e3* on
  POLAK6_0021 — refinement amplifying error on a rank-deficient matrix.

* 11:50 :refinement:best-iterate:
  Replaced with best-iterate strategy: track smallest ||r|| across all
  refinement steps and return the corresponding x. Allows non-monotone
  intermediate steps (extreme-scaling matrices need this) but guarantees
  the returned x is no worse than the unrefined solve. Removed the
  POLAK6 regression and improved pass count over both unrefined and
  naive-refined. Cumulative session delta vs the pre-postorder baseline:
  - Sparse inertia 150997 → 152038 (+1041)
  - Sparse residual 151626 → 152788 (+1162)
  - Sparse worst residual 2.61e21 → 3.14e-4 (25 orders of magnitude)

* 12:15 :triage:bench-tool:
  Added failure analysis to bench: per-failure record collection,
  family-grouped table, top-15 worst residuals individually, dense ∩
  sparse cross-comparison. Removed the obsolete 153k-line per-row
  print. Findings:
  - Dense 1507 failures: 880 are "feral solves at machine precision but
    rmumps disagrees on inertia label of boundary pivots" (QPNBLEND,
    MSS1, CORE1, CRESC*, KIRBY2 — *not feral bugs*, oracle definitional
    disagreement). 400 real residual failures clustered in problem
    families: ACOPP30 (68, residual ~3e-2), FBRAIN3LS, CERI*, HS46,
    DEVGLA2, PFIT2, ...
  - Sparse top failure: ERRINBAR_0824 with CORRECT inertia but bad
    residual (3.14e-4) — different category than dense failures.
  - Cross-comparison: 1376 matrices fail in both, 131 dense-only, 88
    sparse-only.

* 12:45 :triage:polak6:
  Built examples/triage_polak6.rs. POLAK6_0021 (n=9, κ ≈ 1e46):
  - Default zero_tol: inertia (4,1,4), residual 8.97e-1
  - Strict tol (1e-30): inertia (5,4,0), residual 34.4
  - Loose tol (1e-3): inertia (3,1,5), residual *4.6e-17* (machine
    precision)

  The loose-tol experiment proved the matrix is solvable. The default
  tol gets a worse answer because pivots in the band [eps·1e-10,
  100·eps] are flagged as zero by factor (counted in inertia.zero) but
  *divided by* in solve. The threshold mismatch is the bug.

* 13:00 :fix:threshold-mismatch:
  Stored zero_tol and zero_tol_2x2 in both Factors and FrontalFactors,
  populated from BunchKaufmanParams. Both dense and sparse solves now
  consult these stored thresholds and skip pivots that factor counted
  as zero. POLAK6_0021 default residual: 8.97e-1 → *4.605e-17* (machine
  precision). Tests in tests/threshold_consistency.rs (5+1 ignored).
  All 121 tests pass. Commit 95e6760.

* 13:30 :ultraplan:consensus:
  Wrote dev/plans/phase-1b-consensus-exit.md (~430 lines, 8 phases).
  The strict 100%-vs-rmumps spec criterion is unreachable without
  delayed pivoting (Phase 2). Replace with multi-source consensus
  across feral, rmumps, canonical Fortran MUMPS 5.8.2 (ref/mumps,
  unbuilt), and canonical Fortran SSIDS (ref/spral, unbuilt). For
  each matrix, classify as Definitive/Borderline/Excluded based on
  agreement across all four. Phase 1b exits when feral satisfies
  every Definitive verdict.

  rmumps is a Rust port of MUMPS by the same author developing feral.
  Treating it as ground truth means co-bugs in rmumps and feral are
  invisible. Consensus catches that. Decision recorded in
  dev/decisions.md with a reconsideration clause: if canonical Fortran
  oracles end up agreeing with rmumps to within float64 across the
  corpus, the strict criterion can be reinstated. Commit 9bb7d2e.

* 14:00 :triage:acopp30:
  Built examples/triage_acopp30.rs. ACOPP30_0000 (n=209, iter 0,
  delta_w=delta_c=0, expected (72,137,0)):
  - Many 2×2 pivot blocks (variable rows have zero Hessian diagonal)
  - One block at D[58] has subdiag 4.219e-15, both diagonals zero
  - All threshold variations (default, 1e-30, 1e-8, alpha=0.1) give
    inertia (71, 137, 1), residual 3.15e-2
  - Lower alpha helps the residual marginally (3.15e-2 → 2.43e-2) but
    inertia stays wrong

  Found a real bug in src/dense/factor.rs::count_2x2_inertia: the
  near-singular branch uses a00 to decide eigenvalue sign but the
  comment says "the other has sign of trace". For KKT matrices with
  zero-diagonal variable rows (a00=0), this miscounts. Attempted the
  trace-based fix:
  - On ACOPP30: no change (trace also 0, would give (0,0,2) not (1,1,0))
  - On bench: dense residual 152717 → 152701 (-16), sparse -1
  - Net regression. The buggy code is calibrated to rmumps. The fix is
    mathematically correct but the oracle is the one we're abandoning.

  Decision: revert, leave a KNOWN BUG comment, document in
  dev/tried-and-rejected.md, re-attempt after canonical MUMPS becomes
  the oracle. ACOPP30 is fundamentally a Phase 2 issue (delayed
  pivoting) — the BK strategy forms 2×2 blocks for zero-diagonal
  variable rows and one block is rank-deficient. No threshold tuning
  fixes it.

* 14:30 :triage:errinbar:misclassification:
  Built examples/triage_errinbar.rs. ERRINBAR_0824 (n=27, iter 824,
  delta_w=delta_c=0, expected (18,9,0), diagonal range 1e-8 to 1.5e12,
  κ ratio ≈ 1.5e20):

  | Path                          | refined residual |
  |-------------------------------+------------------|
  | Dense                         | 1.872e-4         |
  | Sparse default (1 supernode)  | 3.140e-4         |
  | Sparse single (nemin=10000)   | 3.140e-4         |
  | Sparse multi (nemin=1, 11sn)  | 2.319e-4         |

  All four within factor of 2 on a κ ≈ 1e20 matrix. Inertia (18,9,0)
  matches MUMPS sidecar in every case. Fundamental ill-conditioning:
  refinement helps but can't recover the lost ~12 digits.

  *Misclassification finding.* My previous "ERRINBAR_0824 is the worst
  sparse-only failure" claim was wrong. The bench's "Top 15 worst
  residuals" lists sort by magnitude, not by failure exclusivity.
  ERRINBAR_0824 fails dense too (1.87e-4 vs 6e-9 tolerance) but the
  ERRINBAR family is below the top-25 cutoff in the dense family
  table because per-family counts are dominated by ACOPP30 et al.

  ERRINBAR_0824 is in the "failed in BOTH" 1376-matrix set, not the
  "sparse-only 88" set. The 88 sparse-only matrices have not actually
  been triaged — I picked the wrong representative.

  This is fine. The consensus pass (Phase 5 of the exit plan) will
  characterize the 88 sparse-only matrices automatically: if canonical
  Fortran MUMPS and SSIDS agree with feral-dense on those 88 matrices,
  the sparse pipeline has real bugs to fix. If they all disagree, the
  88 are just borderline matrices where rounding accumulates differently
  in multifrontal vs monolithic factorization. The right tool for
  identifying which world we're in is the consensus framework, not
  individual triage.

  ERRINBAR_0824 itself is Category "Numerically intractable" — belongs
  in Borderline or Excluded under the consensus criterion. Both dense
  and sparse give the same answer to within ~30%, so neither is
  "wrong" — they're both at the precision limit of double-precision BK
  on a κ=1e20 matrix.

* 14:50 :phase-1-complete:
  Phase 1 of dev/plans/phase-1b-consensus-exit.md complete. All three
  scheduled triages done:
  1. POLAK6_0021 — fixed by threshold-mismatch fix (commit 95e6760)
  2. ACOPP30 family — Phase 2 issue (delayed pivoting), 2x2 inertia
     bug deferred (commit 1da42f4)
  3. ERRINBAR_0824 — fundamental ill-conditioning, not a feral bug

  Net effect on the corpus from this session:
  - Sparse inertia: 150997 → 152038 (+1041, +0.7%)
  - Sparse residual: 151626 → 152788 (+1162, +0.7%)
  - Sparse worst residual: 2.61e21 → 3.14e-4 (25 orders of magnitude)
  - Dense residual: 152489 → 152717 (+228)

  No more low-hanging fixes identified. Remaining ~400 residual
  failures are dominated by Category 2 (BK pivot quality on
  ill-conditioned matrices, requires Phase 2 delayed pivoting) and
  Category "Numerically intractable" (matrices like ERRINBAR_0824
  that are at the precision limit).

  Ready to proceed to Phases 2-5 of the consensus exit plan: build
  the native MUMPS and SSIDS oracles, run them on the 153k corpus,
  compute consensus verdicts, and use those to define the actual
  Phase 1b exit criterion.
