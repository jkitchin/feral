#+TITLE: Session journal 2026-05-19-01
#+STARTUP: showall

* 20:55 :near-singularity:ipm-integration:research:
Picked up the user request: pounce's FERAL backend cannot bump delta_w on
KKT systems that are ill-conditioned but land on correct inertia. Ipopt+MA57
solves three Mittelmann-class problems in 100-291 iters; pounce+FERAL stalls
because the perturbation handler never fires.

Root cause traced: FERAL's default ZeroPivotAction::ForceAccept
(src/dense/factor.rs:444,660) force-accepts a near-singular pivot, sets
needs_refinement=true, returns FactorStatus::Success. The only
near-singularity-adjacent fact reaching the IPM is needs_refinement -- which
is internal to SparseFactors, a coarse boolean, and already true on healthy
cascade-break factorizations. Not thresholdable, not specific.

MA57 reports this via CNTL(2) small-pivot threshold -> INFO(1)==4
(rank-deficient) -> Ipopt SYMSOLVER_SINGULAR -> PerturbForSingularity, a
distinct escalation branch from PerturbForWrongInertia. FERAL has no analog.

Concluded: FERAL must expose min|λ(D)|, the smallest accepted pivot
magnitude. Wrote dev/research/near-singularity-signal.md and
dev/plans/near-singularity-signal.md. Additive only -- no factor/solve
behavior change.

* 21:05 :near-singularity:api:
Implemented SparseFactors::{min,max}_pivot_magnitude over a shared
pivot_magnitude_extent() pass, mirroring the existing min_diagonal()
(factorize.rs:882). Key design points:
- 1×1 pivot magnitude is |d_diag[k]|.
- 2×2 block: larger magnitude (|t|+√disc)/2 is cancellation-free; smaller is
  |det|/larger since |λ₊·λ₋|=|det|. Avoids catastrophic cancellation on
  near-singular blocks.
- Distinct from min_diagonal(): that is signed-min (most-negative eigenvalue)
  for unconstrained inertia correction; near-singularity needs magnitude-min.
Solver delegates added. 4 tests in tests/pounce_interface.rs with hand
oracles:
  diag(5,-2,3,-7)  -> min|λ|=2, max|λ|=7, min_diagonal=-7
  [[0,1],[1,0]]    -> 2×2 block, min=max=1 (|smaller eig|, not d_diag[0]=0)
  diag(1,1e-14,-3) -> inertia (2,1,0) still correct, min|λ|≈1e-14,
                      ratio min/max≈3e-15 -- thresholdable; inertia silent
  None before factor
All 4 pass; cargo clippy --all-targets -D warnings clean; factorize:: 25/25.

* 21:18 :near-singularity:capi:
Added C ABI feral_min_pivot / feral_max_pivot (return -1.0 sentinel on
no-factor / null handle -- unambiguous since a magnitude is non-negative).
Declared in feral-ipopt-shim/include/feral_capi.h. Test capi_min_max_pivot:
sentinel before factor + on null handle; after factoring [[1,2],[2,1]]
(eigenvalues 3,-1) under FERAL_SCALING=identity the 2×2 D block gives
min|λ(D)|=1, max|λ(D)|=3 (hand oracle). capi:: 3/3 pass, clippy clean.

Pounce-side consumption (different repo, spec only in research note §4):
after FactorStatus::Success, if feral_min_pivot/feral_max_pivot <
singular_pivot_ratio (≈1e-12), return ESymSolverStatus::Singular ->
PerturbForSingularity -> delta_w. Keeps pounce's residual-ratio
pretend_singular path unchanged; adds the cheaper solver-reported trigger.

* 21:55 :ci:stress-smoke:cross-arch:f01:
Push of the 8-commit backlog turned CI red -- stress-smoke
report.py PR-blocking gate, flagged=3: rankdef_5_2, rankdef_50_5,
rankdef_exact_50_5 all "zero=0 (rankdef, expected>=1)". NOT from this
session's near-singularity work (the `check` job -- test/clippy/fmt --
passed green). Root cause: commit c7471ce (F-01 sign-fallback, #39,
2026-05-17) was committed locally but never pushed, so CI ran the
stress gate against it for the first time tonight.

Diagnosis: this is the #28 cross-arch BK-pivot divergence, exposed by
the #39 band widening -- NOT the deterministic every-arch F-01 flip
of the four #39-allowlisted matrices. Evidence:
- f01-rankdef-underreporting.md probe_f01.rs audit (aarch64): all
  three have 1 strict-zero pivot -> zero=1.
- CI x86: zero=0 (BK produces a slightly different pivot landing just
  above EPS; rankdef_5_2's pivot is logged at 9.3e-17, straddling
  EPS=2.22e-16).
- Local report.py rerun on aarch64 tonight: (2,2,1), (25,24,1),
  (24,25,1) -- all zero=1, flagged=0, EXIT=0. The divergence is
  purely x86-vs-aarch64.
- For rankdef_50_5, MUMPS (ICNTL(24)=1) itself reports zero=0
  (f01 note line 369 + classify() comment) -- x86 feral matches MUMPS.

Fix: three ALLOWLIST entries in external_benchmarks/stress/report.py
citing #28 (not #39 -- conflating a cross-arch divergence with the
deterministic F-01 flip would mislabel failure as intended behavior).
Each factor is numerically valid (rel_res <= 3.2e-15). Local report.py
EXIT=0 after the change; x86 CI will be the real confirmation.

Open item flagged to user, not folded in: classify()'s own comment
names rankdef_50_5 as a known MUMPS-zero=0 matrix yet still flags it
-- the gate's expected_zero for that matrix is arguably wrong and
should be corrected rather than allowlisted. And #28 is now a
4-matrix divergence; the issue should be updated to track the scope.

* 22:40 :ci:stress-smoke:allowlist:issue-tracking:
Investigated the two follow-ups. Correction: #28 is CLOSED and is the
M5 milestone "wire stress-suite smoke into CI" -- a placeholder, not a
bug tracker. The ALLOWLIST contract says "remove the entry when the
issue closes", so all four #28 citations (saddle_rankdef_50_10_3 plus
tonight's three) violated it. The saddle entry's own reason text even
said "(separate issue)" -- the original author knew #28 was wrong.

Fix: opened #40 "Cross-arch BK-pivot divergence on borderline
rank-deficient matrices" (label bug), documenting the x86/aarch64
EPS-straddle, the four affected matrices, evidence, root cause (BK
pivot selection is not bitwise-portable: FMA/SIMD reduction order
differs), and the real fix (portable reduction order or a band wide
enough that FMA-scale perturbations cannot flip the decision).
Re-pointed all four ALLOWLIST entries #28 -> #40; dropped the stale
"(separate issue)" phrase from the saddle entry. report.py syntax OK,
EXIT=0, flagged=0; saddle_rankdef_50_10_3 shows ALLOWLISTED(#40).

Follow-up 1 (rankdef_50_5 expected_zero is the gate's bug, not
feral's) NOT done -- it relaxes a gate criterion, so it waits on user
sign-off plus a re-run of the MUMPS ICNTL(24)=1 oracle to confirm
zero=0 before baking the value into classify().

* 23:30 :ci:stress-smoke:mumps-oracle:inertia:follow-up-1:
User approved follow-up 1. Before touching classify(), ran the real
MUMPS 5.8.2 oracle (ICNTL(24)=1 -- confirmed set at mumps_bench.F:109;
inertia_zero = INFOG(28)) on all 8 rankdef synth .mtx files via
external_benchmarks/mumps_oracle/mumps_bench, dummy all-ones RHS
(inertia is RHS-independent). Verified table:

  rankdef_5_2             (2,2,1)    zero=1
  rankdef_10_3            (3,4,3)    zero=3
  rankdef_50_5            (26,24,0)  zero=0   <- confirms the claim
  rankdef_200_20          (111,89,0) zero=0   <- confirms the claim
  rankdef_exact_50_5      (24,26,0)  zero=0   <- NEW: also zero=0
  rankdef_exact_100_10    (54,43,3)  zero=3   <- NOT zero=0
  saddle_rankdef_50_10_3  (50,37,3)  zero=3
  saddle_rankdef_100_20_5 (100,75,5) zero=5

So the MUMPS-reports-zero=0 set is THREE matrices, not two:
rankdef_50_5, rankdef_200_20, rankdef_exact_50_5. The f01 note and
the report.py comment only named the first two; rankdef_exact_50_5 is
a new finding from the oracle run.

Implemented: MUMPS_REPORTS_ZERO0 frozenset in report.py with the
verified inertia in the comment; classify() skips the zero=0 flag iff
the name is in the set; updated the classify() doc comment and README
acceptance rule 3. Removed three now-dead ALLOWLIST entries
(rankdef_50_5, rankdef_exact_50_5 were #40; rankdef_200_20 was #39).
report.py EXIT=0, flagged=0; rankdef_200_20 now passes via the
exemption (no longer ALLOWLISTED), the two cross-arch matrices pass
on aarch64 with zero=1 and will pass on x86 with zero=0. Allowlist
down to 5 entries (2x#40, 3x#39). decisions.md entry appended.

OPEN ITEM flagged to user, NOT folded in: the #39 ALLOWLIST entry for
rankdef_exact_100_10 claims "MUMPS (ICNTL(24)=1) itself reports zero=0
on this matrix". The oracle run above shows MUMPS reports zero=3 on
rankdef_exact_100_10 -- the entry's justification is factually wrong.
feral reports zero=0 there (intended #39 sign-fallback), so feral and
MUMPS-ICNTL(24)=1 genuinely disagree (0 vs 3). Whether feral zero=0 is
defensible (it matches *default* sign-counting MUMPS, the #39 parity
reference) or a real under-detection bug is a #39-scope question, not
follow-up 1. Left untouched; needs its own triage.

* 23:55 :ci:stress-smoke:mumps-oracle:inertia:issue-tracking:
Opened #41 for the open item above. Probed the MUMPS oracle on
stokes_q1p0_8 too (was not in the 8-matrix rankdef run): MUMPS
(98,62,2). So all three remaining #39 ALLOWLIST matrices show feral
zero=0 vs MUMPS-ICNTL(24)=1 zero>0:

  rankdef_exact_100_10    feral (55,45,0)  MUMPS (54,43,3)
  saddle_rankdef_100_20_5 feral (102,78,0) MUMPS (100,75,5)
  stokes_q1p0_8           feral (99,63,0)  MUMPS (98,62,2)

#41 records the verified table, the false "MUMPS reports zero=0"
justification on the rankdef_exact_100_10 entry, and the open
correctness question (intended #39 parity behavior vs under-detection
bug). Suggested first action in #41: run the SSIDS oracle -- if SSIDS
also sign-counts, feral has a consensus partner. Not folded in here;
#41 is its own triage. report.py ALLOWLIST entries still cite #39 --
left as-is, re-citing to #41 is part of that triage, not this task.

* 22:30 :ci:stress-smoke:issue-tracking:f01:doc-fix:
User approved the cheap unambiguous parts: close #39 and fix the
false #41 doc claim; the unified gate lower-bound change stays held
for sign-off.

Closed #39. The FBRAIN3LS_0839 outlier was fixed by c7471ce
(F-01 sign-fallback) on 2026-05-17; #39 stayed open only because a
direct push to main carries no "closes #39" keyword. Verified before
closing: cargo test --test parity parity_fbrain3ls_0839 -> ok (1
passed). Closing comment records root cause, fix, verification, and
the deliberate #39/#41 scope split (#39 = borderline real matrix
diverging from BOTH default oracles; #41 = feral matching
default-MUMPS/MA57 sign-counting but not explicit MUMPS
ICNTL(24)=1 null-detection on constructed rankdef synthetics).

Fixed the false claim in report.py's rankdef_exact_100_10 ALLOWLIST
entry. Old text: "MUMPS (ICNTL(24)=1) itself reports zero=0 on this
matrix" -- the oracle run (2026-05-19) showed MUMPS-ICNTL(24)=1
reports (54,43,3), zero=3. New text states the verified story
honestly: feral reports (56,44,0) zero=0 matching the MA57 oracle's
(56,44,0); MUMPS-ICNTL(24)=1 reports (54,43,3) zero=3; the two
genuinely disagree; the correct-oracle question is tracked in #41.
report.py EXIT=0, flagged=0 after the edit (rankdef_exact_100_10
still ALLOWLISTED(#39), reason text now accurate).

HELD, not done: dropping the rankdef gate lower bound 1 -> 0
(0 <= zero <= expected), which MA57 reporting zero=0 on all 8
synthetics would justify and which would retire MUMPS_REPORTS_ZERO0
and all 5 ALLOWLIST entries. That is a gate relaxation -- needs the
human sign-off CLAUDE.md requires.
