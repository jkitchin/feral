#+TITLE: Session 2026-08-10-01 — issue #128 remaining bundle
#+STARTUP: showall

* 00:40 :issue-128:orientation:
Issue #128 is a 5-part allocation-churn bundle. Checked each part against
the tree at =f7a152a= rather than trusting the issue body (written against
=660224d=):

- Item A (dense Schur pack buffers): *already landed*. =bpack1= laziness in
  =fc4e9b8= (session 2026-07-10-05), =apack=/=bpack0= pooling via =PackPool=
  on =FactorScratch= in =484bda7= (session 2026-08-09-01). The intra-front
  rayon path deliberately keeps per-range fresh allocations — recorded in
  the =pack_pool= doc comment, not an oversight.
- Item C (=DenseLu::update= clones): issue itself says to do it as part of
  the #115 dense-update rework, "not before". Out of scope here.
- Item D (postorder per-node Vecs): issue says lowest priority, "only if
  symbolic profiling still shows it after #124/#127".
- Items B and E: unaddressed. Those are this session's scope.

* 00:45 :issue-128:item-b:allocation:baseline:
Baseline from =tests/lu_update_alloc_probe.rs= (in-tree casctanks fixture,
m=2169, 3 segments, 144 updates):

: total: allocs=1610  reallocs=1190  bytes=860888
: per-update: allocs=11.2  reallocs=8.3  bytes=5978

The probe's own doc comment claimed "~82 allocs/update", which is stale —
later pooling rounds already brought it to 11.2. Corrected in the header.
11.2 is consistent with the issue's "~9 residual per-update allocations"
claim, so item B is real.

* 01:05 :issue-128:item-b:pooling:
Pooled all nine sites named in the issue, following the existing
=mem::take= / restore-on-every-exit-path discipline (=ft_rw=,
=saved_scratch=): =touched=, =supp=, =changed=, =saved_uperm_inv=,
=spike_stack=, =spike_reach=, =col_r_holders=, =elim_heap=, =elim_offdiag=.
Added =restore_update_pools= as the sibling of the existing
=restore_elim_pools=, and threaded =heap= through the latter — the heap
must be cleared on *restore*, not on entry, because a mid-sweep
=NeedsRefactor= exit leaves ranks queued.

Also fixed the rollback leak the issue mentions: =rollback= overwrote
=u_rows[i]= and dropped the post-update buffer. Now =mem::replace= + push
to =row_pool=.

Result: 11.2 -> 3.8 allocs/update.

* 01:15 :issue-128:item-b:allocation:measurement:
3.8 was higher than the ~1 the retained eta =ops= Vec should cost, so I
wrote a throwaway backtrace-attributing global allocator to name the rest
rather than guess. Three sites, nothing else:

: 225  sparse_update.rs:189  saved_pool.pop().unwrap_or_default() + extend
: 143  sparse_update.rs:705  ops.push(FtOp::Axpy{..})
:   6  sparse_update.rs:268  u_above[r].extend(..)

=ops= is retained in =self.etas= — unpoolable, and the issue names it as
legitimate. The other two are *working-set growth*, not churn: each
=saved_pool= buffer is allocated once ever and only when the changed-row
count passes the pool's high-water mark, which keeps rising because FT
fill grows along the chain. Per-segment first-half vs last-half confirms
the shape (seg2: 3.96 -> 1.96/update).

Attribution also caught a site the issue's list misses: the dense
=SparseLu::update= wrapper =collect()=s a fresh sparse entering column on
every call, and that wrapper is what the probe (and dense-column callers)
actually invoke. Pooled as =entering_scratch=. 3.8 -> 2.8.

Final: *11.2 -> 2.8 allocs/update (-75%), bytes 5978 -> 1521 (-75%)*,
reallocs 8.3 -> 6.3.

* 01:30 :issue-128:item-b:bit-exactness:validation:
The suite has no hardcoded bit oracle for the *LU update* path
(=golden_bits.rs= covers the dense symmetric factor only), so I built a
temporary before/after harness: replay the casctanks trace, digest the
raw IEEE-754 bits of =ftran= and =btran= solutions after every update, and
compare against the same run with =src/lu/{sparse_update,sparse_factor}.rs=
stashed.

: 1008 digest steps  OVERALL_DIGEST=496c17cb930c9e11  (identical)

The trace applies 144 updates with zero =NeedsRefactor=, so the rollback
change was uncovered by that alone. Added a strict-budget second pass
(=max_growth: 1.5=) to force it:

: strict seg0: rollbacks=0
: strict seg1: rollbacks=16
: strict seg2: rollbacks=3
: 1584 digest steps  OVERALL_DIGEST=12eb7d4263be3473  (identical)

Bit-identical on both passes, 19 rollbacks exercised. Full =cargo test=:
411 lib + all integration binaries, 0 failed. Clippy clean.

Tightened the probe's regression guard: the blanket bound stays at 250 for
any trace, plus a new 8.0 bound gated on the default in-tree fixture
(deterministic there). 8.0 is ~3x the measured 2.8 and below the 11.2 the
pre-change code produced on the same fixture, so un-pooling any of the
buffers fails the test.

* 02:10 :issue-128:item-e:supernode:measurement:
Item E: =find_supernodes= sets =nrow = col_counts[first_col].max(ncol)=.
Quantified the error before touching it, using
=SymbolicFactorization::static_rows(i).len()= (issue #125's static frontal
layout) as the truth oracle — an independent computation that
=tests/static_assembly_maps.rs= already pins against both a from-scratch
=BTreeSet= recompute and the numeric =build_row_indices= output:

: matrix         nemin  under  worst   sum_nrow  sum_true
: grid20x20        1       0      0       2254      2254
: grid20x20        8      49    -16        646      1071
: grid40x40       32      74    -41       1844      3083
: banded800b12     8      99     -7       1299      1988
: chainkkt1200     8      99     -2       2395      2593

Exactly the predicted shape: *zero* error at nemin=1 (no amalgamation —
fundamental supernodes have nested column patterns, so
=col_counts[first_col]= is exact), and a systematic *undercount* once
merging turns on, never an overcount. Worst summed case 1844 vs 3083, a
40% underestimate.

* 02:25 :issue-128:item-e:fix:
The issue suggests a seen-bitmap union during amalgamation, but
=find_supernodes= only has =col_counts=, not the pattern. Derived a closed
form instead. By Liu's elimination-tree containment,

:   struct(L[*,j]) \ {j}  subset of  struct(L[*,parent(j)]) union {parent(j)}

a merged group's row set is exactly the child's own (dense) column block
=[child_first, parent_first)= united with the parent group's row set, and
those are disjoint. So

:   merged_nrow = child_group_ncol + parent_group_nrow

which is the union *cardinality*, not a bound, and composes for chains
under both iteration orders. Implemented as a running =snode_nrow= vector
updated at each merge (=snode_nrow[root_p] += child_ncol=).

Re-ran the probe: *zero error on every supernode*, all 7 matrix families x
3 nemin values. The closed form is exact, so the bitmap is unnecessary.

Also updated the two =merge_flop_budget= guard sites (in =find_supernodes=
and the mirrored merge-bias predicate) to the corrected merged height.
They had the same understatement, which made merges look *cheaper* than
they are — the wrong direction for a guard meant to reject expensive
merges. The knob defaults to =None=, so the default path is unaffected.

* 02:40 :issue-128:item-e:validation:dispatch-flip:
Per the issue's instruction ("assert results identical while the
parallel/sequential choice may differ, and record any flips"):

*Numeric results identical.* Digested the raw IEEE-754 bits of every
front's =l=/=d_diag=/=d_subdiag= plus inertia, 9 matrices x 2 nemin.
All 18 =FACTOR= digests and all 18 inertias are byte-identical before and
after. =nrow= never reaches the numeric kernels; =build_row_indices=
recomputes the truth.

*Consumers corrected, as intended:*

: matrix          nemin   flops before -> after    par        peak before -> after
: grid20x20         8        54760 ->  142009    false          6512 ->  13448
: grid40x40        32      1389966 -> 3929494    false          3200 ->  81808
: grid60x60        32      4310558 -> 12216885   false -> TRUE  42912 -> 218320
: banded2000b6      8       129320 ->  391304    false           128 ->    864

*One dispatch flip recorded:* grid60x60 at nemin=32 crosses
=PAR_MIN_FLOPS= (1e7) and now routes to the parallel driver. That is
precisely the failure mode the issue predicted ("an underestimate flips
borderline matrices to sequential").

=peak_contrib_bytes= was the worse offender — grid40x40 nemin=32 was
underestimating contribution-block memory by *25x* (3200 vs 81808 bytes).

* 02:55 :issue-128:item-e:tests:
Two regression tests.

1. =issue_128e_merged_nrow_is_union_cardinality= (unit, supernode.rs).
   Hand-computed 5x5 fixture where the old proxy undercounts by one; the
   oracle is the hand-derived symbolic factor in the doc comment, and the
   test guards its own premises (=col_counts=, etree shape, fundamental
   layout) so a change elsewhere fails loudly instead of silently
   invalidating the hand computation. My first two attempts at this
   fixture had wrong premises — I dumped the actual structure and rebuilt
   the oracle from it rather than asserting what I assumed.
2. =tests/issue128_supernode_nrow.rs= (integration). =nrow == static_rows(i).len()=
   for every supernode across 14 fixtures x 3 orderings x 7 nemin values
   (>5000 supernodes, >500 merged), plus =nrow >= ncol= and the
   =row_indices.len() == nrow= invariant the F3.2b split asserts.

Confirmed the integration test has teeth: reverted =supernode.rs= and it
fails on the first merged front (=grid8x8 Amd nemin=2 supernode 2: nrow=3,
true 5=).

Full suite: 802 passed, 0 failed. Clippy clean.

* 03:15 :issue-128:item-d:profiling:gate:
Item D is gated by the issue on "only if symbolic profiling still shows it
after #124/#127". Measured before deciding, via
=SupernodeParams::symbolic_profiler= (best of 7 runs):

: matrix          n       total    postorder   renumber   share
: grid60x60      3600    3011us      205us      912us     37%
: grid120x120   14400   10286us      725us     3169us     38%
: chainkkt6000   6000    4125us      421us     1299us     42%
: chainkkt30000 30000   21182us     2291us     6590us     42%

Gate met: the postorder-bearing stages are 32-44% of symbolic time.
(=renumber= is not pure postorder — it re-permutes the pattern and rebuilds
etree+col_counts — but the merge-biased postorder is inside it.)

* 03:35 :issue-128:item-d:arena:
Replaced =EliminationTree::children()='s =Vec<Vec<usize>>= inside the three
postorder variants with a CSR child arena (=ChildArena=: =ptr= + =idx=,
built by counting sort over ascending j, so each parent's slice starts
ascending — identical to what =children()= produced).

The key observation making this safe: all three ordering rules
(=sort_children_by_size=, =merge_bias_partition=,
=schur_partition_children=) are pure functions of
=(slice, sizes, bias/is_schur)= and never depend on traversal state. So
every node's slice can be ordered *once, in place*, before the walk, and
the DFS stack collapses to =(node, cursor)=.

One subtlety worth recording: the two partitioning rules previously built
their groups with =iter().copied().filter(..).collect()=, which preserves
input order, and =sort_unstable_by_key= is only deterministic given a
fixed input sequence. An in-place *unstable* partition would feed the
sorts a different permutation and could silently change the emitted
postorder — i.e. the fill-reducing ordering. Implemented
=stable_partition= (with a reused scratch buffer) rather than the cheaper
two-pointer swap.

* 03:45 :issue-128:item-d:validation:
*Order identity.* Digested =perm=, =perm_inv=, every supernode's
=(first_col, ncol, nrow)=, =static_row_flat=, and =peak_contrib_bytes=
over 12 matrices x 3 orderings x 3 amalgamation strategies x 3 nemin,
plus the Schur-constrained variant:

: SYMBOLIC_IDENTITY_DIGEST=bd7e8146fbb6b0a0   (identical before and after)

*Allocations* (=tests/issue128_postorder_alloc.rs=, new):

: chain n=2000    before 4004    after 8
: chain n=4000    before 8004    after 8
: chain n=8000    before 16004   after 8

Exactly the predicted =2n= (one =children()= row per node + one sorted Vec
per stack push), now flat at 8. The star fixture was already 8 before —
=vec![Vec::new(); n]= does not allocate for empty rows, so only the chain
shape exposed the 2n. Worth knowing: a star-only probe would have measured
nothing.

*Timing* (best of 9):

: grid120x120    total 9800 -> 8621us (-12%)   postorder 843 -> 228us (-73%)
: chainkkt30000  total 20709 -> 16719us (-19%) postorder 2131 -> 519us (-76%)
:                                              renumber 6870 -> 4339us (-37%)

Confirmed the new probe fails on the pre-change code (chain n=8000: 16004
allocations vs the bound of 64).

Full suite: 803 passed, 0 failed. Clippy clean.
