* 13:00 :issue-10:gate-measurement:
User asked "fix issue #10" (APP path). Per the FERAL feature
lifecycle, research first. Found that the issue's own posted
re-open comment sets a measurable gate — "fresh diag_supernode_cost
shows ns/nnz dominates ns/sup on a relevant cluster." Ran the binary
to check the gate before any code work.

* 13:05 :issue-10:diag-supernode-cost:
~cargo run --bin diag_supernode_cost --release~ on post-d7267fe.
Result: across every corpus row and every nemin in the ACOPR30
sweep, ns/sup exceeds ns/nnz by 4× to 36×. ACOPR30_0067 nemin=32
(the cluster the gate names explicitly): ns/sup=943, ns/nnz=61
— ratio 15× the wrong way. HAIFAM_0082 (widest corpus fronts,
max 86): ns/sup=1174, ns/nnz=33 — ratio 36× the wrong way.

The gate condition is the opposite of what the data shows. APP
work is not justified today by the issue's own re-open criterion.

* 13:15 :issue-10:stale-motivation:
Re-traced the motivating gap. The 89 ns/nnz_L figure in
dev/research/mumps-small-frontal-speed.md is stale on the current
build:
- ~fused_gamma0~ landed ad05ff4 (2026-04-11) — eliminates per-pivot
  γ₀ scan on scalar's no-swap branches, the same trick the issue
  attributes uniquely to MUMPS MAXFROMM.
- 32×32 SIMD body landed d3f1132 (2026-05-13) — quad pulp dispatch
  for trailing updates on the dominant CHAINWOO front shape.

The two big mechanisms the APP proposal targeted are already in
place via different code paths.

* 13:25 :issue-10:closure:
Wrote dev/research/dense-app-path.md documenting the gate
measurement and design space. Appended dev/decisions.md entry
recommending closure. Appended dev/tried-and-rejected.md entry
recording the not-undertaken work and the lesson.

Lesson reinforced (same as 2026-05-12 (c) BLAS-3 quad parking):
the session-checkpoint "Next session should" list is not a
substitute for re-measuring the gate. The previous session
advanced #10 as the next target on the strength of #9 landing,
without re-running diag_supernode_cost. One binary run was the
difference between writing dead code and recording a clean
closure.

* 18:45 :issue-8:fma:phase-2:
Phase 2 plumbing landed: NumericParams.fma, Solver::with_fma builder,
and dispatch shims through 6 helpers in factor.rs + 2 in
block_ldlt32.rs. New tests/fma_opt_in_roundtrip.rs constructs a
112x112 arrow saddle KKT (96 SPD diagonal block, 16 dense constraint
rows; expected inertia (96, 16, 0)), factors it twice through the
Solver builder (default vs .with_fma(true)), and asserts:
- both produce identical inertia
- both back-solves achieve |Ax-b|_inf / |b|_inf < 1e-10

Mechanics: do_1x1_update / do_2x2_update / peek_ahead_column /
peek_ahead_replay / apply_blocked_schur / apply_blocked_schur_panel
all take fma: bool as the last param; each branches `if fma { ...
fma kernel ... } else { ... nofma kernel ... }` at every Schur call
site. block_ldlt32::update_1x1_block32 and update_2x2_block32 do
the same. Entry points that already hold &BunchKaufmanParams pass
params.fma into the helpers.

Test counts: 451 passed, 0 failed, 27 ignored under cargo test
--release. cargo clippy --release --all-targets -- -D warnings
clean (added two #[allow(clippy::too_many_arguments)] for the now
8-arg peek_ahead_replay and apply_blocked_schur). 19 unrelated
struct-literal sites in tests/ and src/bin/ updated to set
`fma: false` (the field is non-optional).

Next: Phase 3 verification — rerun the Mittelmann pinene_3200 NLP
with .with_fma(true) and compare wallclock + inertia vs the default
path. Then Phase 4 release (CHANGELOG, decisions, bump to 0.3.1).
