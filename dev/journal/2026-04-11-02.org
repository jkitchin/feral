* 14:50 :testing:property:
  Added 6 property-based tests with deterministic xorshift64 PRNG.
  Tests cover random SPD, indefinite, KKT, badly-scaled matrices at
  sizes n=2..50. Each test verifies: inertia sums to n, permutation
  validity, L unit lower triangular, D block structure, P·L·D·Lᵀ·Pᵀ
  reconstruction, and solve residual.
  Evidence: all 6 pass. Random KKT reconstruction tolerance needed to
  be generous (1.0) due to κ≈1e8 amplifying O(n³) check error.

* 14:55 :testing:stress:
  Added 8 stress tests: LAPACK extension (Test 6) branch, arrow matrix
  (n=20), all-2×2-pivots (n=10), large SPD (n=50), large indefinite
  (n=100), extreme scaling (1e-8 to 1e8, n=15), tridiagonal (n=30),
  and KKT trajectory with δ from 1e-2 to 1e-10 (n=7).
  Evidence: all pass. LAPACK extension test confirmed matrix is
  indefinite (3,1,0) not SPD — corrected assertion. Extreme scaling
  test needed solve_refined and loose tolerance (1.0) due to κ≈1e16.

* 15:00 :optimization:fused:
  Implemented fused update+argmax per research note Section 6.3.
  Rank-1 and rank-2 updates now track the column-0 off-diagonal max
  of the NEXT column during the first-column update pass. Main loop
  uses fused values when available, falls back after swaps.
  Evidence: all 31 tests pass unchanged — correctness preserved.
  No performance measurement yet (need larger matrices for meaningful
  comparison).

* 15:05 :benchmark:
  Wired benchmark harness with 8 built-in matrices (4 SPD + 4 KKT).
  Release-mode results:
    spd_10:     factor 8μs,    solve 17μs
    spd_50:     factor 80μs,   solve 5μs
    spd_100:    factor 308μs,  solve 8μs
    spd_200:    factor 1273μs, solve 27μs
    kkt_10_3:   factor 10μs,   solve 1μs
    kkt_30_10:  factor 40μs,   solve 2μs
    kkt_50_15:  factor 101μs,  solve 4μs
    kkt_100_30: factor 543μs,  solve 12μs
  All inertias correct. Scaling: factor is O(n³/3) as expected.
  spd_200 factor ≈ 1.3ms — reasonable for unblocked scalar kernel.
