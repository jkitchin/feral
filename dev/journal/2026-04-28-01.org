#+TITLE: Journal 2026-04-28-01
#+STARTUP: showall

* 06:30 :perf:profiling:bench:
User asked whether we should adopt faer to speed up feral. Before
answering, profiled the current bench to see where time actually
goes. Built `src/bin/profile_hot.rs` looping 7 representative KKT
matrices (HS118, BATCH, AVION2, HAHN1, VESUVIO, CRESC132 + BATCH_0500)
through the full pipeline x200 reps. Stage breakdown via hand
timers:

  sym=64.4%  factor=31.7%  solve=3.9%

Sampled with `samply record --rate 4000` (after `cargo install samply`
and `[profile.release] debug = true`). Symbolicated via atos against
the dSYM. Top inclusive functions in the profile_hot binary:

  scaling::mc64::compute_matching     26.13%
  symbolic::run_external_ordering     15.32%  (METIS ND)
  ordering::postorder::postorder      10.49%
  dense::factor::do_1x1_update        14.82%
  dense::factor::factor_frontal_blocked_in_place  11.22%
  dense::schur_kernel::axpy_minus_unroll4_nofma   6.36% (self)

Top SELF was `axpy_minus_unroll4_nofma` at 6.36% — the actual SIMD
inner kernel, already on faer's pulp primitive.

Conclusion: faer cannot fix this. 51%+ of wall is graph algorithms
(MC64 + METIS + postorder) that faer does not touch. Dense kernel
upside ≤ 12% wall, half of that already captured by pulp.

* 07:00 :perf:investigation:agent-team:
Dispatched four parallel research agents:
- spral-expert: SSIDS MC64 default policy
- mumps-expert: MUMPS MC64 default policy and ICNTL(8) fall-through
- Explore: feral MC64 wiring + symbolic stages + prologue map
- Explore: prior-art search across decisions/tried-and-rejected/
  research/journal for the three proposed interventions

Findings:
- SSIDS defaults to NO scaling (`spral/src/ssids/datatypes.f90:233`).
- MUMPS SYM=2 defaults to MC64 BUT has a key skip rule: when
  `(missing_diag + zero_diag) < N/10` it skips MC64 and falls
  through to cheap symmetric Ruiz equilibration (SIMSCA), at
  `mumps/src/dana_aux.F:1388-1416`. This is the canonical
  KKT-aware skip.
- feral has Policy 4 fallback (correctness, not perf gating)
  and an `Auto` route that picks MC64 when `diag_only/n >= 0.3`
  (arrow detection); it does NOT have MUMPS's inverse "skip when
  diagonal is mostly populated" test.
- feral already has a `Solver` struct in src/numeric/solver.rs
  that caches symbolic across re-factorizations of the same
  pattern — `bench_solver_reuse.rs` exercises it on 4 hardcoded
  families. The pattern fingerprint is weak (n + col_ptr.len +
  row_idx.len) but works for the IPM use case.
- The per-matrix `bench` walks 154k matrices via the FREE-FUNCTION
  API and recomputes symbolic on every call. This explains the
  64% sym share in profile_hot — it's an artifact of the bench
  harness, not real production cost.

* 07:20 :perf:bench:solver-corpus:
Wrote `src/bin/bench_solver_corpus.rs` to measure the realistic
IPM-tail picture: walk the full kkt corpus, group by family
(`<FAM>_NNNN.mtx` → `FAM`), per family run two scenarios — one
persistent Solver and free-function loop — and report per-family
plus aggregate speedups.

Smoke test on 12 named families:
  geomean speedup 1.89x  p10 1.45x  p50 1.79x  p90 3.40x  max 3.45x
  implied symbolic share of freefn wall: 30.8%

Full corpus run (534 families, 19410 iterates, cap=64 per family):
  aggregate speedup: 1.70x
  geomean speedup:   2.86x
  p50: 3.00x  p90: 4.08x  max: 6.41x
  symbolic calls:    986 (vs 19410 free-fn = 19.7x fewer)
  implied symbolic share of freefn wall: 41.3%

The conservative pattern-fingerprint catches genuine pattern changes
within a family (e.g. VESUVIO: 22 sym calls in 64 iterates because
NNZ varies between iterates), but reuse rate is still ~20x on average.

Bottom 10 families by speedup show small/medium matrices where
either symbolic is small relative to numeric (VESUVIO 1.00x), or
the Solver bookkeeping costs more than it saves (BENNETT5 0.82x,
worth a future investigation but not material — single outlier in
534).

* 07:40 :perf:conclusion:
The bench harness (`src/bin/bench.rs`) is measuring an unrealistic
workload. The right perf gate going forward is `bench_solver_corpus`.
Optimization effort that targets symbolic-phase cost should be
evaluated against the realistic 41% sym share, not the artificial
64% from per-matrix free-function loops.

Did NOT modify `bench.rs` — the per-matrix walk is still useful
for inertia/residual correctness sweeps and oracle ratio
comparisons on individual matrices. Both bench harnesses now
co-exist; `bench_solver_corpus` is the new perf-tuning ground truth.

Next session candidates (in order of estimated wall savings):
1. Implement MUMPS's missing-diagonal MC64 skip (canonical KKT
   gating; cite mumps/src/dana_aux.F:1411). Estimated 15-20%
   wall savings on freefn bench, 5% on solver-corpus bench.
2. Strengthen PatternFingerprint with XXH3(col_ptr || row_idx).
   Eliminates the false-cache-hit footgun without changing
   throughput on legitimate IPM workloads.
3. Profile bench_solver_corpus with samply to see what dominates
   *after* symbolic is amortized. The dense kernel + assembly
   should be more visible.

* 09:00 :perf:probe:mc64:abandoned:
Tackled the planned MUMPS missing-diagonal MC64 skip. Built a
throwaway probe (`probe_missing_diag`) to size the threshold and
found the literal rule mistranslated to feral's regime: 501 of
569 corpus families would-skip, of which 289 are arrow-KKT that
empirically WIN on MC64 (lever-C work). Reason: feral's KKT
corpus stores explicit dual regularization (-1e-8 on VESUVIO,
slack barrier perturbations on HS118), so `missing+zero diag`
is ~0 on every matrix. MUMPS designed the rule for raw
unregularized SYM=2 inputs.

Discussed with user — they asked whether the corpus is even
representative of how feral will be used or if we are skewing
development. Pivoted to a δ_c sensitivity probe.

* 09:30 :perf:probe:deltac:robustness:
Built `src/bin/probe_deltac_sensitivity.rs`. Detects the dual
reg block as the longest contiguous tail run of identical
small-magnitude diagonals (|x| ≤ 1e-2, rel-tol 1e-4, min run 4).
For each multiplier in {1e-4, 1e-2, 1, 1e2, 1e4}, perturbs the
detected entries and reruns `pick_scaling_strategy`,
`pick_ordering_preprocess`, and a 5-run-median symbolic + numeric
+ refined-solve. 11 matrices probed, 9 with detectable dual block
(HS118 and KIRBY2 had heterogeneous slack diagonals — no clean
δ_c).

Result: 0/9 matrices flipped routing across an 8-order δ_c span.
Inertia stable. Wall time ±5% (within noise). Residuals scale
with effective δ_c as expected for refined-solve on a more
singular matrix.

Conclusion: Auto routing is δ_c-robust by structural signature
(arrow-KKT detection and low-degree compression both gate on
column-degree ratios, not magnitudes). The corpus's specific
δ_c choice does not skew the routing logic; a downstream
consumer with different mu_init / regularization should see
identical routing answers.

Recorded as a `decisions.md` entry. The MUMPS rule is recorded
as a `tried-and-rejected.md` entry citing the regime mismatch.
The throwaway `probe_missing_diag` binary was deleted; the
useful `probe_deltac_sensitivity` is committed as the basis for
the decision.

* 10:15 :refactor:solver:fingerprint:
Strengthened `PatternFingerprint` in `src/numeric/solver.rs`.
Old structure was `(n, col_ptr_len, row_idx_len)` — three integers
that collide silently on any two matrices with the same (n, nnz).
New structure is `(n, nnz, structural_hash: u64)` where
`structural_hash` is a `DefaultHasher` (SipHash) of `col_ptr ||
row_idx`. The IPM hot path is unaffected because successive
iterates have byte-identical column structure and the equality
test short-circuits on the prefix; this just closes the footgun
for general callers who might hand `Solver` two different
matrices with identical (n, nnz).

Added 3 tests: F1 byte-identical patterns equal; F2 same (n,nnz)
different `row_idx` distinct; F3 same (n,nnz) different `col_ptr`
distinct. All 8 solver unit tests pass; full suite stays 205/205.
Committed as 8b0c435.

* 10:45 :perf:profile:samply:bench-corpus:
Re-ran samply on `bench_solver_corpus` with --unstable-presymbolicate
to get DWARF-resolved frames. 9039 samples at 1kHz over the full
534-family bench (~9 s wall). Self-time category breakdown
(persisted to `dev/profiles/bench_solver_corpus_2026-04-28.summary.txt`):

| Category                             |  self% |
|--------------------------------------+--------|
| I/O (mtx read+parse)                 |  35.9% |
| Allocator / memset / free            |  15.3% |
| Symbolic / ordering / postorder      |   9.5% |
| Dense Schur SIMD kernel              |   8.5% |
| Scaling (hungarian/mc64/infnorm)     |   7.3% |
| Dense factor (frontal+pivot)         |   6.8% |
| Multifrontal assembly                |   2.6% |
| (other)                              |  14.0% |

Inclusive: `Solver::factor` 21.8%, `factorize_multifrontal*` 25.6%,
`symbolic_factorize_with_method` 25.9%, `read_mtx` 33.6%.

Read of the data:
1. **I/O dominates the harness** — 36% wall on file read+parse is
   inherent to the benchmark, not the solver. On a real IPM
   workload where the matrix is already in memory, this 36% goes
   to zero, and the remaining solver wall would be dominated by
   the dense kernel and the multifrontal frontal factor.
2. **MC64 is no longer the top scaling cost** — `mc64::compute_matching`
   self-time is 0.7%, inclusive 7.7%. The hot scaling work is now
   `hungarian_match` (2.9% self) which is called *from inside* MC64
   (one of two augmenting-path passes) plus `compute_infnorm` (2.9%)
   for the InfNorm path. The session-01 finding ("MC64 is 26% of
   wall on the per-matrix bench") doesn't reproduce here precisely
   because `bench_solver_corpus` exercises the Solver-cache
   amortization path: 988 symbolic calls vs 19410 free-fn calls,
   19.6× fewer.
3. **Dense Schur kernel is the single biggest hot kernel** — 8.5%
   self combined across `axpy_minus_unroll4_nofma` (5.2%),
   `axpy2_minus_unroll4_nofma` (1.9%), and
   `schur_panel_minus_nofma_strided_dual` (1.4%). The W-2 2x2
   inline + B-1 dual-column DSYRK work that landed in the prior
   two checkpoints is paying off in `factor_frontal_blocked_in_place`
   (4.7% self / 14.7% inclusive).
4. **Allocator activity is large** (15.3% combined for malloc,
   free, memset, memmove). Some of this is benchmark harness
   churn (per-iterate `CscMatrix::from_triplets` builds new vectors);
   some is workspace allocation inside `factor_one_supernode`.
   Future lever: pre-size the workspace once per Solver lifetime
   and reuse across iterates. Not actionable today without a deeper
   look — the workspace path was already refactored in session
   2026-04-21.

Aggregate harness output (unchanged from prior bench): 534
families, 19410 iterates, **1.67x aggregate Solver vs free-fn**,
**geomean 2.83x**, p50 3.00x, p90 4.05x. Implied symbolic share
of free-fn wall: 40.2%, fully amortized by Solver.

Conclusion for the session-01 question "what dominates after
symbolic is amortized?": the dense per-supernode factor + Schur
kernel together account for ~22% of wall (combined dense self),
with the remaining numeric cost split between scaling (7.3%) and
multifrontal assembly (2.6% self / 19.1% inclusive). The biggest
remaining lever is continuing to widen the dense kernel
(Phase 2.4.x track).

Profile JSON saved at
`dev/profiles/bench_solver_corpus_2026-04-28.json.gz` (+ syms).

* 14:30 :perf:dense-kernel:attribution:bail-reasons:
Walked the 2026-04-28 samply stacks to attribute leaf
axpy_*_unroll4_nofma samples to callers (n=9039). Result:

| caller                            | wall % |
|-----------------------------------+--------|
| do_1x1_update (scalar fallback)   |  3.6%  |
| do_2x2_update (scalar fallback)   |  1.0%  |
| peek_ahead_replay (panel)         |  1.5%  |
| factor_frontal blocked (Phase A)  |  0.8%  |
| schur_panel...strided_dual (NR=2) |  1.4%  |

Combined scalar fallback = 4.6% wall. Combined panel work =
2.4% wall (excl. dual kernel). Best-case NR=4 widening saves
1.4% * 0.5 = 0.7% wall. Scalar fallback is 6.6x larger lever.

Built diagnostic: PANEL_DIAG_ENABLED atomic gate + 10 counters
(panel_full/partial/delayed, fallback_2x2_{need_swap,swap_1x1_wins,
lapack_1x1_wins,growth_or_det}, scalar_tail_steps, pivots_inline,
pivots_scalar). Wired at 4 panel bail sites in lblt_panel_frontal,
panel exit in factor_frontal_blocked_in_place, post-fallback scalar
step, and scalar tail branch. Probe binary
src/bin/probe_panel_attribution.rs runs across 10 representative
matrices.

Probe output (aggregate over 9 matrices that succeeded; CHAINWOO
factor failed):

  pivots: in=491 (86.1%) scal=79 (13.9%) tail=49
  panels: full=16 part=23 dly=0
  bails(2x2): swap=19 (82.6%) w11=2 (8.7%) lpk=2 (8.7%) g/d=0 (0.0%)

**83% of panel bails are swap-required 2x2** (r != col+1). This
is exactly the case Phase A explicitly deferred per
dev/plans/dense-kernel-blas3.md ("Out of scope: 2x2 with symmetric
swap, still ScalarFallback").

Per-matrix breakdown:
- VESUVIA, HS118, BATCH: 100% inline, no bails. Clean.
- ACOPR30: 84.6% swap-2x2 bails. Strongest signal.
- VESUVIO: 0% inline, 100% fallback. Worst case.
- HAHN1, AVION2, CRESC100, CRESC132: mixed but swap-2x2 dominates.

Decision: defer NR=4 dual widening. Next dense-kernel lever is
Phase A2 — extend lblt_panel_frontal to handle swap-required 2x2
inline. Growth + det checks already exist; new work is the
symmetric swap in-place inside the panel preserving the
deferred-Schur invariant.

Estimated wall impact: ~2-3% (eliminates do_2x2_update + most
post-bail do_1x1_update sweeps for ~80% of bails). Need a real
plan + research note before implementation.

Committed as 97c16e5 (research note + diagnostic + probe binary +
Cargo.toml entry).

* 15:00 :plan:dense-kernel:phase-a2:
Wrote dev/plans/dense-kernel-w2-2x2-swap.md and supporting research
note dev/research/dense-kernel-w2-2x2-swap.md for Phase A2 (swap-2x2
inline). Key argument: swap_rows_cols already operates correctly on
the deferred panel state because (a) its row swap loop ranges
j in 0..p which covers the full committed L block, and (b) the
symmetric swap commutes with the rank-c update by Π·M·Π^T algebra.

The new precondition is two-column peek-ahead: both col+1 and r must
be brought to scalar-state before the swap, because both are read
during gamma_r, swap-1x1 reject, and growth/det checks.

Plan sequenced as: (1) tests-first with three new fixtures
(bare swap-2x2, chain, swap-then-1x1), (2) no-op refactor to thread
&mut perm into lblt_panel_frontal, (3) impl + new INLINE_2X2_SWAP_OK
counter, (4) re-profile.

Estimated wall impact 2-3% aggregate, ~10-20% on ACOPR30. Committed
as 19bb815.

Tasks 10-14 in TaskList track the implementation steps.
