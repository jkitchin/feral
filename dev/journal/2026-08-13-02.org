#+TITLE: Journal 2026-08-13-02
#+STARTUP: showall

* 15:05 :issue161:lu:orient:
Goal for this session: issue #161 part B — hyper-sparse triangular solves.

Issue #161 has two halves. Part A (numeric factorization 1.6-3.9x SuperLU on
real LP bases) is already implemented and measured in PR #160
(Suhl-Suhl triangularization + dense-bump splice, open against main, 3.39x on
the bump-heavy class). Part B (triangular solves not work-proportional) is
filed as "described, not implemented" — the issue explicitly says the numbers
size the prize but do not demonstrate a fix. That is the gap to close.

Not touching part A: PR #160 is open and measured; duplicating it here would
conflict.

* 15:20 :issue161:lu:codeinspection:
Read the four triangular kernels in =src/lu/sparse_solve.rs=. They split into
two shapes, and the split explains the issue's headline number exactly:

- =lsolve= (L y = s) and =ut_solve= (U^T z = s) are *scatter* form. They test
  =s[k] == 0.0= and skip, so their flop count is already proportional to the
  solution.
- =usolve= (U w = s) and =lt_solve= (L^T v = s) are *gather* form. Every row
  of U / every column of L is read regardless of what is nonzero:
  =O(nnz(U))= and =O(nnz(L))= on every call.

Issue #161 measured "0.74x the cost for 0.0003x the output". Half the solve
going to ~0 and half staying at full cost predicts ~0.5x-0.75x. Consistent.
The gather halves are the target.

Third finding, not in the issue: =ut_solve= dereferences =u_rows[i]= (one heap
allocation per row) to read =row.first()= *before* testing =s[i]=. So although
its flops are proportional, it pays m cache misses per call regardless of
sparsity. =lsolve= already hoists its zero test; =ut_solve= does not. That is a
one-line fix independent of everything else.

* 15:35 :issue161:lu:design:
The predecessor graphs the reach needs:

- =usolve=: edges =c -> {k : U[k,c] != 0}=. That is exactly =u_above[c]=,
  which already exists (built at =sparse_factor.rs:486=, maintained
  incrementally across FT updates at =sparse_update.rs:236=, and invariant-
  checked in =verify=). Nothing to build.
- =lt_solve=: edges =i -> {k : L[i,k] != 0}= — the row-wise structure of L.
  L is stored CSC only. Must be built. Safe to build once at factor time
  because the FT update never touches the base L (documented at
  =sparse_factor.rs:41=).

Topological order: descending =uperm[k]= (triangular rank) for usolve —
*not* position index, since U is upper triangular in uperm order after an FT
update — and descending position for lt_solve.

=compute_spike= (=sparse_update.rs:335=) already does this exact shape for the
L graph: mark array, explicit stack, =sort_unstable=, clear marks over the
touched list. Following that idiom rather than inventing a second one.

Research note written to =dev/research/hyper-sparse-solves-2026-08-13.md=
before touching any code, per protocol.

* 16:40 :issue161:lu:bug:tests:
The sweep counter earned its keep on the first run. All nine differential
tests passed on correctness and =reach_route_agrees_with_dense_route_on_unit_rhs=
failed on the vacuity assertion:

  the reach route must have fired on most of the 400 ftran + 400 btran
  sweeps, got 0

Cause: =reach_cap()= tested =self.reach.mark_len() != self.m= to decide whether
the workspace was live — but =ftran_core= =mem::take=s the workspace out of
=self= for the duration of the call, so =self.reach= is the empty =Default= at
exactly the moment the test runs. The route was disabled on every solve and
every correctness test still passed, because the dense fallback is correct.

That is the whole argument for =hyper_sparse_sweeps()= existing. Without it
this ships as a no-op with a green suite.

Fixed by testing the taken-out =rw= instead.

* 16:55 :issue161:lu:measurement:
First interleaved A/B, m=4000, 400-col bump, unit-vector rhs (1280 calls/arm):

  dense sweep     ftran mean 68.3  p50 60.1 us | btran mean 57.5  p50 51.3 us
  reach-limited   ftran mean 35.0  p50 23.5 us | btran mean 33.5  p50 23.9 us
  -->             ftran 1.95x mean / 2.56x p50 | btran 1.72x / 2.15x
  dense-rhs fallback 0.97x   max |diff| = 0.000e0

Bit-identical between routes, which is expected: the omitted terms are exactly
=v * 0.0=.

Solution density is bimodal on this fixture — ftran(e_i) has p50=3 nonzeros and
p90=366 — so the mean is set by the minority that reach through the bump and
understates the hyper-sparse case. Reporting both, not the flattering one.

* 17:10 :issue161:lu:measurement:threshold:
Swept =hyper_sparse_max_density= to choose the default rather than guessing:

  0.05  2.08x   3.1 nodes/sweep   fallback 0.98x
  0.10  2.06x  32.8               0.97x
  0.25  2.04x  39.5               0.98x
  0.50  2.04x  39.5               0.93x
  1.00  1.98x 133.8               0.67x

The win is flat — the solves that matter have reaches far below any of these.
What the cap controls is the *downside*, and 1.00 (where nothing can ever abort)
shows what that is worth: 0.67x on a dense rhs. Shipping 0.25.

* 17:30 :issue161:lu:negative:
Chased the residual O(m). A hyper-sparse ftran at m=4000 still costs ~22 us with
a reach of 3.4 positions, so I hypothesised the four permuted gather/scatters
around the core solve (all m accesses are random through a permutation) and
replaced them with fill + sequential scan + one random access per nonzero.

It does nothing. A/B toggling only that gate:

  sparse marshal   ftran 33.6  btran 31.6  fallback 0.90x
  dense  marshal   ftran 31.3  btran 33.0  fallback 0.93x

The arms straddle each other. Reverted; written up in tried-and-rejected.md.

The hypothesis was wrong because the permuted access is random *within 32 KB*,
which is L1/L2-resident — it was never paying the misses I assumed. A phase
probe shows the residual spread evenly over ~6 linear passes:

  loop base (fill)                 0.2 us
  ftran_partial (P-gather+lsolve) 10.3 us
  ftran - ftran_partial           13.2 us
  full ftran                      22.1 us

There is no single term left. Getting under the O(m) floor needs a sparse-rhs
entry point, i.e. an API addition, not a cheaper walk of a dense one.

* 17:50 :issue161:session:tests:
Full suite green: 40 test binaries, 0 failures, =cargo test --release=.
=cargo clippy --all-targets -- -D warnings= clean, =cargo fmt= clean.

=cargo run --bin bench --release= runs but the container has **no corpus** —
every partition reports N/A / "no matrices have oracle timings", same as
session 2026-08-10-01. No LDLT regression comparison is possible here. The
change is confined to =src/lu/=, which the LDLT corpus does not exercise, but
that is an argument for low risk, not evidence of no regression.

* 18:40 :issue161:lu:api:design:
Follow-up requested: remove the O(m) floor, adding an API if that is what it
takes. It is: the floor is the =ftran(&mut [f64])= signature, not the kernels.

Added =ftran_sparse= / =btran_sparse=: rhs as (index, value) pairs, solution
appended to a caller-owned =Vec<(usize, f64)>=. Every step is proportional to
nnz or to the reach — scatter O(nnz(rhs)), L-solve over the GP reach, etas
O(ops), U-solve over the reach, gather O(nnz(x) log nnz(x)).

The delicate part is the dense accumulator that must be all-zero between calls
and restored in O(touched). A leak corrupts the *next* solve, not the one that
leaked, so the reset runs on every exit path including the error path out of the
U-solve, and the test interleaves failing solves with good ones rather than
batching them.

Deliberately did NOT skip zero-source eta ops. It would be O(ops) either way,
and skipping diverges from =FtEta::apply_forward= when mult is non-finite
(mult * 0.0 = NaN, not 0.0). Bit parity with the dense path is worth more than
a branch that changes no asymptotics.

* 19:05 :issue161:lu:measurement:scalability:
Measured, bump=0 band=3, unit-vector rhs, ftran p50:

  m       dense sweep   reach-limited   sparse API   work (scalar ops)
  1000       7.8 us        4.7 us        0.30 us     12.5
  4000      40.8 us       20.3 us        1.07 us     12.1
  16000    183.4 us       89.6 us        2.47 us     13.1
  64000   1251.0 us      616.7 us        3.34 us     11.0

*The work column is the result.* m grows 64x, the operation count does not move.
The dense sweep grows 160x, which is what O(m) looks like. At m=64000 the sparse
API is 277x the dense sweep on ftran, 234x on btran.

Time still grows 11x over that 64x range. That is NOT a residual O(m) term — it
decelerates (4x in m buys 2.3x in time from 4k->16k, 1.35x from 16k->64k), which
is a cache/TLB plateau. The accumulator is 512 KB at m=64000, past L2, so each
of the ~12 scattered accesses is a miss. O(work x miss cost), not O(m).

This is exactly why =last_sparse_solve_work()= is asserted on instead of a
timing. A reintroduced O(m) term at these sizes would look like a constant
factor, indistinguishable from noise. The counter is deterministic.

* 19:20 :issue161:lu:measurement:honest:
On the *bump* fixture the sparse API is ftran p50 0.83 us vs 57.3 us dense
sweep (69x) but *mean* 9.6 us vs 62.8 (6.5x). The minority of solves that reach
through the whole bump dominate the mean, and there the sparse API does the same
work as the dense sweep plus a sort.

That is the honest shape: enormous where the solution is genuinely sparse,
roughly neutral where it is not. Callers with dense solutions should keep using
=ftran=; the doc comments say so explicitly rather than leaving it to be
discovered.

* 19:35 :issue161:python:tests:
Added the Python bindings — issue #161's own reproducer is Python, so a
Rust-only fix is not reachable from it. =ftran_sparse(rows, vals) -> (rows,
vals)=, =btran_sparse=, =last_sparse_solve_work=, and
=hyper_sparse_max_density= threaded through the constructor.

Built with maturin and ran the suite against the installed wheel: 68 passed,
1 skipped, including three new tests (dense-oracle agreement, the
work-does-not-grow-with-n assertion through the binding, and dense-engine
rejection).

Note for future sessions: =python -m pytest= must be run from *outside*
=python/=, since the source =feral/= directory shadows the installed package
(ImportError: cannot import name '_feral' from partially initialized module).

Rust suite: 823 tests across 87 binaries, 0 failures. clippy -D warnings and
fmt clean on both crates.

(The "40 binaries" figure in the 17:50 entry above was wrong — that count came
off a =head -40='d log, not off the suite. Left in place; this journal is
append-only.)

* 20:10 :issue161:merge:
PR #160 (issue #161 part A) landed on main as b071d54, with two follow-up fixes
and its own session checkpoint at =dev/sessions/2026-08-13-01.md=. Merged
origin/main into this branch.

Renamed this session's files to -02 *before* merging, since #160 took the -01
slot. Did that as its own commit so the rename is not tangled with conflict
resolution.

Chose merge over rebase. A rebase replays my two feature commits first and the
rename last, so the add/add collision on the session files reappears on the
first commit and has to be resolved against a tree where the rename has not
happened yet. A merge resolves everything once against the final content.

Conflicts and how they were resolved:

- =src/lu/mod.rs=, =src/lu/sparse_factor.rs= — *auto-merged*. Both sides add a
  =LuParams= field and =SparseLu= fields; the additions are disjoint.
- =python/src/lu.rs= (5 hunks) — all the same shape: both sides add one
  parameter to =build_params= / the constructor signature. Kept both.
- =CHANGELOG.md=, =dev/decisions.md=, =dev/tried-and-rejected.md= — kept both
  sides, main's entry first. The latter two are append-only logs, so ordering
  by landing time is the correct resolution rather than a judgement call.
- =dev/context.md= — regenerated with =assemble-context.sh=, not hand-merged.

* 20:25 :issue161:merge:tests:composition:
A clean textual merge is not evidence of a clean semantic one, and there is a
real interaction here: #160's dense-bump route *splices a dense factorization
into the sparse L and U*, and my reach-limited sweeps navigate exactly those
structures (=u_above= for the U sweep, the row-wise L index for the L^T sweep).
A splice producing entries those indices did not describe would truncate a reach
and return a wrong answer.

Neither branch could have tested this — part A predates the reach work, part B
was written against a tree without the splice. So it is the merge's test:

- =reach_route_composes_with_the_dense_bump_route= (lu_hyper_sparse.rs)
- =sparse_solves_compose_with_the_dense_bump_route= (lu_sparse_rhs.rs)

Both assert =used_dense_bump()= as well as their own route's counter, so neither
half can be silently absent. Both pass.

End-to-end with part A on (m=4000, bump=400, dense_bump_max_dim=1664, route
confirmed fired):

  dense sweep     ftran mean 83.2 p50 75.4 us | btran mean 44.8 p50 39.9 us
  reach-limited   ftran mean 36.6 p50 24.3 us | btran mean 33.6 p50 23.9 us
  sparse API      ftran mean 11.8 p50  1.55 us | btran mean 11.2 us
  --> sparse API vs dense sweep: ftran 7.1x  btran 4.0x

Note nnz(LU) goes 30778 -> 35250 (fill 4.05x -> 4.64x) with the bump route on:
part A trades fill for factorization speed, exactly as PR #160 documented
(147,796 -> 172,449 on its own basis). That makes each solve slightly more
expensive in absolute terms; the solve-side ratios hold.

* 20:35 :issue161:merge:tests:
Post-merge: 840 tests across 88 binaries, 0 failures. Python rebuilt with
maturin: 68 passed, 1 skipped. clippy -D warnings and fmt clean on both crates.
