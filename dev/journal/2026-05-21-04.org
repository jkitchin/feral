#+TITLE: FERAL session journal 2026-05-21-04
#+STARTUP: showall

* 17:30 :issue47:cho:explicit-zeros:repro:
Asked to check whether issue #47 (explicit-zero entries defeating the
zero-(2,2)-block fast path, ~2× slower on CHO parmest) still reproduces
after Fix 1 (42434a5) + Fix 2 (80c05f5).

First: standalone iter-0 factor, both variants, current HEAD —
=probe_explicit_zeros= (new). cho_iter0_kkt.mtx (stripped, 205512 nnz)
vs cho_iter0_kkt_with_explicit_zeros.txt (326245 nnz, 120733 explicit
zeros). Result: stripped 439ms/1.508M nnz_L, kept 456ms/1.515M — no
penalty, inertia exact (21672,21660,0) both, residual ~1e-9. So iter-0
alone shows nothing — consistent with the issue's own caveat ("iter-0
is not the slow case").

End-to-end CHO parmest, current feral HEAD (temporarily repointed
pounce-feral at the local checkout + env-gated its zero-strip; both
POUNCE edits reverted afterwards, binary rebuilt):

| variant            | wall   | IPM iters | feral factor calls |
| zeros stripped     | 10.6 s |        41 |                 50 |
| explicit zeros kept| 22.6 s |        35 |                 44 |

#47 STILL REPRODUCES — ~2.1× wall, ~2.5× per factor — with the #46
cascade fix in place. It is not the #46 cascade.

* 17:50 :issue47:mc64:cache:root-cause:
Localised #47. Per-factor timing instrumented in pounce-feral (temp):
stripped run has ~11 expensive (~520ms) + ~39 cheap (~19ms) factor
calls; kept run has all 44 calls at ~400ms — zero cheap refactors.

Reproduced standalone: =probe_explicit_zeros= now factors each matrix
4× on one warm Solver.

  stripped:  cold 434ms -> warm 15/14/15ms   symbolic_calls=1  mc64_cache_hits=1,2,3
  kept:      cold 468ms -> warm 359/359/360ms symbolic_calls=1  mc64_cache_hits=0,0,0

Root cause: explicit zeros defeat the MC64 value-bounded scaling cache
(Track B2). Evidence:
- Cold factor fine either way (~450ms) — not a cascade, not fill.
- Symbolic IS reused either way (symbolic_call_count stays 1). The
  pattern fingerprint (PatternFingerprint::of, solver.rs:83 — hashes
  col_ptr+row_idx only) is constant for the kept matrix since POUNCE
  fixes structure once. Symbolic cache is not the problem.
- MC64 cache never hits with explicit zeros (mc64_cache_hits stays 0),
  so the Hungarian match reruns every factor — ~345ms of the ~360ms
  warm refactor. Stripped, the cache hits -> warm refactor ~15ms (24×).

The explicit-zero (2,2) diagonals make the cache's diagonal-dominance
value-bound test (mc64_value_bound_passes) fail, so it conservatively
recomputes scaling every iterate. That recompute is the whole 2×.

Implication: the issue's hypothesised mechanism ("#46 fast path keys on
structural absence") is wrong. The fix is issue option (a) — feral
drops explicit zeros in from_triplets/symbolic so the kept matrix is
structurally identical to the stripped one and the MC64 cache engages.
#47 stays open. Posted the pinned root cause on the issue.

* 18:00 :issue44:narx:per-factor:assessment:
Asked whether issue #44 (NARX_CFy per-factor cost, ipopt-feral times
out at 600s) is still relevant after Fix 1 + Fix 2.

=probe_narx_factor= (new), default Solver, current HEAD, the captured
corpus:

  NARX_CFy_0000  factor 1207ms  nnz_L 5.79M  inertia (43973,48256,0)  res 6.3e-12
  NARX_CFy_0001  factor  433ms  nnz_L 1.73M  exact                    res 2.2e-14
  NARX_CFy_0002  factor  417ms  nnz_L 1.73M  exact                    res 4.4e-14

Total FRESH 2.06s vs the pre-Fix-1 FRESH replay of 1.97s (journal
2026-05-21-01) — unchanged. Fix 1 + Fix 2 did not move NARX_CFy.

Expected: the 2026-05-21-01 profiling already established NARX_CFy is
large-dense-front bound (449 supernodes nrow>128 = 94.9% of the iter-0
loop; "NARX is CB-immune"; MC64 only 29ms). Fix 1 attacks the
delayed-pivot cascade amplifier; NARX has no cascade to break, so its
bottleneck — honest BLAS work in large fronts — is untouched.

Conclusion: #44 STILL RELEVANT, stays open. Distinct from #47 (which is
the MC64 cache). #44 needs a real factor-speed effort on the large-front
kernel; #47 needs explicit-zero normalization. Different fixes.

* 18:45 :issue47:mc64:correction:root-cause:
Corrected the #47 mechanism after reading the cache code with the
human. The §17:50 entry (and the comment first posted on #47) claimed
explicit zeros defeat the MC64 value-bounded scaling cache via its
diagonal-dominance value-bound test. WRONG — value_bound.rs already
disqualifies explicit-zero-diagonal rows (qualifying_rows:155; test
explicit_zero_diagonal_disqualifies_row) and is never consulted here
because the cache is never *populated*.

Re-ran probe_explicit_zeros with scaling_info instrumentation:
  stripped: scaling_info=Applied every call, mc64_fallbacks=0, cache_hits 1,2,3
  kept:     scaling_info=Mc64FallbackToInfnorm{Mc64ScalingDegenerate}
            every call, mc64_fallbacks 1..4, cache_hits 0,0,0.

Real mechanism: explicit zeros tip MC64's symmetric matching into
Mc64ScalingDegenerate (scaling spread > 1/EPS — the 3e82 blow-up
documented at scaling/mod.rs:171, research note
kkt-mc64-scaling-blowup-2026-05-20, issue #45). A degenerate MC64 ->
ScalingInfo::Mc64FallbackToInfnorm, not Applied. The B2 cache populates
only on Applied (solver.rs:803), else None (solver.rs:812) -> never
populated -> the full Hungarian reruns every warm factor (~345ms) and
is discarded. That wasted Hungarian is the entire 2x.

The stripped matrix's MC64 returns Applied — so explicit zeros are the
*cause* of the degeneracy, not merely a cache-key mismatch. Drop-zeros
is therefore the root-cause fix, not a workaround. Orthogonal option: a
negative cache (remember MC64-degenerate fingerprints, skip the wasted
Hungarian). Posted the corrected mechanism on #47.

* 19:10 :issue47:mc64:reference:mumps:ssids:
Asked what MUMPS 5.8.2 and SPRAL SSIDS do with explicit zeros, to
inform the #47 fix scope. Spawned mumps-expert and spral-expert.

Both converge: NEITHER strips explicit zeros from the input matrix or
the analysis pattern. Analysis (ordering, etree, symbolic) is
value-blind; the stored pattern incl. zeros is the contract. Stripping
at input would change etree/fill and break the value<->pattern index
map. This confirms the "do NOT strip the whole matrix" caution
(value-dependent fingerprint -> re-analysis churn).

The matching/scaling kernels handle zeros locally:
- SSIDS (scaling.f90:640 hungarian_wrapper, :1524 auction_match,
  match_order.f90:100): builds a private working copy and does
  `if (val == 0.0) cycle` BEFORE `log(|val|)`. A stored zero = non-edge
  for the matching. An explicit-zero (2,2) diagonal -> that column is
  unmatched -> Duff-Pralet correction (scaling.f90:767-800) sets its
  scale from matched neighbors, 1/0=1 convention -> scaling bounded.
  Equilibration is max-based, needs no strip (`where(maxentry>0)`).
- MUMPS (dana_aux.F:3854-3860): keeps zeros in the Hungarian but maps a
  zero-valued edge to a large FINITE cost FACT+RINF (not log(0)=-inf,
  not removal); RINF is a matrix-scaled infinity surrogate. Post-match
  guard: scale factor > 0.5*log(huge/2N) -> warn-and-proceed. Norm
  scalings: row/col max==0 -> scale 1.0.

Implication for #47: the fix is NOT a matrix-level strip. It is ~5
lines in feral's MC64 symmetric Hungarian kernel — drop explicit zeros
from the matching working copy before the log transform (SSIDS style),
and handle the resulting unmatched columns (Duff-Pralet correction or
identity for that column). feral currently feeds zeros into the
matching -> log(0) garbage -> 3e82 degenerate scaling ->
Mc64ScalingDegenerate -> InfNorm fallback -> cache never populates.
feral already has the MUMPS-style "detect degeneracy + fall back" half;
it lacks the SSIDS-style "don't feed log(0)" half. Smaller and
lower-risk than the from_triplets canonicalization considered earlier.

* 20:30 :issue47:mc64:routing:correction:repro:
Mechanism re-pinned a THIRD time — and this time verified empirically
before claiming anything. Code inspection while preparing the fix
found build_cost_graph (mc64.rs:256) ALREADY drops explicit zeros
(lines 274, 300: `if val == 0.0 { continue; }`). So the §19:10 fix
scope ("SSIDS-style drop zeros in the MC64 Hungarian") was wrong — that
is already done. MC64's cost graph is zero-free for both matrices.

Instrumented probe_explicit_zeros to print pick_scaling_strategy's
structural inputs and routing decision. Result:

  route stripped            diag_only=0      (0.000)  max_col_nnz=10861  -> InfNorm
  route explicit zeros kept diag_only=21660  (0.500)  max_col_nnz=10866  -> Mc64Symmetric

THE divergence point. pick_scaling_strategy (mod.rs:601) counts STORED
entries. A constraint column in the zero-(2,2) block, lower-triangle
stored, has only its diagonal. Kept: that diagonal is an explicit 0.0
-> nnz_col=1, row_idx==j -> counted diag_only. Stripped: the 0.0 is
gone -> nnz_col=0 -> NOT diag_only. So diag_only/n is 0.500 (kept) vs
0.000 (stripped); has_slack_mass (>=0.30) flips; kept routes to
Mc64Symmetric, stripped to InfNorm.

compute_scaling_auto_with_cache returns early on non-MC64 routing
(mod.rs:363), so the stripped matrix NEVER runs MC64 — it is InfNorm
with scaling_info=Applied, cache populates, warm 15ms. The kept matrix
runs MC64, degenerates (issue #45, 3e82 spread), falls back ->
Mc64FallbackToInfnorm{Mc64ScalingDegenerate}, cache never populates,
warm 370ms. Confirmed in the same probe run.

Correction to §18:45: "the stripped matrix's MC64 returns Applied" is
WRONG — the stripped matrix never runs MC64 at all. Its Applied comes
from InfNorm.

Root cause is now solid: explicit zeros are not structure, but
pick_scaling_strategy — a structural heuristic — counts them, so they
flip routing. An explicit-zero-only column is not slack mass.

Two fix options, recorded for the human decision:
- A (value-aware routing): make pick_scaling_strategy skip explicit
  zeros when computing nnz_col/diag_only/max_col_nnz. Principled — the
  kept matrix then routes identically to stripped (InfNorm). Blast
  radius: build_synth_kkt (the issue-#45 oracle, mod.rs:700) pushes
  explicit-zero state+constraint diagonals (lines 735, 750), so its
  routing flips to InfNorm too; tests T2/T3 (auto_falls_back_on_
  catastrophic_mc64_spread, auto_keeps_mc64_when_spread_below_guard)
  assert pick_scaling_strategy==Mc64Symmetric as a precondition and
  would need rework (the spread guard becomes defense-in-depth with no
  live Auto-path test, or build_synth_kkt gains a nonzero-diagonal
  variant re-measured via probe_mc64_synth).
- B (negative cache): when MC64 degenerates for a fingerprint, record
  it in the B2 cache and skip the Hungarian on warm refactors. Fixes
  #47's cost (the 2x is all warm refactors) without touching routing
  or the issue-#45 tests. Cold still pays one wasted Hungarian.

Posted the corrected mechanism on #47. Asking the human to pick A/B.

* 20:39 :issue-47:scaling:fix:implemented:
Implemented Option A — value-aware pick_scaling_strategy. The human
denied the A/B AskUserQuestion, directing "update it then fix it that
way"; took A as the root-cause fix (B only masks warm-refactor cost
and leaves a value-blind router in place).

Change (src/scaling/mod.rs): the per-column loop in
pick_scaling_strategy no longer does nnz_col = end - start. It now
iterates k in start..end, skips entries where values[k] == 0.0, and
counts only numeric nonzeros toward nnz_col / max_col_nnz; a column is
diag_only only when its single numeric nonzero is the diagonal. Doc
comment updated to state both counts ignore explicit stored 0.0
(issue #47). One extra contiguous values[] read per stored entry, no
second pass, no alloc.

Verification on the real CHO parmest iter-0 KKT (probe_explicit_zeros,
route_diag instrumentation):
  route stripped            diag_only=0      -> InfNorm
  route explicit zeros kept diag_only=21660  (0.500) -> Mc64Symmetric   [before]
  route explicit zeros kept diag_only=0              -> InfNorm         [after]
Kept matrix now routes identically to stripped: scaling_info=Applied,
mc64_cache_hits=1,2,3, mc64_fallbacks=0, warm refactor ~16ms (was
~370ms, ~23x). Inertia (21672,21660,0) exact, residual 5.04e-9
unchanged.

Tests-first: two new unit tests built three ways via a Cdiag enum
{Zero, Absent, Real} — Zero/Absent must route InfNorm, Real must route
Mc64Symmetric (pick_scaling_strategy_explicit_zero_diag_not_slack_mass);
explicit-zero off-diagonals on nonzero-diagonal constraint columns must
still route Mc64Symmetric (pick_scaling_strategy_explicit_zero_offdiag_
ignored). Both fail on pre-fix code, pass after. Oracle is the routing
contract itself, hand-reasoned — not a reference solver.

* 20:42 :issue-47:issue-45:tests:build-synth-kkt:
Issue-#45 spread-guard test fallout, as predicted in the §20:30 A
blast-radius note. build_synth_kkt builds explicit-zero (2,2)
diagonals, so after the value-aware change its KKT routed to InfNorm
and T2/T3 (auto_falls_back_on_catastrophic_mc64_spread,
auto_keeps_mc64_when_spread_below_guard) failed:
  assertion `left == right` failed
    left: InfNorm   right: Mc64Symmetric

Resolved by adding nslack: usize to build_synth_kkt — it now appends
nslack disconnected genuine unit-diagonal slack columns (real 1.0 on
the diagonal, nothing else). That restores diag_only slack mass so the
synth KKT routes to MC64 again, without touching the theta/x/constraint
chain that drives the mc_spread/in_spread degeneracy oracle. T2/T3/T4
call build_synth_kkt(8, 80, ..., 120). Added an in_spread > 1e3
precondition to T2/T3 so the oracle self-validates: if the slack
columns had perturbed the spread the precondition would catch it.
After: all 47 scaling tests pass, full cargo test green (EXITCODE:0,
163 "test result: ok", zero failures), clippy --all-targets clean.

* 20:44 :issue-47:session-end:bench:
cargo run --bin bench --release: 8 synthetic matrices, all inertia
exact (spd_*, kkt_*), factor times in line with prior sessions
(spd_200 389us, kkt_100_30 203us). The bench harness uses in-repo
synthetic matrices, not the POUNCE corpus, so #47 (a warm-refactor
caching effect on a real KKT) does not show here — expected, no
regression. The #47 speedup evidence is the probe_explicit_zeros
numbers above (~23x warm refactor).

Committed: ed147dd test(issue-47) probe routing diagnostic;
e49694b fix(scaling) value-aware pick_scaling_strategy (#47);
129f268 docs(plan) issue-47 plan.
