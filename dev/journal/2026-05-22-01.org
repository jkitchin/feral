#+TITLE: FERAL journal 2026-05-22-01
#+STARTUP: showall

* 07:15 :issue-49:review:scaling:regression:
Reviewed issue #49 — "0.4.0: #47 value-aware scaling routing regresses
LDLᵀ factor cost on explicit-zero-(2,2) KKT (Mittelmann ex4_2)". The
issue pins commit e49694b (#47) and reports POUNCE ex4_2_320 going
~10.8s -> TIMEOUT 600s, ex4_2_160 ~2.4s -> 19.2s.

Built probe_issue49 (src/bin/) — loads a KKT .mtx, prints the routing
decision value-blind (pre-#47) AND value-aware (post-#47), then factors
under Auto/Mc64Symmetric/InfNorm (low-level) and on a warm Solver
(POUNCE's analyze-once/refactor-many path).

Finding 1 — #47 does NOT change ex4_2 routing. Both forms route to
InfNorm:
  ex4_2_160: value-blind  InfNorm [diag_only=25917 (0.336), max_col_nnz=6]
             value-aware  InfNorm [diag_only=0     (0.000), max_col_nnz=6]
The arrow-head gate is max_col_nnz > 32; ex4_2 has max_col_nnz=6 (a
banded KKT, not an arrow KKT), so has_arrow_head is FALSE and the
matrix routes to InfNorm regardless of how diag_only is counted. #47
only ever flips routing when has_arrow_head is already true. e49694b
is exonerated as the cause.

Finding 2 — the issue's recommended repro does not reproduce. Factored
ex4_2_160_iter0_kkt.mtx (n=77115, 255354 stored nnz, 25917 explicit
zeros) with a standalone feral at HEAD (bb3b712):
  low-level factor (Auto/Mc64/InfNorm) ~52-55 ms, all identical
  warm Solver: cold 271.8 ms, warm 51.7-55.6 ms x4
  nnz_L=3.84M, n_delayed=3, inertia (51198,25917,0) exact
  (matches mtx header neg_evals=25917), rel_res 2.4e-13
  symbolic_calls=1, mc64_cache_hits=1..4, mc64_fallbacks=0
ex4_2_320 (n=307835): low-level ~0.38s, inertia (204798,103037,0)
exact, rel_res 1.8e-13. NOT 2.56s.

So standalone feral bb3b712 on the iter-0 KKT is in the FAST regime —
consistent with the issue's *good* (b3e4d3e, 2.4s) end-to-end number,
not the 19.2s regression. The issue's own verification recipe ("factor
the iter-0 .mtx standalone, compare") shows no regression.

Finding 3 — the issue mis-reads its iter-0 evidence. The "0.55s iter-0
factor" is a COLD number (one-time MetisND symbolic for n=77k is in
it); the "good-feral 2.4s/21it = 0.11s/iter" it is compared against is
an average dominated by cheap ~52ms warm refactors. Cold-vs-average is
apples-to-oranges; the cold factor costs ~0.27-0.55s in BOTH feral
revisions.

Conclusion: the regression (if the 19.2s end-to-end is real) is NOT on
the iter-0 KKT and NOT caused by #47. It must be on later IPM iterates
— ex4_2's (2,2) block fills in as delta_c becomes nonzero and the
(1,1) block fills as lambda becomes nonzero, so iterate k>>0 is a
denser/harder matrix — whose dumps are not in the repro. The remaining
functional suspects in b3e4d3e..bb3b712 are 42434a5 (#46 fine-grained
delayed pivoting), 80c05f5 (#48 2x2 inertia), c990def (B2 MC64 cache)
— NOT e49694b.

Recommended back to the issue: POUNCE must dump the slow iterates
(kkt:5, kkt:10, kkt:15, kkt:20), not just kkt:0; only then can the
range be bisected against an actually-slow matrix. Also worth ruling
out the pounce-feral wrapper (does it build a fresh Solver per iterate
/ does the pattern fingerprint stay stable so symbolic+MC64 caches
engage — in this probe they do, symbolic_calls stays 1).

* 07:49 :issue-49:issue-44:cache:scaling:regression:correction:
Rechecked #49 on the explicit "there is a cache issue" hypothesis —
the 07:15 entry under-weighted c990def (B2 cache). Three new facts.

Fact 1 — the B2 "MC64 scaling cache" FIRES on ex4_2, which never runs
MC64. Root cause: compute_infnorm (src/scaling/infnorm.rs:35,94)
returns ScalingInfo::Applied, byte-identical to the MC64 Applied. The
B2 population gate (solver.rs:803) is matches!(scaling_info, Applied),
so it caches the InfNorm vector and on the next warm factor() injects
the STALE iter-k InfNorm vector as ScalingStrategy::External.
Dumped 12 real consecutive ex4_2_160 IPM iterates from POUNCE
(/tmp/ex42d, --dump kkt:0-12) + 10 of ex4_2_320 (/tmp/ex42d320).
probe_cache_sequence IPM-PATTERN run:
  _160: calls 0-3 cache MISS, calls 4-11 CACHE HIT (8/12)
  _320: calls 0-6 cache MISS, calls 7-9 CACHE HIT (3/10)
The cache is live on this matrix class, not dormant.

Fact 2 — decisions.md "B2 ships as latent infrastructure / value-bound
rejects every warm iter / harmless" (2026-05-21 entry) is FALSE for
explicit-zero-(2,2) KKTs. It was validated only on pinene_3200 /
rocket_12800, both fully-populated δ-regularized diagonals where the
ratio-growth condition explodes 1e8→1e10 and rejects. ex4_2's (2,2)
block is STRUCTURALLY zero → value_bound.rs qualifying_rows() excludes
25917/77115 rows (34%) on _160, 103037/307835 on _320. The bound then
aggregates only over the stable (1,1) block and PASSES. The "Deviation
1 / qualifying rows" carve-out is exactly what un-dorms B2 here.

Fact 3 — the cache is NOT the #49 cost regression. Standalone feral
factor cost is FLAT regardless of cache state: _320 ~242-291 ms
(misses) vs ~241-250 ms (hits); _160 ~52 ms vs ~43 ms. Inertia exact
throughout (_320 (204798,103037,0), _160 (51198,25917,0)); n_delayed
small; rel_res 2.4e-13. The stale-scaling reuse is a latent
CORRECTNESS defect, benign on every ex4_2 iterate measured — not a
perf regression. e49694b (#47) stays exonerated.

Where the #49 regression actually lives: POUNCE ex4_2_320 timing
(--dump timing:all, max_iter=3): factorization 3.106 s / back_solve
0.041 s / overall 3.378 s — ~0.78 s per factor for the first 3 iters,
then it explodes (the kkt:0-14 dump run hit the 400 s timeout at
~iter 9 → iters 3-9 cost ~397 s). Standalone feral factors those same
dumped iter-9 KKTs in 0.25 s. So POUNCE's per-factor blows up on late
iterates while standalone feral on the dumped matrix does not — i.e.
POUNCE is NOT factoring the raw dumped kkt_solve_001 matrix, or pays
cold symbolic every iterate. This is the known #44/#48 "iter 6-9
factor-time blowup" cost cluster, NOT the cache. _160 stays fast
(POUNCE total 2.03 s, 13 iters) because its symbolic is cheap.

Conclusion: the user's instinct was right — there IS a cache bug
(B2 caches/reuses InfNorm scaling on explicit-zero-(2,2) KKTs), and
it should be fixed (gate population on MC64-actually-ran, not on
ScalingInfo::Applied). But it does not cause #49's cost regression;
that is the per-factor cost cluster reproducing only inside the
POUNCE-feral drive, not on the standalone dumped KKT. #44 relevance:
same cache fires there too (NARX routes Mc64Symmetric, caches a
genuine MC64 vector) but #44's cost is likewise the cascade, not the
cache. Probes: src/bin/probe_issue49.rs, probe_cache_sequence.rs.

* 09:30 :issue-49:cache:fix:scaling:
Fixed the B2 cache bug from the 07:49 entry. Root cause: B2 population
gate was matches!(scaling_info, Applied), and InfNorm's compute_infnorm
also returns ScalingInfo::Applied — indistinguishable from MC64-complete
Applied. So B2 cached the InfNorm vector and re-injected a stale iter-k
InfNorm scaling as ScalingStrategy::External on the next warm factor().
Fix (solver.rs): added an mc64_ran gate — the cache only populates when
the effective strategy is Mc64Symmetric, OR Auto routing (via
pick_scaling_strategy) to Mc64Symmetric, AND scaling_info==Applied.
InfNorm / Identity / External never populate B2. On a non-MC64 route the
cache is explicitly cleared (mc64_scaling_cache = None).
New test mc64_cache_does_not_engage_on_infnorm_route_issue_49: factors
tridiag(6,10,1) 3x on one Solver, asserts pick_scaling_strategy==InfNorm,
mc64_cache_hit_count()==0, symbolic_call_count()==1.
Post-fix probe_cache_sequence on all 10 ex4_2_320 iterates: mc64_hits=0
(was 3/10 before). 23 solver tests + full suite green; fmt+clippy clean.
Committed 86fb953. CHANGELOG [Unreleased] updated.

* 10:10 :issue-49:pounce:performance:parallel:race:intermittent:
Task (b): investigated the claimed POUNCE-feral per-iterate cost gap
("~40s/iter in POUNCE vs ~0.25s standalone"). The premise is wrong.
Clean measurements (print_timing_statistics, per-line timestamps):
  ex4_2_320 to convergence (17 iters): 10.6s parallel / 17.5s serial
  ex4_2_160 to convergence (21 iters): 2.29s
  12-run repeat batch: parallel 10.2-10.8s, serial 16.9-17.7s, all exit 0
Per-factor ~0.5s parallel — fully consistent with standalone feral warm
factor ~0.25-0.3s. No systematic gap. POUNCE's wrapper rebuilds
CscMatrix::from_triplets every factor() but symbolic + pattern caches
stay engaged (symbolic_calls=1), so warm refactors stay cheap.

BUT a real intermittent pathology exists. One uncapped convergence run
TIMED OUT at 600s stuck during iteration 11 — a single factorization
hung ~590s. This is exactly #49's "10.8s -> TIMEOUT 600s": 10.8s is
normal, 600s is this hang. Properties:
  - feral parallel factorization is value-deterministic (fixed
    snode.children assembly order); iteration logs bit-identical to 7
    sig figs across all runs through iter 11. Hang gives CORRECT output
    + ~1000x wall-time blowup (0.5s -> ~590s) => EXECUTION-TIME race,
    not a numerical trajectory split.
  - Rate: ~1/12 per RUN is the wrong unit. A run does ~17 factorizations
    and hangs if any one does: P(run)=1-(1-p)^17 ~= 17p ~= 1/12 =>
    p ~= 1/200 per factorization. NOT deterministic-at-an-iterate — the
    12-run batch all completed 17 iters (passed iter 11) cleanly; a
    deterministic slow iterate would have hung all 12.
  - NOT #47 (ex4_2 routes InfNorm regardless, max_col_nnz=6, no
    arrow-head), NOT the B2 cache, NOT --dump kkt I/O (reproduces on
    no-dump runs), NOT the known #44/#46/#48 DETERMINISTIC delayed-pivot
    cascade.
Read run_parallel_task in factorize.rs (rayon::scope + pending-decrement
trampoline, fetch_sub(1,AcqRel), prev==1 spawns parent): structurally
correct on inspection. Prime suspect: a lost spawn in that trampoline
(parent snode task never scheduled => permanent hang). A DOUBLED spawn
there is the sibling bug — the now-fixed 2026-04-21 inertia race.
Journal agent confirmed: NO prior FERAL record of intermittent/
nondeterministic PERFORMANCE pathology; this is new and undocumented.

Reproduction probe: src/bin/probe_hang_loop.rs factors one dumped KKT
REPEAT times on a warm Solver (parallel|serial), flushing per-call so a
hang shows as a stalled line and a soft spike (>5x median) is flagged.
Launched 1000x parallel on ex4_2_320_009_kkt.mtx to convert the
~1/200 race into a catchable event; serial arm to A/B the scheduler.

Conclusion: cache bug fixed; the "cost gap" as described does not exist;
#49 actually caught a rare (~1/200-per-factorization) execution-time
race in the parallel multifrontal driver. Recommend scoping the hang
hunt as its own task — needs the reproduction harness above plus a
deeper read of the spawn/steal/wakeup path.

* 11:30 :issue-49:pounce-44:correction:retraction:parallel:
CORRECTION to the 10:10 entry — the "real intermittent race in feral's
parallel driver" claim is RETRACTED as an overclaim. Two facts forced
the walk-back.

Fact 1 — probe_hang_loop finished: 1000 back-to-back PARALLEL factors of
ex4_2_320_009_kkt.mtx on one warm Solver. median=277.1ms, worst=1095.2ms
@call0 (the cold symbolic, worst/median=4.0x), ZERO spikes, ZERO hangs,
total 294.3s, exit 0. Combined with the 12 full POUNCE runs (0 hangs),
the hang is unreproduced everywhere I controlled. One uncapped POUNCE
run timed out at 600s (last printed line iter 10) — but it was never
localized: the log just stopped; I assumed feral factorization but never
ruled out POUNCE's own step computation or a system transient. A single
unreproduced, unlocalized timeout is NOT a demonstrated feral bug.
Calling it "a race in feral's parallel scheduler" was not warranted.

Fact 2 — the human pointed at POUNCE issue #44 (BLEACHNG): "non-
deterministic solve — fast Error_In_Step_Computation vs >90s hang",
n=17, m=0. Same fast-vs-hang symptom. Decisive: n=17 is far below any
feral parallel threshold (one tiny dense front) and n=17 cannot take
90s in feral under any pivoting — so for BLEACHNG feral's parallel path
is exonerated; the nondeterminism MUST be POUNCE-side. POUNCE #44's own
notes name the suspect: "HashMap iteration order ... nondeterministic
iteration order ... uninitialized scratch". POUNCE does use HashMap in
its .nl / Hessian-tape code (pounce-cli/src/nl_tape.rs,
nl_hessian_program.rs, nl_reader.rs).

Reframed hypothesis (UNPROVEN, no feral race needed): POUNCE assembles
KKT triplets in a HashMap-influenced order -> per-process random seed
makes the triplet order vary run-to-run -> from_triplets sums duplicate
coordinates in a different order -> ULP-level KKT value differences ->
on a near-singular KKT this can flip feral's delayed-pivot trajectory
and occasionally tip it into the KNOWN DETERMINISTIC #44/#46/#48
cascade into a dense root front. Deterministic feral behavior fed
nondeterministic input — which is exactly why the 1000x FIXED-matrix
probe found nothing. Caveat: ex4_2_320 iteration logs were bit-identical
to 7 figs across captured runs (mild evidence against run-to-run input
divergence for that problem; ULP noise can hide below 7 printed figs).

Corrected conclusion: (a) cache bug fixed (86fb953) — solid. (b) the
"~40s/iter" premise is refuted (ex4_2_320 ~10.6s, reproduced 12x) —
solid. The lone 600s timeout is unreproduced, unlocalized, and NOT a
demonstrated feral defect; it belongs under POUNCE #44 (POUNCE-side
nondeterminism), not as a feral bug. Next step is read-only on the
POUNCE side: is the KKT triplet order HashMap-dependent? Do not chase a
feral parallel-scheduler race that 1000 factorizations could not show.

* 12:10 :issue-49:nondeterminism:correction:evidence:
CORRECTION to both the 10:10 and 11:30 entries: they state the
ex4_2_320 iteration logs are "bit-identical to 7 sig figs" across runs.
That is FALSE — I repeated it from the prior session summary without
checking the logs, which were on disk the whole time.

Direct evidence of nondeterminism. Iteration 4, primal-infeasibility
column, four POUNCE ex4_2_320 runs:
  pounce_ts      4  3.7409062e0  7.33e-13  3.75e-6 ...
  pounce_conv    4  3.7409062e0  7.32e-13  3.75e-6 ...
  pounce_ab      4  3.7409062e0  7.32e-13  3.75e-6 ...
  pounce_iter11  4  3.7409062e0  7.33e-13  3.75e-6 ...
Every other printed digit on every iteration line matches; inf_pr at
iter 4 splits 7.32e-13 / 7.33e-13, two runs each. The runs differ only
in max_iter / --dump flags, none of which can change iter-4 arithmetic
— so this is genuine run-to-run variation, ~1e-15 absolute. It is the
fingerprint of order-dependent floating-point summation: inf_pr ~7e-13
is near-total cancellation, which amplifies ULP-level (1e-16) summand
noise into the visible 3rd digit.

So nondeterminism IS present — proven, ULP-level. But it is upstream of
feral: feral factorization is value-deterministic (1000x fixed-matrix
probe = flat), so fixed input gives fixed output. The variation enters
in POUNCE's KKT assembly or constraint/derivative evaluation.
HashMap-ordered iteration is the textbook Rust cause of order-dependent
summation, and POUNCE uses HashMap in its .nl/Hessian-tape code — so the
HashMap hypothesis is now evidence-backed (not proven; exact path not
traced).

Net, precisely stated: (1) ULP-level nondeterminism — confirmed,
upstream of feral. (2) It is normally harmless — all 12+ runs still
converge in 17 iters. (3) The 600s timeout — still n=1, unlocalized;
whether ULP noise can occasionally tip a near-singular KKT into the
deterministic #44/#46/#48 cascade is plausible but UNPROVEN, not
asserted. The earlier confusion was conflating "cannot reproduce a
600s hang" (true) with "everything is deterministic" (false).
