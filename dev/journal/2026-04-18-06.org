#+TITLE: Session 2026-04-18-06 Journal
#+STARTUP: showall

* 16:00 :orientation:kahip:k3:
User: "do K3". From the 2026-04-18-05 checkpoint, priority 1 is
KaHIP phase K3 — flow-based edge refinement using the push-relabel
primitive from K2. Scope per dev/plans/ordering-kahip.md §K3
(lines 327-373) and audit items 2, 3, 10, 12:
  - Bug 2: source/sink construction must say "vertices at distance
    EXACTLY bnd_distance from the boundary" — NOT "deepest vertex".
  - Bug 3: Most Balanced Min Cut is a v1 concern (significant
    quality left on the table otherwise).
  - Item 10: edges are undirected; model as two anti-parallel
    directed edges, each with the full weight as capacity.
  - Item 12: `bnd_distance` must be configurable; KaHIP adapts it
    adaptively, but fixed defaults (2-5) are fine for v1.

Scope for this session:
  - Add `crates/feral-kahip/src/graph.rs`: an undirected weighted
    graph type with CSR storage (needed by K3/K4/K5/K6).
  - Add `crates/feral-kahip/src/flow_refine.rs`: flow-based edge
    refinement of an input bisection.
  - Tests: deterministic 2D grid, balance respected, monotone
    non-worsening, determinism, pinning of fixed nodes.
  - Keep MBMC to a 2-cut search (residual-BFS-from-source vs
    residual-BFS-from-sink): pick the balanced one. Full MBMC
    (flow manipulation) deferred to a K5/K6 bench.

No prior K3 attempts in tried-and-rejected.md.

Module stays `pub(crate)` with `#[allow(dead_code)]` until K5/K6
consume it; public surface `kahip_order` remains Internal error.

* 17:30 :kahip:k3:bugs:
Hit three K3 implementation/test bugs in sequence:

1. Grid_7x7 test oracle wrong — claimed stair-step cut ≥13, actual
   is exactly 12 (6 horizontal + 6 vertical crossings along r+c=6).

2. INF_CAP overflow. Used `i64::MAX/4` per pin; with many pins on
   super-source the cumulative `excess[source] -= cap` overflowed.
   Fixed: inf_cap = (sum_in_band_edge_weight / 2) + 1 (halved
   because edges array counts each undirected edge as two
   anti-parallel directed entries).

3. Fixed-node depth fallback. When a small part is fully contained
   inside the BFS ball, no vertex has dist==bnd_distance, so
   fixed_src_count or fixed_snk_count = 0 and K3 short-circuits to
   false. Fixed: pin_depth_p0 = min(max_dist_in_part_0,
   bnd_distance) and symmetrically for p1. Pins the deepest
   vertices actually present in each part.

* 18:00 :kahip:k3:oracle:
Even after fixing the algorithm bugs, grid_7x7 with bnd_distance=3
returned false: min-cut candidates were cut=6 but parts (40,9)
and (43,6), exceeding slack=30. Reason: bnd_distance=3 on a 7x7
grid lets the band cover essentially the whole grid, with only a
handful of corner pins constraining structure. The min-cut
isolates tiny peripheral clusters.

Two fixes together:
- bnd_distance=2 (per research doc §oracle 3): band = r+c ∈ {4..9},
  5 source pins (r+c=4), 4 sink pins (r+c=9). Candidates:
  cut=8, parts (33,16) and (39,10).
- max_imbalance=0.4 (slack=35): accepts (33,16). Cut drops from
  12 → 8 (33% reduction).

Keeps the test meaningful: verifies K3 finds a strict improvement
on a deliberately bad bisection with realistic pins and tolerances.

* 18:15 :kahip:k3:done:
K3 complete. 40/40 feral-kahip tests pass, clippy clean with
`-D warnings`. Scope delivered:
- UndirectedGraph CSR type (graph.rs) with cut_weight, neighbors,
  eweights accessors, from_csc_unit_weights constructor.
- flow_refine_bisection (flow_refine.rs): boundary/band BFS,
  anti-parallel undirected-edge model, configurable bnd_distance
  and max_imbalance, two-cut MBMC (residual-BFS from source vs
  from sink).
- 8 K3 tests (empty, path-optimal, grid-7x7 improvement,
  determinism, balance rejection, non-worsening, fixed-node
  pinning) + 4 graph tests.

Deferred per plan:
- Full MBMC (residual-flow manipulation) — K5/K6 follow-up.
- Adaptive bnd_distance — K6 tuning.
- Vertex weights — K4 will introduce.

* 19:00 :kahip:k4:research:
User: "then K4-K6 first. start mongoose with a subagent."
Spawned Mongoose research subagent (general-purpose,
background) to produce dev/research/ordering-mongoose.md
(algorithm + license check). Main thread proceeds with K4.

K4 research note written at
dev/research/ordering-kahip-k4.md. Core idea: given a refined
edge bisection from K3, build a node separator by reducing to
minimum weighted vertex cover on the boundary-bipartite cut-edge
graph. Directed s → B_0 → B_1 → t flow network:
  - source arcs to every B_0 vertex, cap = vweight(v)
  - sink arcs from every B_1 vertex, cap = vweight(v)
  - cross-edge arcs B_0 → B_1, cap = INF
The min-cut saturates only source/sink arcs; saturated arcs
correspond exactly to the min-weight vertex cover by König's
theorem. Simpler and more direct than generic node-splitting;
matches Sanders-Schulz 2011 §4.4 literally.

v1 scope: unit vertex weights, single call, no internal balance
enforcement. 8 test oracles: empty boundary, path (sep=1),
7x7 grid horizontal (sep=7), K_{3,3} (sep=3), determinism,
separator validity invariant, weighted picking, no-interior-
vertex invariant.

* 19:30 :kahip:k4:done:
K4 implemented at crates/feral-kahip/src/node_separator.rs.
flow_node_separator(graph, where_, vweight) → Option<NodeSeparator>.
Boundary extraction, directed bipartite network, push_relabel
call, separator extraction via saturated source/sink arcs.
9/9 new K4 tests pass on first run (no debugging needed). 49/49
feral-kahip tests total pass; clippy clean. Committed as 36e400d.

* 19:45 :mongoose:research:subagent:
Mongoose research subagent returned. Key findings:
  - License: GPL-3.0-only (confirmed from SuiteSparse stable
    README SPDX identifier). Blocks source reuse but not
    clean-room from paper (FERAL protocol anyway).
  - Paper: Davis/Hager/Kolodziej/Yeralan 2020, ACM TOMS 46(1)
    Article 7, DOI 10.1145/3337792.
  - Published Mongoose is edge-partitioning-only — the ND-outer
    + AMD-inner pipeline in tasks.org is the paper's future-work
    section, NOT shipped code. This materially changes the scope.
  - Recommendation: option (b), adopt HEMSR coarsening +
    2-phase hybrid refinement into feral-metis/feral-kahip
    behind feature flags, rather than build a dedicated
    feral-mongoose crate.
  - Open question: has a vertex-separator extension shipped
    since 2020? Worth a quick check of SuiteSparse `dev`
    before committing to follow-up plan.

Full note: dev/research/ordering-mongoose.md (330 lines).

* 20:30 :kahip:k5:k6:done:
K5 + K6 complete. User: "finish K5 and K6". Research note
(dev/research/ordering-kahip-k5-k6.md) set architecture: reuse
feral_metis::internals (coarsen, fm_refine, initial_partition,
graph, rng) like feral-scotch does, plug K3 flow_refine at each
uncoarsening level, K4 flow_node_separator to lift bisection to
separator, AMD leaf via feral_amd::amd_order.

Implementation:
- crates/feral-kahip/src/cycle.rs: K5 multilevel_bisection +
  graph_to_undirected bridge. Mode tuning (Fast/Eco/Strong)
  controls n_sep_trials, coarsen_floor, amd_switch, fm_pass_cap,
  bnd_distance, and flow-refinement aggressiveness.
- crates/feral-kahip/src/node_nd.rs: K6 kahip_nd_order recursive
  driver. Patterned on feral-scotch/src/node_nd.rs; swaps halo FM
  for K3 and direct vertex-separator FM for K4.
- crates/feral-kahip/src/lib.rs: kahip_order_full now wired to
  node_nd::kahip_nd_order; status-line doc updated from "scaffold"
  to "K2-K6 complete".
- Cargo.toml: added feral-amd and feral-metis path deps.

Tests: 61/61 feral-kahip tests pass (+12 new: 5 cycle, 7 node_nd).
Coverage: trivial 10-vertex graph, determinism across repeats,
balance within slack, all three modes on 12x12 grid, graph bridge
preserves edges; diagonal pattern, 10x10 grid → AMD leaf,
16x16 grid → multilevel (cycles > 0), determinism, disconnected
components, Fast/Eco/Strong end-to-end, empty graph.

Clippy -D warnings clean on first try.

Out of scope v1 per research note:
- OrderingMethod::KahipND dispatch in symbolic_factorize — deferred
  pending benchmarking.
- K1 data reduction integration.
- Full V/F-cycle re-coarsening (Sanders-Schulz §4.3).

* 21:15 :kahip:k1:fill:regression:
Wired K1 data reduction into kahip_nd_order, hoping to fix c-big's
3.29x KaHIP/AMD fill blow-up. Three discoveries, in order:

1) Initial wiring (all four rules enabled, max_ratio=0.99): fill got
   WORSE across the board. Geomean KaHIP/AMD went 1.032 -> 2.094.
   c-big went 3.29x -> 3.92x. Three matrices blew up catastrophically:
   vesuvia 25.8x, vesuvio 51.6x, vesuviou 41.9x, cresc132 95.3x.

2) Found a real bug in expand_permutation: Rule-2 path interiors
   were anchored to endpoint u only, but fill-preservation requires
   the path to be eliminated before BOTH endpoints. When pos(w) <
   pos(u), the path stays alive when w is eliminated and produces
   extra fill. Fixed by choosing whichever of the two anchors has
   lower reduced-perm position. Geomean 2.094 -> 1.876. vesuvia
   recovered (393k -> 49k fill). vesuvio/vesuviou/cresc132 did NOT
   recover, so there are more bugs.

3) Rolled back Rules 2-4 entirely; kept Rule 1 (degree-1 cascade).
   Geomean KaHIP/AMD = 1.023 — KaHIP is now THE BEST on-average
   ordering (AMD 1.000, METIS 1.024, SCOTCH 1.038, KaHIP 1.023).
   Min-fill wins: KaHIP 37, tied with AMD. c-big 2.59x (better than
   METIS's 2.69x and closer to SCOTCH's 1.00x). Total KaHIP time
   dropped from 147s to 84s because Rule-1 cascade shrinks the
   graph fed to the expensive flow refinement.

Root cause for Rules 2-4 blow-ups is not understood. Claims in
Ost-Schulz-Strash 2021 that these rules are "fill-preserving" are
clearly true only under implicit constraints on the downstream
ordering, not for any ordering on the reduced graph. Documented in
dev/tried-and-rejected.md with hypotheses to investigate.

Changes: data_reduction.rs gets ReduceOptions{degree2_simplicial,
degree2_nonsimplicial, twins, subset} with ::conservative() (Rule 1
only) and ::full() (all rules, test-only). Expansion fix is
permanent. node_nd.rs uses ::conservative().

All 61 feral-kahip tests pass. 97 workspace lib/bin tests pass.
Clippy clean.

* 22:05 :ordering:auto:dispatch:
Implemented OrderingMethod::Auto adaptive dispatcher. Rule set based
on cheap CscPattern features (n, avg_deg = nnz/n):
  - n > 100_000 && avg_deg < 5  -> ScotchND
  - n <  10_000 && avg_deg < 15 -> KahipND
  - else                        -> Amd

Rule choice motivated by per-matrix bakeoff data:
  - SCOTCH dominates the large-sparse / arrow class (c-big 1.00x vs
    AMD, KaHIP 2.59x).
  - KaHIP's K1 reductions uniquely beat AMD on small-sparse matrices
    (cresc132 0.607x, chwirut1 0.998x, hahn1 0.993x).
  - AMD is the best default everywhere else.

Bakeoff over 41 matrices confirms this:
  - geomean fill_auto / fill_amd = 0.988 (best)
  - wins: AMD 37, METIS 31, SCOTCH 28, KaHIP 37, Auto 41
    (Auto never strictly loses a matrix)
  - total symbolic time: Auto 15.37s -- fastest, beats AMD by 0.27s
    because SCOTCH is faster than AMD on c-big (15.1s vs 15.2s).

Implementation is ~30 lines in src/symbolic/mod.rs: new enum variant,
choose_adaptive helper, unreachable! arm in run_external_ordering.
Tests: symbolic_factorize_auto_produces_valid_perm + unit test of
choose_adaptive branches. All 23 symbolic tests pass.
