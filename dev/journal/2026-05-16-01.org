#+TITLE: Journal — 2026-05-16-01

* 10:00 :f03:bloweybl:zero-pivot:default:
F-03 fix landed. `NumericParams::default()` now sets
`on_zero_pivot: ZeroPivotAction::ForceAccept`, overriding the
`Fail` inherited from `BunchKaufmanParams::default()`. Dense default
stays `Fail`.

Oracle evidence (from research note
`dev/research/f03-bloweybl-rank-rejection.md`):
- MUMPS 5.8.2: inertia=(20001, 10001, 1), residual 2.4e-14
- MA57 2023.11.17: inertia=(20001, 10001, 1), residual 2.7e-15
- Both agree on one zero pivot; sibling `bloweybq` (half-size,
  same generator) factors cleanly with zero null pivots.

Tests:
- new `f03_default_force_accept_factors_isolated_zero_pivot` in
  `tests/pounce_interface.rs` — asserts diag(1, 0, 1) factors to
  Success with inertia (2, 0, 1) under the new default.
- `solve_after_singular_returns_no_factor` and
  `i4_singular_under_fail_returns_singular_clears_factor` updated
  to opt-in to `Fail` via `Solver::with_params`.
- `cargo test --release` all green (full suite, no regressions).
- `cargo clippy --all-targets --release -- -D warnings` clean.

Stress baseline movement:
- bloweybl: `NumericallyRankDeficient` → ok, inertia=(20001, 10001, 1),
  residual 1.68e-15 (matches MUMPS+MA57 to within an order of
  magnitude). Real reliability improvement.
- rankdef_5_2/10_3/50_5: status=fail → ok (with zero=1 instead of
  expected zero=k). These were previously masked by report.py's
  carve-out for `Fail`-rejected rankdef matrices; the F-01 bug
  was always there, F-03 just made it visible. Now flagged=4
  (was 2), but all 4 are the same root cause (#21).

* 10:30 :baseline:report:
Refreshed `external_benchmarks/stress/baseline.txt` and
`baseline.json`. New totals: 24 ok, 4 flagged (all four rankdef
synth rows, all flagged by F-01 under-reporting). Real-world
matrices: 18 GHS_indef all ok, 6 synth non-rankdef all ok.

* 18:00 :f01:split:fix:
Implemented the F-01 fix as a *split* between solve-time and
factor-time thresholds rather than a simple bump of `zero_tol`.

Background: the initial F-01 attempt bumped `zero_tol` directly to
`sqrt(n)·EPS·||A_scaled||_inf` and propagated the bumped value into
`Factors.zero_tol`. This caused `src/dense/solve.rs:194,210` to
skip dividing by pivots in the rank-deficiency band even on
non-rank-deficient ill-conditioned matrices. Observed regression on
synth/ill_cond_e14 (n=100, cond≈1e14): rel_res 7e-16 → 2.88e-7.

Fix shape:
- New `BunchKaufmanParams::null_pivot_tol` (and `_2x2`), defaulting
  to `zero_tol` so dense callers unchanged.
- New case-a' branch in `try_reject_1x1_frontal`, `do_1x1_pivot`,
  `count_1x1_inertia`, `count_2x2_inertia`, and the basic `factor`
  last-pivot loop: when `zero_tol < |d| <= null_pivot_tol` AND
  `on_zero_pivot == ForceAccept`, count one zero in inertia but
  leave `d` and `L` intact and return `Accepted` so the trailing
  update fires normally. The solve then divides by the
  small-but-real `d` (gated by strict `Factors.zero_tol`),
  preserving residual quality.
- Reject-threshold updated to use `null_pivot_tol` as its floor so
  rank-def pivots get routed through the case split.
- `override_null_pivot_tol` bumps `null_pivot_tol` only; `zero_tol`
  stays at EPS.

Results (stress baseline after split):
- All 28 matrices ok, 0 flagged.
- ill_cond_e14: rel_res 7.08e-16 (fully recovered).
- rankdef_5_2: (2, 2, 1), unchanged.
- rankdef_10_3: (4, 5, 1), unchanged.
- rankdef_50_5: (25, 24, 1), unchanged.
- rankdef_200_20: (109, 88, 3) — was (112, 88, 0) before F-01.
  Detects 3 of 20 constructed zeros, consistent with MUMPS 5.8.2
  ICNTL(24)=1 behavior (MUMPS also reports zero=0 on this matrix).
- All 256 lib tests + 206 integration tests pass.
- clippy clean.

Updated CHANGELOG.md (F-01 entry under Added) and
`dev/research/f01-rankdef-underreporting.md` (implementation
outcome section with empirical table and split rationale).

* 19:30 :issue-17:rocket_12800:capi:regression-test:
Landed three commits closing the primary thread of issue #17:
- a82ec85: cargo fmt fix-up for F-01 (9910fce slipped past hooks).
- da23d13: ~feral_new()~ in ~src/capi.rs~ constructs ~Solver::new()~
  with cascade-break OFF (matches 585d739 Rust-side default), with
  ~FERAL_CASCADE_BREAK=on|1|true|yes~ opt-in for A/B reproduction.
  ipopt-feral parity with pounce-feral now lives in feral, not in
  per-caller env workarounds.
- 4de8a56: ~tests/issue_17_robot_1600_cascade_off.rs~ locks two
  invariants — ~NumericParams::default()~ keeps cascade-break
  disarmed (CI-safe synthetic check), and ~robot_1600_0003~ factors
  via ~Solver::new()~ and reports MUMPS reference inertia
  ~(14399, 9601, 0)~ (skips when corpus absent).

* 20:00 :issue-17:rocket_12800:finishing-thread:investigation:
Investigated the residual thread from #17: "rocket_12800 has a
second inertia disagreement at iter 10 even with CB=off." Goal:
either reproduce + root-cause, or document a clean punt.

Method: rebuilt pounce against current feral (F-01 + da23d13),
ran ~pounce rocket_12800.nl max_iter=3~ with ~POUNCE_DUMP_KKT~
pointed at ~/tmp/rocket_dump.jsonl~ to capture every KKT system
pounce-feral was asked to factor. 8 solves dumped (165 MB).

Repacked each into a symmetric MTX and ran ~diag_robot1600_eigs~:

- solve_00 (iter 0): feral reports ~(51201, 38400, 0)~ — matches
  IPM expected. Smallest |eigs| ~1.3e-5, no near-zeros.
- solve_01 (iter 1, first attempt): feral reports ~(51199, 38402, 0)~
  — 2 NEGATIVES OVER EXPECTED. Smallest |eig| ~+6.64e-5~, still
  far above any null-pivot floor. This is the WrongInertia that
  triggers pounce's ~IpPDPerturbationHandler~ to bump ~delta_w~
  and re-solve, exactly as designed for genuine negative-curvature
  detection.
- solve_02 (iter 1, after first perturbation): still ~(51199, 38402, 0)~
  — perturbation hasn't shifted enough negatives to positive yet.
- solve_05 (iter 2): back to ~(51201, 38400, 0)~ after sufficient
  perturbation. Smallest |eigs| ~5.4e-5.

~cb=off~ and ~cb=default~ give bit-identical inertia on all four
solves — cascade-break is no longer involved (it's None in
defaults). No pivots below 1e-12, 1e-10, 1e-8, OR 1e-6 in any of
the four — the F-01 split's null-pivot band is empty here, so
F-01 cannot have caused the disagreement either.

Conclusion: there is no feral bug on rocket_12800 iter 1. The
matrix genuinely has 38402 negative eigenvalues (the IPM expected
38400). MUMPS / MA57 would report the same negative count and the
same WrongInertia; their IPM would also regularize at iter 1.

The original issue text:

  "iters 0-9 match reference iter table to 7 sig figs;
   regularization fires at iter 10 where reference doesn't"

predates a stack of feral changes (626ac39 F-03 sparse default to
ForceAccept; 9910fce F-01 split; da23d13 CB-off C-API default;
several PAR_MIN_FLOPS / threadpool tweaks). The IPM trajectory
under those changes is different — iter-1 inertia is not where
feral and the references disagree any more. With current defaults
the run still MaxIters, but the failure mode has shifted into
restoration-phase divergence (objective walks from -1.0e0 to
-1.6e0 to -1.0e0 across iters 9-60 in restoration), which is an
IPM-side line-search/restoration quality story rather than a
linear-solver story.

What this closes: the feral half of #17 is closed. The thread is
"rocket_12800 still MaxIters in pounce-feral", which is now a
pounce-side investigation (restoration-phase tuning, max_soc,
acceptable_tol, mu strategy) — feral reports correct inertia on
every solve we dumped.

Not closed (but moved out of feral scope):
- A fresh capture of rocket_12800 under ipopt-MUMPS to confirm
  the reference IPM does NOT regularize at iter 1, and to dump
  reference inertia per iter for cross-check.
- Pounce-side iter-table reproduction with the previous reference
  to confirm what (if anything) the trajectory shift is due to.
- Whether ~cascade_break_ratio=Some(0.7)~ (the working island on
  robot_1600) is the right thing to recommend for control-class
  Mittelmann problems — separate decision question.

Evidence kept in ~/tmp/rocket_dump.jsonl~ (165 MB, not committed)
and ~/tmp/rocket_solves/solve_*.mtx~ (extracted matrices, not
committed). Reproduction recipe captured here in the journal.

* 21:30 :issue18:narx_cfy:cascade-break:inertia:diagnostic:
Issue #18 NARX_CFy MaxIter root-cause found: pounce-feral's
hard-coded =cascade_break_ratio=0.5, eps=1e-10= default is
introducing inertia errors that trigger IPM WrongInertia loops.
Without cascade-break, feral returns the IPM-expected inertia on
the same matrices.

Repro:
- ~POUNCE_DUMP_KKT=/tmp/narx_dump.jsonl POUNCE_FERAL_REFINE=1
  pounce NARX_CFy.nl max_iter=110~ → 9.3 GB dump, 494 KKT solves.
- Result: MaxIter at iter 110, 252s, =dual_inf=1.0= stuck,
  =constr_viol=8.6e-8= (primal converging, dual stalled).
- Index pass: 125 Success / 369 WrongInertia. WrongInertia at
  =solve_01= already (long, sustained).
- =CAVEAT=: =num_neg_evals_actual= in the dump is
  =last_neg_evals=, only updated on Success. So the dumped =act=
  field is stale on WrongInertia records; must extract MTX and
  re-factor to get true reported inertia.

Extracted 15 representative solves (=/tmp/narx_solves/=) and ran
=diag_robot1600_eigs= (extended with =cb=pounce= config matching
pounce-feral's =Solver::new().with_cascade_break(0.5)
.with_cascade_break_eps(1e-10)=):

| solve | status (dump) | cb=off          | cb=pounce        | smallest |D|       |
|-------+---------------+-----------------+------------------+--------------------|
|   000 | Success       | (48509,48256,0) | (48509,48256,0)  | +1.8e-8 (cb=off)   |
|   001 | WrongInertia  | (48509,48256,0) | (48510,48255,0)* | +2e-16 cb=pounce   |
|   100 | WrongInertia  | (48508,48257,0) | (48507,48258,0)* | -7e-9 cb=off       |
|   250 | WrongInertia  | (48509,48256,0) | (48509,48256,0)  | +1e-8 cb=off       |
|   300 | Success       | (48509,48256,0) | (48509,48256,0)  | +1e-8 cb=off       |
|   400 | WrongInertia  | (48509,48256,0) | (48508,48257,0)* | +1e-8 cb=off       |
|   478 | WrongInertia  | (48509,48256,0) | (48509,48256,0)  | -4e-8 cb=off       |
|   485 | WrongInertia  | (48509,48256,0) | (48509,48256,0)  | +9e-3 cb=off       |
|   491 | WrongInertia  | (48509,48256,0) | (48509,48256,0)  | +1e0  cb=off       |

(* = inertia disagrees with expected)

Expected (IPM): pos=48509, neg=48256, zero=0.

Findings:
1. =cb=off= matches expected on 14 of 15 solves. Only solve_100
   shows a genuine 1-neg overshoot, with smallest |D|=-7.45e-9
   (borderline; near the BK pivot acceptance floor). This is
   indistinguishable from MUMPS/MA57 territory and would
   trigger the same WrongInertia there.
2. =cb=pounce= introduces 100–400 tiny pivots (|D|<1e-10) that
   the inertia routine signs arbitrarily, shifting the count
   by ±1 on solves 001, 100, 400. =solve_001= had
   136 pivots below 1e-12 with cb=pounce vs 0 with cb=off.
3. Solves 485 and 491 (well-conditioned: smallest |D| = 9e-3
   and 1.0) were tagged WrongInertia by pounce but BOTH
   configs return correct inertia in offline reproduction.
   Hypothesis: parallel-reduction non-determinism in the
   factor (default =with_parallel(true)= for both pounce-feral
   and diag, but ordering differs from the actual pounce run).

This parallels the issue #17 robot_1600 finding: cascade-break
helps SOME problems (pinene_3200 — 33ms vs 94s) and hurts
others. The C-API switched to CB-off default in da23d13.
Pounce-feral still hard-codes CB-on at
=pounce-feral/src/lib.rs:67-73=. The same flip — make CB-on
opt-in via =POUNCE_FERAL_CASCADE_BREAK=on= — would be the
direct hardening for #18.

Action proposed (NOT yet executed): mirror the C-API default
in pounce-feral. The robot_1600 hardening + this NARX_CFy
finding form two of two corroborating cases. Pinene_3200 is
the only known cb-on beneficiary; if regression there is
significant, gate via a per-problem heuristic instead.

Evidence kept in ~/tmp/narx_dump.jsonl~ (9.3 GB, not
committed), ~/tmp/narx_solves/solve_*.mtx~ (extracted, not
committed), ~/tmp/narx_index.py~ and ~/tmp/narx_extract.py~
(extractor scripts). Diag extension at
~src/bin/diag_robot1600_eigs.rs~ is uncommitted.

* 11:30 :acopp30:scaling:mc64:issue-23:plateau-2:
Picked up issue #23. After the SSIDS det floor closed the
original 8 ACOPP30 suspects (1.5e-14..1.9e-9 rel_ref), a full
105-matrix sweep via the new ~src/bin/diag_acopp30_residual.rs~
binary revealed 6 NEW plateau matrices at iters 59, 63-67:

| iter | rel_ref  | inertia (feral)   |
|------+----------+-------------------|
| 0059 | 1.88e-06 | (71, 137, 1)      |
| 0063 | 1.56e-01 | (71, 137, 1)      |
| 0064 | 1.74e-01 | (71, 137, 1)      |
| 0065 | 9.07e-02 | (71, 137, 1)      |
| 0066 | 1.49e-01 | (71, 137, 1)      |
| 0067 | 9.07e-02 | (71, 137, 1)      |

All 6 emit min|D|=0 (literal-zero ForceAccept). MUMPS gets 1e-5,
SSIDS gets 1e-13. ~cond1(A) ~ 3e16~.

Built ~src/bin/probe_acopp30_64.rs~ to sweep NumericParams
knobs: pivot_threshold, on_zero_pivot, scaling strategy.
~ScalingStrategy::InfNorm~ and ~ScalingStrategy::Identity~ both
give rel_ref ~ 1e-14 on all 6 plateaus, with a different pivot
pattern (101x1+54x2 vs 115x1+47x2 under Auto/MC64). Forcing
~DIAG_SCALING=infnorm~ on the full sweep: 105/105 ACOPP30
matrices pass rel_ref < 1e-10.

Root cause: ~ScalingStrategy::Auto~ routes ACOPP30 to MC64
(arrow-KKT shape), and Policy 4's first guard at
~src/scaling/mod.rs:284~ skips the diagnostic when
~raw_diag_range >= 1e6~. ACOPP30 has ~raw_drng = 1.06e10~, so
MC64 is used unconditionally. On this family MC64 produces a
catastrophic scaling that yields a zero pivot. The existing
Policy 4 ~mc_off/in_off~ ratio fallback can't catch the case
because both metrics are +inf (both scalings leave dead
diagonals).

Built ~src/bin/probe_scaling_policy4.rs~ over a 9-matrix panel
(ACOPP30, MEYER3NE, MSS1, VESUVIA, VESUVIO, VESUVIOU, HS75,
CRESC132, MUONSINE). Discovered ~in_spread~ (max|s|/min|s| of
the InfNorm scaling vector) is the clean discriminator:

| in_spread bracket | matrices                        | MC64 outcome      |
|-------------------+---------------------------------+-------------------|
| < 100             | ACOPP30 (1.63), MSS1 (1.09),    | mixed — ACOPP30   |
|                   | HS75 (20.8), MUONSINE (99.6)    | BAD               |
| 100..10k          | CRESC132 (859), VESUVIO (1.3e4) | tie               |
| > 10k             | VESUVIA (7.6e7), VESUVIOU       | MC64 strictly     |
|                   | (8.4e5), MEYER3NE (6.6e9)       | wins              |

Proposed fix: pre-MC64 InfNorm trial in
~compute_scaling_auto_with_cache~. If ~max|s|/min|s| < 1e3~,
return InfNorm immediately. Catches the 6 ACOPP30 plateaus
without flipping any of the matrices Policy 4 was designed to
keep on MC64. Research note:
~dev/research/acopp30-plateau-2.md~.

* 22:30 :issue-23:scaling:fix:landed:
~src/scaling/mod.rs::compute_scaling_auto_with_cache~ gained the
~IN_SPREAD_GUARD = 1e3~ pre-MC64 InfNorm trial. Commit 8986679.

Verification:
- ~cargo test --lib scaling~: 26 pass (incl. new
  ~auto_picks_infnorm_on_acopp30_0064~ regression + renamed
  ~auto_picks_infnorm_on_hs75_0000~).
- Bench partitions: dense small/medium 1.38/1.80, sparse 1.74/1.74
  (PASS / PASS).
- 105/105 ACOPP30 pass rel_ref < 1e-10 under default Auto.
- Policy 4 panel: zero regressions.

Pushed origin/main (3 commits ahead: 8986679 fix, d02b39a diag,
2289d1d NARX_CFy test).

Next session goal per checkpoint: issue #33 (Mittelmann clnlbeam,
97% main-thread in scalar 1×1 pivot path; extreme repro of #12
small-front gap).

* 23:30 :issue-33:slb:ab:result:negative:
Ran the SmallLeafBatch on/off A/B that issue #33 suggestion 1
asked for: re-test SLB on the actual 1D-banded Mittelmann panel
(clnlbeam, henon120, lane_emden120, dirichlet120; 20 matrices)
under the hypothesis that real workloads might tip the
signal-vs-noise that Phase 2.11 synthetics failed.

Result: panel geomean 1.002x, median 0.999x, min 0.947x, max
1.071x. 2 wins, 1 loss out of 20. FAILS the #11 criterion (median
≥ 1.10x AND max regression ≤ +5%).

Binary: ~src/bin/diag_clnlbeam_slb.rs~. Research note:
~dev/research/issue-33-slb-ab.md~.

Key finding: the matrices with many small groups (avg 4.7-6.9
members) show ZERO benefit, while the matrices with very wide
groups (avg 200+) show only modest 1.05-1.07x. So per-supernode
driver overhead is NOT the dominant cost. Per-front kernel work
(TPP scan + scalar Schur update) really is the bottleneck.

This is the empirical answer to #11 and re-prioritizes the
roadmap: SLB cannot fix #33; #10 APP must land first. Going to
append a tried-and-rejected entry tomorrow and re-aim at #10.

* 12:00 :issue-10:maxfromm:dense:parity:
MAXFROMM Phase 1 implementation landed. Per the research note
~dev/research/issue-10-app-vs-maxfromm.md~ (committed 03d3bbb):
added ~TppMethod::{Plain, Maxfromm}~ enum and
~BunchKaufmanParams::tpp_method~ (default Plain). Modified
~scalar_pivot_step~ to take a ~cached_maxfromm: &mut Option<f64>~
slot; at the start of each pivot iteration, if the slot is Some
AND the diagonal dominates by alpha, short-circuit the AMAX scan
and route directly to the 1×1-at-k accept path. After
~do_1x1_update~, scan column k+1 once and stash for the next
iteration. Cleared on 2×2 / delay / rejection / panel pass.

Modified 3 callers of ~scalar_pivot_step~ (~factor_frontal~ +
two sites in ~factor_frontal_blocked~) to thread the cache.

Tests: new ~tests/maxfromm_parity.rs~ (5 tests, scalar + blocked
× SPD + indefinite + ncol<nrow). One bug caught on first run:
~factor_frontal_blocked~ did not clear the cache before panel
calls; a stale Some from a post-panel scalar fallback could leak
into the next iteration's scalar step. Fixed by setting
~cached_maxfromm = None~ before every ~lblt_panel_frontal~
call. All 5 parity tests pass after the fix; full ~cargo test
--release~ green (~257 lib tests + all integration tests + 0
failures); clippy clean.

Behavior with default Plain: byte-identical to pre-Phase-1.
Maxfromm requires opt-in via ~tpp_method = TppMethod::Maxfromm~.
Next: Phase 2 corpus validation (clnlbeam panel A/B,
inertia corpus sweep) before considering a default flip.
