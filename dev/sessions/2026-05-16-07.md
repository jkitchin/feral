# Session 2026-05-16-07

## Goal

Close issue #26 (M3 stress-corpus expansion): triple the SuiteSparse
matrix count in `external_benchmarks/stress/manifest.tsv` from 18 to
≥80, covering remaining `GHS_indef` (n ≤ 100k), `Schenk_IBMNA`,
`Schenk_AFE`, and `Boeing/bcsstk*` indef stiffness blocks. Acceptance:
fetch.py succeeds for all rows; run.py completes in < 1 h wall;
report.py is byte-stable across two back-to-back runs.

## Accomplished

- Manifest expanded from 36 rows → 125 rows. SuiteSparse subset
  grew from 18 → 104 (target was ≥ 80; landed with 30% headroom).
- Three SuiteSparse groups added:
  - `Schenk_IBMNA`: 49 indef circuit-Jacobian rows (all n ≤ 100k)
  - `GHS_indef`: 33 new rows across saddle / pde / opt / dense /
    mech / indef categories
  - `Boeing`: 4 indef stiffness rows (`bcsstk35`, `bcsstk37`,
    `bcsstk39`, `nasa1824`)
- `fetch.py` succeeded for every new SuiteSparse row
  (`91 newly downloaded, 21 already present` from the symlinked main
  worktree cache).
- `run.py` full-suite wall: **4 m 35 s** on Vesuvio (default
  hardware), comfortably within the 1 h acceptance ceiling. Per
  matrix factor times: largest GHS_indef (`turon_m`, n=189924) =
  166 ms; Schenk_IBMNA c-XX series sub-millisecond to ~10 ms each.
- `report.py` exit code is **0** (122 ok, 0 flagged, 3 missing
  cuter_kkt rows whose source files live outside the agent worktree).
- `diff /tmp/report_a.txt /tmp/report_b.txt` returned no diff →
  STABLE across back-to-back runs.
- Five matrices dropped after factor-time validation surfaced reader
  incompatibility (pattern-only Boeing nasa2910/4704; integer-only
  GHS_indef aug2d/aug2dc/aug3d). Recorded in `tried-and-rejected.md`.
- Schenk_AFE skipped entirely (all matrices ≥ 504k rows, outside
  the n ≤ 100k tier; SPD-rate is high; near-duplicate mesh slices).
  Recorded in `tried-and-rejected.md`.
- Research note `dev/research/stress-corpus-m3.md` covers group
  selection, category assignment, SPD filter, runtime math,
  pathologies, and final per-category counts.
- CHANGELOG `[Unreleased]` updated under `Added — M3 stress corpus
  expansion (#26)`.
- `cargo fmt --check`: clean. `cargo clippy --all-targets -- -D
  warnings`: clean. `cargo test --lib`: 259 passed, 0 failed.

## Benchmark Results

No new source files were touched, so `cargo run --bin bench --release`
results are unchanged from session 2026-05-15-05 (the calibration
session). The new metric is the stress-suite wall time:

```
$ time python3 external_benchmarks/stress/run.py --time-limit 3600
...
done 127 (failures 0)

=== done ===
sidecars: external_benchmarks/stress/out
analyze with: python3 report.py

real    4m35.174s
user    7m36.408s
sys     0m21.398s
```

`report.py` summary (final pass):

```
total 125: ok=122, flagged=0, missing=3, other=0
=== by category ===
category        total     ok   flag   miss
borderline          3      0      0      3
cascade             2      2      0      0
dense               6      6      0      0
illcond             2      2      0      0
indef              59     59      0      0
mc64_resistant      1      1      0      0
mech                4      4      0      0
near_sing           3      3      0      0
opt                15     15      0      0
pde                 5      5      0      0
rankdef             6      6      0      0
saddle             15     15      0      0
saddle_rankdef      2      2      0      0
stokes              1      1      0      0
wide_frontal        1      1      0      0
```

## Decisions Made

No architectural decisions. Corpus additions only; `decisions.md`
untouched per the agent instructions.

## Abandoned Approaches

- 5 matrices initially added then dropped (pattern + integer MM
  format unsupported by feral's reader). See
  `dev/tried-and-rejected.md` 2026-05-16 entry.
- Schenk_AFE skipped entirely (size out of tier, SPD-dominant).
  See `dev/tried-and-rejected.md` 2026-05-16 entry.

## Next Session Should

- Optional follow-up: extend `src/io/mtx.rs` to accept
  `coordinate pattern symmetric` (synthesize value 1.0) and
  `coordinate integer symmetric` (parse as f64). Re-add the 5
  dropped matrices to the stress manifest.
- Optional follow-up: pick 1–2 representative `af_shell*` matrices
  (Schenk_AFE indef shells) and add as a coarse-mesh stress entry
  to exercise the dense-supernode path on million-row inputs.
- The `borderline` cuter_kkt rows remain "missing" in the agent
  worktree because `data/matrices/kkt/FBRAIN3LS/` is not present.
  This is a worktree-setup nuance, not a bug.
