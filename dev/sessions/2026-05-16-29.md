# Session 2026-05-16-29 — Integration: #13 merge, #18 residual gates, #11 close

## Goal

Continuation after context compaction. Two parallel intents from the
preceding session:

1. Launch issue #13 (per-supernode fixed-overhead reduction) work in a
   background agent, work issue #18 (NARX_CFy convergence stall) in
   the foreground.
2. Land the outputs cleanly: merge #13 to main once its agent reports
   green, ship a residual-gate regression test for #18, and dispose of
   the parallel #11 agent's re-eval result.

## Accomplished

- **#18 — residual-gate regression test (ea98f98).** Extended
  `tests/issue_18_narx_cfy_cascade_off.rs` from 3 inertia tests to 6
  (added `matvec_lower_sym`, `rel_residual`, and three
  `narx_cfy_iter_{0,1,2}_refined_residual_below_gate` tests with a
  1e-10 threshold). Locks the second half of the issue #18 fix —
  catches a future regression where the C ABI's default flip from
  `solve_many` to `solve_many_refined` (597a90a) reverts, or where
  `solve_refined` itself silently becomes a no-op. All 6 tests pass.
  Posted regression-gate summary on #18 (issue stays open pending
  user-side rebuild verification per prior comment).

- **#13 — agent merge + cleanup (0b60d9a).** Background agent
  (`a2e427810b5ec8a16`) finished `perf(dense): in-place scratch-pooled
  scalar fallback`. Verified its worktree commit: cargo test full
  release suite all-green, fmt + clippy --all-targets -D warnings
  clean. Cherry-picked onto main (the worktree branch base diverged
  after my #18 commit), fast-forwarded, pushed. Removed the locked
  worktree + branch. Per-supernode ns/sup wins (−15 % to −41 %) hold
  on CRESC100 / ACOPR30 / KIRBY2 / HAIFAM / HAHN1 / GAUSS2 / AVION2 —
  primary acceptance gate cleared by a wide margin. Posted merge
  confirmation table on #13.

- **#11 — rejection commit + close (40f687c).** Background agent
  (`a0fd99cd1319a3cd0`) re-ran the Phase 2.11 `SmallLeafBatch::On`
  default-flip experiment under the issue's documented protocol
  (5 × 2 variants, interleaved, 66-matrix `comparison/sample.tsv`,
  ~55 min wall). Result REJECT: tiny+small median +1.44 % (criterion
  ≥ +10 %) and worst per-matrix slowdown +50 % (criterion ≤ +5 %)
  both fail. Agent paused for direction on harness disposition;
  resolved on standing "make-the-call" instruction. Approved the
  agent's strict reading of the failure-path workflow (drop the
  one-off `FERAL_SMALL_LEAF` env-var hook + the
  `issue_11_ab.py` driver from tree; reproduction recipe carried by
  the rejection entry + journal). Single commit lands
  `dev/tried-and-rejected.md` + session checkpoint + journal. Posted
  closing comment on #11 (issue had already auto-closed; comment
  records the result).

## Benchmark Results

No `cargo run --bin bench --release` this session — this was an
integration session over three independent agent outputs; the
authoritative perf numbers live in each merged commit's body:

- #13: `dev/journal/` + commit `0b60d9a` body — per-supernode ns/sup
  table for 8 long-tail KKT matrices.
- #11: `dev/tried-and-rejected.md` 2026-05-16 entry — per-bucket
  speedup / worst-slowdown table on 66-matrix comparison sample.

## Decisions Made

None new in main (decisions.md is unchanged this session). The
#11 disposition is documented in `dev/tried-and-rejected.md`, not
`dev/decisions.md`, per the failure-path workflow.

## Abandoned Approaches

- `SmallLeafBatch::On` default flip, post-SIMD + post-APP regime —
  rejected; see `dev/tried-and-rejected.md` 2026-05-16 entry.

## Next Session Should

- Investigate the **+7.7 % `large` bucket median speedup** the #11
  re-eval surfaced (with +12.5 % worst slowdown). It's out of scope
  for #11 but a real signal for a separate small-flip experiment on
  the large-front regime. Likely candidate: differentiate
  `SmallLeafBatch` thresholds by frontal width / nrow rather than
  a single global toggle.
- Confirm with user whether ipopt-feral rebuild + re-run on NARX_CFy
  has happened so #18 can be closed.
- Cleanup pass on stale agent worktrees (11 locked worktrees still
  on disk under `.claude/worktrees/`, several from completed agents).
  Not done this session to avoid stepping on in-flight work.
