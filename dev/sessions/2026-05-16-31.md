# Session 2026-05-16-31 — Land the #38 MC64-cache-staleness fix

## Goal

Land the fix identified in session-30 (stale MC64 cache reused across
warm `Solver::factor` calls produces silently wrong inertia and
explodes cost on real arrow-KKTs). Per session-30's "next session
should":

- Write research note `dev/research/mc64-cache-staleness-2026-05-16.md`
- Land the fix + regression test
- Re-test #37 (pinene_3200) under the fix
- Post finding-comment on #38

## Accomplished

- **Fix landed: db20166** `fix(#38): invalidate stale MC64 cache
  between factor() calls`. One-shot cache invalidation at the end of
  `Solver::factor`: clears `last_symbolic.cached_mc64` after every
  numeric call. The cache stays valid for the first numeric call
  after symbolic (values match by construction); subsequent calls
  fall through to a fresh `mc64::compute_symmetric(matrix)` against
  current values. Cost: one extra MC64 (~100–200 ms on n ≈ 1e5) per
  warm refactor when scaling resolves to `Mc64Symmetric`.

- **Regression test**
  `numeric::solver::tests::mc64_cache_invalidated_after_factor_issue_38`.
  Inspects `last_symbolic.cached_mc64` directly after one `factor()`
  call and asserts it is `None`. Field-inspection rather than
  behavioural: Sylvester's law keeps inertia invariant under any
  symmetric scaling on well-conditioned small matrices, so the
  downstream wrong-inertia symptom only manifests on large arrow-KKTs
  — a 4×4 reproducer is insensitive. Verified the test fails when the
  fix is removed (panics on the assertion) and passes when restored.

- **Test suite green.** `cargo test --release` exit 0;
  `cargo fmt --check` and `cargo clippy --all-targets -- -D warnings`
  pass via pre-commit hooks.

- **#38 finding-comment posted**:
  <https://github.com/jkitchin/feral/issues/38#issuecomment-4468206937>.
  Reframes the issue as silent inertia corruption (not just "warm
  slowdown") with the four diagnostic tables (warm replay,
  warm-vs-fresh, PAR=0 localisation, InfNorm control) from
  session-30. Closes Failure A; leaves issue open for Failure B
  (CB=on iter-4 disagreement vs MA57) which is a separate
  investigation.

- **Research note written**:
  `dev/research/mc64-cache-staleness-2026-05-16.md`. Covers the
  5-step failure trace, why the symptom is drifting inertia rather
  than obviously wrong solve (BK pivoting destabilises under
  mis-scaling), the four fix options analysed against the IPM use
  case, all diagnostic evidence tables, and the relationship to #37.

- **#37 retest done; cache fix is NOT a rescue for pinene.** Ran
  `probe_pinene_issue38_fix` (uncommitted) through 10 `pinene_3200`
  dumps with default warm `Solver::new()` under the fix:

  | iter | factor | inertia |
  |---:|---:|---|
  | 0 | 0.983 s | OK (64000 / 63995 / 0) |
  | 1 | 0.917 s | OK |
  | 2 | 0.706 s | OK |
  | 3 | 0.872 s | OK |
  | 4 | 0.940 s | OK |
  | 5 | 451.871 s | OK |
  | 6+ | killed @ 600 s wall | (incomplete) |

  Inertia stays correct throughout (no silent corruption). The
  iter-5 cost explosion is the wide-supernode cascade the #37 issue
  body diagnosed — independent of the MC64 cache. Session-30's
  "likely also rescues #37" hypothesis is dead. #37 remains closed
  under the per-problem `feral_cascade_break=yes` mitigation.

- **#18 (NARX_CFy): user reports ipopt-feral now solves it.** That's
  the user-side rebuild + re-run gate session-29 was waiting on.
  Feral-side fix chain: 597a90a (default `feral_solve` →
  `solve_many_refined`) gated by 2289d1d (inertia trajectory tests)
  and ea98f98 (refined-solve 1e-10 residual gate). Issue can close
  pending the user's confirmation comment with iter count /
  wallclock from their run.

## Benchmark Results

`cargo run --bin bench --release` — both Phase 2.8.1 partition gates
pass at p90, no regressions:

```
--- Dense Phase 2.8.1 exit partition (factor ratio vs MUMPS) ---
bucket                    count      p90     target  verdict
small-frontal (<200)     147982     1.37     <= 2.0     PASS
medium (<500)            152145     1.87     <= 3.0     PASS

--- Sparse Phase 2.8.1 exit partition (factor ratio vs MUMPS) ---
bucket                    count      p90     target  verdict
small-frontal (<200)     153455     1.74     <= 2.0     PASS
medium (<500)            153560     1.75     <= 3.0     PASS
```

Top-10 worst per-matrix ratios unchanged from session-30 baseline
(MUONSINE_0000 30.6×, KIRBY2_*  ~6-8×, SWOPF_* ~6×). The fix only
touches a single boolean assignment in `Solver::factor`; bench
matrices factor cold so the cache path isn't exercised here.

## Decisions Made

None new. The cache-invalidation strategy chosen (option 2 in
session-30's plan: recompute on every `factor()` when scaling
resolves to `Mc64Symmetric`) is documented in the research note;
options 3 (revalidation) and 4 (MC64 warm-start) explicitly
deferred.

## Abandoned Approaches

- **Behavioural integration test on 4×4 block-antidiag matrix.**
  Initially wrote `tests/issue_38_mc64_cache_warm_vs_fresh.rs`
  asserting `warm.inertia() == fresh.inertia()` across two
  value-perturbed matrices on the same pattern. The test passed
  with the fix removed — Sylvester's law of inertia preserves
  inertia under any symmetric scaling, so on small well-conditioned
  matrices the bug doesn't manifest behaviourally. Removed; replaced
  with the in-module field-inspection unit test which actually
  catches the regression. Recorded in tried-and-rejected.

## Next Session Should

- **Close #18 with the fix chain summary**, pending user-side
  confirmation of iter count / wallclock from the successful
  ipopt-feral NARX_CFy run. (User signalled the run succeeds; the
  close comment should cite 597a90a → 2289d1d → ea98f98 and the
  end-to-end ipopt-feral evidence.)

- **#38 Failure B** (CB=on iter-4 numerical disagreement vs MA57
  on rocket_12800). Separate mechanism from Failure A; not
  addressed by the cache fix. Needs its own reproduction + root
  cause; the rocket dumps in `/tmp/rkt_*.bin` are still usable.

- **#37 wide-supernode cascade** (independent track). The pinene
  probe in this session confirmed the cache fix is not a rescue.
  The cascade-break-arming strategy is a real algorithmic gap;
  session-30 ruled out the AutoRace ordering flip. Three options
  remain from #37's issue body: (1) document-and-ship the
  per-problem opt-in (current state), (2) shape-based default
  (detect wide trailing supernodes during symbolic and arm CB for
  that solve), (3) fix the underlying robot_1600 mis-fire so CB=on
  can be the global default.

- Outstanding from prior sessions (not touched here):
  - +7.7 % large-bucket median speedup signal from #11 re-eval
  - Stale agent worktree cleanup under `.claude/worktrees/`

## Reproducer notes

The pinene probe (`src/bin/probe_pinene_issue38_fix.rs`) stays
uncommitted alongside the session-30 helpers (`feral_replay.rs`,
`feral_time.rs`, `Makefile`). Run with:

```
cargo run --release --bin probe_pinene_issue38_fix
```

It expects `data/matrices/kkt-mittelmann/pinene_3200/pinene_3200_NNNN.{mtx,json}`
present. Output table is reproduced verbatim in the research note
and journal §18:30.
