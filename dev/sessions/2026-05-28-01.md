# Session 2026-05-28-01

## Goal

Continuation of issue #56 ("Dense Thomson-Hessian per-iter throughput:
1.57×–2.34× slower than MUMPS on elec*"). Branch
`issue-56-thomson-hessian-throughput` already had Lever A (permute
cache + pre-built permuted_pattern) and the Phase 2/3 probe scaffold
from prior sessions; this session was charged with finishing the
remaining throughput levers identified by the per-phase probe,
re-validating, closing #56, and merging.

## Accomplished

- **Lever B (`c33f023`) — fused single-pass `contribextract` write.**
  Phase 2 sub-phase drill-down localized 27–32 % of total wall at
  n=200 to `dense_bookkeeping`, with 1435 µs (10 % of total wall) in
  `contribextract` and 289 µs of that in the resize-zero pre-pass.
  Replaced `resize(cdim², 0.0) + lower-triangle overwrite` (every
  lower-triangle cell written twice) with
  `reserve + unsafe set_len + single-pass loop` writing each cell
  exactly once (zero for upper-triangle, a-value for lower-triangle).
  Bit-identical to the prior contrib block — `extend_add` reads only
  `ci ≥ cj`, root-Schur extractor canonicalizes via transpose, and
  `parallel_corpus_parity` binary-compares the full buffer. Long-form
  safety comment at the `factor_frontal` site (f64 has no Drop →
  briefly-uninitialized bytes are sound; loop writes every cell
  before any read).

  Re-measurement at n=200 (darwin/aarch64, 9 warm reps):
  - factor min (par on): 19731 → 18648 µs (−5.5 %)
  - factor min (par off): 18528 → 16597 µs (−10 %)
  - contribextract: 1435 → 850 µs (−41 %)
  - contribzerofill: 289 → 3 µs (≈ gone)
  - factor min at n=100 (par on): 2977 → 2753 µs (−8 %)
  - n=50: within noise

- **Phase 3 probe extension (`b0b9b7e`) — already on branch from prior
  session, formalized this session.** `per_phase_breakdown_via_solver`
  drives `Solver::factor()` and snapshots `mc64_cache_hit_count()` /
  `scaling_info()` per rep. Finding: MC64 scaling cache is **correctly
  inactive** on Thomson — `pick_scaling_strategy` picks InfNorm, and
  InfNorm is deliberately not cached per #49 (replays a stale iter-0
  scaling on a drifted iter-N matrix). The Phase 2 prologue
  `scaling_us` is real recurring per-iter work, not a cache miss.

- **Lever C (`5de817b`) — vectorize InfNorm inner loop (hoist + pulp
  SIMD).** Live instrumentation of the KR loop revealed it never
  converges on Thomson: `max_dev` decays geometrically at ratio
  0.5/iter and plateaus at 6.77e-3 — six orders away from the 1e-8
  tolerance — so all 10 iters fire every solve. Two transforms:
  1. **Hoist `row_max[j]` to a register accumulator** in both sparse
     (`compute_infnorm`) and dense (`compute_infnorm_dense`) paths.
     Loop-carried memory dep on `row_max[j]` is replaced by a local
     `col_max` folded once at column end. Diagonal's `row_max[i]`
     write is elided (would be overwritten by end-of-column store);
     its contribution lands in `col_max` instead. Bit-identical:
     max(·,·) is associative on the non-NaN finite inputs here.
  2. **pulp-dispatched SIMD off-diag kernel** for the dense path
     (`scan_offdiag_simd`). Per-lane `mul → mul → abs`, lane-wise
     `max_f64s` into `row_max[i:i+W]`, accumulate `col_max_v` in a
     vector register, `reduce_max_f64s` once per column. Tail via
     `partial_load_f64s` / `partial_store_f64s` — out-of-range lanes
     load zero (max identity). Dispatched through `pulp::Arch::new()`
     (AVX-512 / AVX2+FMA / SSE2 / NEON / scalar fallback).

  Sparse path stays scalar — `row_idx[k]` gather defeats NEON and
  typically loses on AVX2/AVX-512 for short-column sparse-symmetric
  KKTs.

  Re-measurement at n=200 (Phase 2 sequential driver, 3 runs, median):
  - scaling (MC64/InfNorm): 170 → 138 µs (−19 %)
  - total wall: 496 → 473 µs (−5 %)

  Thomson takes the sparse path (n=200 is past the dense fast-path
  gate), so the Thomson win comes from the scalar hoist alone. The
  SIMD kernel is exercised by dense-fast-path-eligible matrices
  (TRO3X3_0013-class small-dense KKTs) and verified by the existing
  `dense_matches_sparse_on_arrow_6x6` / `dense_matches_sparse_on_dense_5x5`
  bit-exact parity tests.

- **Issue #56 closed** with a comment listing the three levers, the
  measurement evidence, and the unresolved items flagged for future
  issues (adaptive KR iter cap, per-supernode dispatch overhead,
  parallel-driver profiler instrumentation).

- **Branch merged fast-forward into `main`** (no main divergence;
  branch was purely additive). `main` is now at `5de817b` and 17
  commits ahead of `origin/main`.

### Evidence
```
cargo test --release                          317 passed; 0 failed
cargo clippy --release --all-targets -- -D warnings    clean
cargo fmt --check                                       clean
cargo run --bin bench --release                         all partition gates PASS
```

## Benchmark Results

```
--- Dense Phase 2.8.1 exit partition (factor ratio vs MUMPS) ---
bucket                    count      p90     target  verdict
small-frontal (<200)     147982     1.33     <= 2.0     PASS
medium (<500)            152145     1.78     <= 3.0     PASS

--- Sparse Phase 2.8.1 exit partition (factor ratio vs MUMPS) ---
bucket                    count      p90     target  verdict
small-frontal (<200)     153455     1.51     <= 2.0     PASS
medium (<500)            153560     1.51     <= 3.0     PASS
```

Compared to 2026-05-27-01 baseline:
- Dense small-frontal p90: 1.37 → 1.33 (−0.04, better)
- Sparse small-frontal p90: 1.59 → 1.51 (−0.08, better)
- Sparse medium p90: 1.60 → 1.51 (−0.09, better)
- Top sparse outlier shifted: KIRBY2_0007 10.25× → 7.97× (better)

The Lever B + C wins materialize in the small-frontal bucket where
the per-supernode bookkeeping dominates and InfNorm cost is a larger
fraction of total wall.

## Decisions Made

- **InfNorm KR inner loop hoists `row_max[j]` to a register
  accumulator** (`src/scaling/infnorm.rs`). The hoist is bit-exact
  by associativity of max on non-NaN finite inputs; it removes the
  loop-carried memory dep that prevented register allocation. The
  diagonal entry's `row_max[i]` write is elided in favor of folding
  through `col_max` — the end-of-column store would overwrite it
  anyway. Documented in code with the bit-identical-by-associativity
  argument. (Appended to `dev/decisions.md`.)

- **Dense InfNorm off-diagonal sweep is pulp-dispatched.** Single
  boundary between this module and `pulp` (mirroring the
  `dense/schur_kernel.rs` Phase 2.4.2 convention). Sparse path stays
  scalar — gather defeats NEON. (Appended to `dev/decisions.md`.)

## Abandoned Approaches

None. The two "could have done" levers from the user-visible options
list (adaptive KR iter cap, Identity scaling pounce-side `.opt`) were
deferred to future work rather than abandoned — they change scaling
quality and require corpus-consensus inertia validation that's out
of scope for the perf-only acceptance of #56.

## Next Session Should

1. Decide whether the issue #55 follow-up worst-case sparse outlier
   (KIRBY2 family, now 7.97× after Lever C improved it from 10.25×)
   needs targeted work or stays in the long-tail.
2. If revisiting InfNorm cost, open a new issue for **adaptive
   KR iter cap with rate-saturation early-stop**
   (`break when dev[k]/dev[k-1] > 0.5 and dev < 1.0`). Requires
   corpus-consensus inertia validation — this is the next ≈18 % of
   total wall on Thomson but it changes scaling quality.
3. If Thomson per-iter still lags vs Ipopt after merging this branch
   into pounce's main, profile the **per-supernode allocation +
   dispatch overhead** (Lever B's residual ~4.5 ms at n=200 attributed
   to per-supernode perm + d_subdiag clone + FrontalFactors return).
   This is a deeper structural change — likely worth its own issue
   with a design plan.
4. `git push origin main` when ready — local main is 17 commits
   ahead of `origin/main`.
