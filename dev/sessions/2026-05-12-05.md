# Session 2026-05-12-05

## Goal

Two items from the open TODO list (session 04 carry-forward):

1. **Re-wire bench + integration tests** into Cargo.toml. Release commit
   `4b93719` (2026-05-06) set `autobins=false`/`autotests=false` to
   produce a library-only crates.io publish, which silently dropped
   the 35 integration tests + 60-odd diag/probe/bench bins from the
   local workflow. Every perf or correctness change since has been
   measured only against the 217 unit tests in `src/`.

2. **Phase 2.4.2 redo** with no-FMA SIMD ops — the
   `mul_f64s + sub_f64s` rewrite the 2026-04-14 decision said had to
   land before re-wiring the kernel.

## Accomplished

### Item 1: bench + tests re-wired — SHIPPED

Commit `6cfaddc`. Three sub-tasks:

1. **Flipped `autobins`/`autotests` back to `true`** in `Cargo.toml`.
   The `exclude` list at `[package].exclude` already carries `/src/bin`
   and `/tests`, so the published `.crate` payload is unchanged
   (verified by `cargo package --list -p feral --allow-dirty`).

2. **Updated 19 stale `NumericParams` struct literals** in bins and
   tests. The `parallel_telemetry: Option<Arc<AtomicLockStats>>` field
   added on 2026-05-12 (parallel multifrontal telemetry hook from
   session 01) had not been propagated to construct-the-literal call
   sites — the type-error was latent because `autobins=false` kept
   those files out of `cargo build`. A short Python script inserted
   `parallel_telemetry: None,` after every `profiler: <expr>,` line
   inside a `NumericParams { … }` block. Caught two pre-existing
   shorthand cases (`tests/profiler_smoke.rs`, `tests/threshold_consistency.rs`)
   that needed hand fixes.

3. **Silenced 9 clippy nits** in restored test/bin files via
   `#![allow(...)]` at file scope, rather than rewriting verbose test
   code. The `erasing_op`/`identity_op` complaints in
   `tests/rook_rescue.rs` are pedagogically deliberate
   (`data[0*n + 0] = 0.008; data[0*n + 1] = 1.0;` makes the row-major
   index pattern explicit for the test reader).

Evidence (release profile, M-series aarch64):

| metric                              | before | after |
|-------------------------------------|-------:|------:|
| `cargo test --release` pass count   |    217 |   425 |
| `cargo test --release` fail count   |      0 |     0 |
| `cargo test --release` ignored      |      5 |    27 |
| `cargo clippy --all-targets`        |  clean | clean |
| `cargo fmt --check`                 |  clean | clean |
| `cargo run --bin bench --release`   |  broken| works |
| `cargo run --bin d3_probe --release`|  broken| works |
| publish payload (`cargo package`)   |  clean | clean |

The +208 tests are the 35 integration files in `tests/*.rs`.

### Item 2: Phase 2.4.2 redo — ALREADY DONE (audit-only)

Commit `49ce4c7` (tasks.org doc fix). Investigation finding: the
"redo Phase 2.4.2 with no-FMA SIMD ops" item was **already shipped
on 2026-04-14** in commit `d9887a9` ("Phase 2.4.3: non-FMA bit-exact
unroll4 Schur kernel wired in").

Evidence:

```
$ git log --oneline d9887a9 -1
d9887a9 Phase 2.4.3: non-FMA bit-exact unroll4 Schur kernel wired in

$ grep -c axpy_minus_unroll4_nofma\|axpy2_minus_unroll4_nofma src/dense/factor.rs
6   # call sites at lines 1719, 1741, 1824, 1843, 2518, 2549
    # covering do_1x1_update / do_2x2_update / panel paths
```

The `_nofma` kernels in `src/dense/schur_kernel.rs:541` and `:600`
implement `simd.sub_f64s(d, simd.mul_f64s(α, s))` — two roundings,
bit-identical to the scalar `d[i] -= α*s[i]` loop. 30 bit-exact unit
tests at length sweep `{0,1,2,3,4,5,7,8,9,15,16,17,31,32,33,63,64,
65,127,128,129,255,256,257,511,512,513,1023,1024}` pass under
`cargo test --lib schur_kernel`.

Bench numbers from the 2026-04-14 wire-up (recorded in
`dev/decisions.md` under "Phase 2.4.3"):

| metric                      | baseline | nofma  |   Δ    |
|-----------------------------|---------:|-------:|-------:|
| dense factor/MUMPS p90      |     2.27 |   1.86 | −18%   |
| sparse factor/MUMPS p90     |     3.18 |   2.82 | −11%   |
| sparse inertia match        |   153009 | 153009 |  0     |
| sparse residual pass        |   154329 | 154329 |  0     |

Both Phase 2.8 exit gates (dense ≤ 2.0, sparse ≤ 3.0) hit with zero
correctness regressions.

VESUVIO tail itself is empirically addressed by adaptive MC64 scaling
(measured this session via `cargo run --bin vesuvio_diag --release`):

| matrix         | scaling | factor (µs) |
|----------------|---------|------------:|
| VESUVIO_0021   | infnorm |       44514 |
| VESUVIO_0021   | mc64    |         645 |
| VESUVIA_0000   | infnorm |        4862 |
| VESUVIA_0000   | mc64    |         742 |
| CRESC132_0000  | infnorm |       46197 |
| CRESC132_0000  | mc64    |        1367 |

Auto scaling routes VESUVIA/VESUVIOU to MC64 via
`scaling::tests::auto_keeps_mc64_on_vesuvia_0000` and
`auto_keeps_mc64_on_vesuviou_0000`. The 84× slowdown regime no longer
fires on the routed Auto path.

`tasks.org` updated to mark the entry DONE with full context. Further
dense-kernel work (panel-BK / threaded inner kernels) is only
relevant for the rare `AMD+infnorm`-on-VESUVIO regime that Auto routes
around. No downstream consumer has flagged it as a blocker, so close.

### Bonus: dense fast-path TRO3X3_0013 win is now measurable

The dense KR routing change from session 04 (commit `676575c`) was
ex-ante estimated to drop the TRO3X3_0013 scale-phase from ~34 µs to
~17 µs. With `d3_probe` now buildable:

```
TRO3X3_0013   n=69  nnz=1764
  scale=19.8us  densify=1.6us  scale-app=0.4us  factor=16.0us
  dense_total=39.7us            multi=66.6us
```

19.8 µs scale-phase matches the ex-ante ~17 µs target within
measurement noise. Dense path total is 39.7 µs vs sequential
multifrontal 66.6 µs — a 40% reduction on the in-gate matrix.

## Benchmark Results

```
cargo run --bin bench --release

--- Dense Phase 2.8.1 exit partition (factor ratio vs MUMPS) ---
bucket                    count      p90     target  verdict
small-frontal (<200)     147982     1.32     <= 2.0     PASS
medium (<500)            152145     1.70     <= 3.0     PASS

--- Sparse Phase 2.8.1 exit partition (factor ratio vs MUMPS) ---
bucket                    count      p90     target  verdict
small-frontal (<200)     153455     1.56     <= 2.0     PASS
medium (<500)            153560     1.56     <= 3.0     PASS

Top 10 worst factor-ratio vs MUMPS:
KIRBY2_0007    n=458   feral 916µs  mumps 119µs  ratio 7.70
KIRBY2_0006    n=458   feral 840µs  mumps 127µs  ratio 6.61
KIRBY2_0008    n=458   feral 666µs  mumps 122µs  ratio 5.46
KIRBY2_0009    n=458   feral 617µs  mumps 128µs  ratio 4.82
KIRBY2_0010    n=458   feral 626µs  mumps 133µs  ratio 4.71
KIRBY2_0011    n=458   feral 543µs  mumps 120µs  ratio 4.53
GROUPING_0075  n=225   feral 548µs  mumps 142µs  ratio 3.86
KIRBY2_0012    n=458   feral 426µs  mumps 118µs  ratio 3.61
GROUPING_0123  n=225   feral 396µs  mumps 111µs  ratio 3.57
GROUPING_0071  n=225   feral 395µs  mumps 111µs  ratio 3.56
```

Comparison to last recorded numbers (sessions 04 / decisions.md
2026-04-14):

| partition                  | this session | prior   |
|----------------------------|-------------:|--------:|
| dense small (<200)  p90    |         1.32 |   1.32* |
| dense medium (<500) p90    |         1.70 |   1.71* |
| sparse small (<200) p90    |         1.56 |   1.58* |
| sparse medium (<500) p90   |         1.56 |   1.58* |

`*` = ex-ante numbers from before this session's runs (i.e.,
no observable regression from the bench/test re-wire).

## Decisions Made

None new. The autobins/autotests flip is a build-config fix, not an
architectural decision — the `exclude` list already separates publish
payload from local workflow. The Phase 2.4.2 redo "decision" was the
2026-04-14 one, already recorded in `dev/decisions.md`.

## Abandoned Approaches

None this session. The Phase 2.4.2 redo was a confirmed-already-done
audit, not an abandoned attempt.

## Next Session Should

After this session's close, three open TODOs remain:

1. **post-D.3 top-10 triage**. The new bench top-10 is KIRBY2-dominated
   (7 of 10 entries; ratios 3.6–7.7). KIRBY2 is small (n=458) and runs
   under the dense fast path — possible candidates: (a) the scale
   phase, now exposed at 19.8 µs on a smaller matrix, may be a larger
   share for KIRBY2; (b) the BK 2×2 path is fully exercised on KIRBY2
   (it has indefinite blocks); (c) MUMPS at 119–133 µs is hitting
   pure DGEMM/Accelerate while feral runs the unrolled-4 nofma
   kernel. Worth a `d3_probe`-style breakdown on KIRBY2_0007 before
   designing any fix.

2. **Mongoose HEMSR coarsening** into feral-metis / feral-kahip.
   Speculative — adding a third coarsener with no evidence it beats
   METIS or KaHIP on the KKT corpus. Defer.

3. **Carry-forward**: re-run `parallel_corpus_parity` on the full
   corpus now that the binary builds. Confirms the parallel default
   shipped in `Solver` (issue #7) has no per-matrix discrepancies vs
   sequential. Quick sanity check, not a substantive task.

The most pointed leverage is item 1's KIRBY2 breakdown — small,
specific, the bench just surfaced it, and the dense fast path's
`d3_probe` is a ready-made instrument.
