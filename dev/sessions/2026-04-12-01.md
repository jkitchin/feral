# Session 2026-04-12-01 — Phase 1b Exit

## Goal

Close Phase 1b. Investigate remaining failures, build a principled
multi-source ground truth (rmumps + canonical Fortran MUMPS 5.8.2 +
canonical Fortran SSIDS + feral), compute consensus verdicts, and
declare Phase 1b complete under a criterion that is honest about
where solvers legitimately disagree.

## Accomplished

### Structural bug fixes (rewriting the solver's correctness baseline)

1. **Postorder pipeline fix** (`7a303d0`) — `symbolic_factorize` never
   applied postorder to the elimination tree before supernode
   detection, so merged supernodes had non-contiguous columns while
   downstream code assumed contiguous ranges. This was the root cause
   of the sparse path's wrong inertia and catastrophic residuals.
   Closed MGH10S_0000 (inertia (50,1,0) → (35,16,0), residual
   2.61e21 → 1.10e-16). Per-corpus delta: sparse inertia match
   98.6% → 99.3%, sparse worst residual 2.61e21 → 3.14e-4.

2. **Best-iterate refinement** (`d954c73`, follows `eab0042`) — naive
   refinement was amplifying error on rank-deficient matrices where
   ForceAccept produced a wrong `A⁻¹`. Best-iterate strategy tracks
   the smallest `||r||` seen across refinement steps and returns the
   corresponding `x`. Closed POLAK6_0021 refinement regression
   (8.97e-1 → 4.6e-17 after threshold-mismatch fix).

3. **Threshold-mismatch fix** (`95e6760`) — factor flagged pivots as
   zero at `100*eps = 2.22e-14` while solve divided by them at
   `eps*1e-10 = 2.22e-26`. The band in between produced catastrophic
   cancellation. Added `zero_tol` + `zero_tol_2x2` fields to `Factors`
   and `FrontalFactors`; both solve paths now consult the stored
   threshold.

4. **`zero_tol = eps` default** (`ef9e103`) — the `100*eps` default
   was too aggressive. Tiny-but-legitimately-positive pivots (e.g.
   `1.83e-14` on CERI651DLS_0534) were being flagged as zero on small
   SPD matrices. Changed the default to `f64::EPSILON` after verifying
   that canonical MUMPS, SSIDS, and rmumps all classify such pivots
   as positive. Closed the final 32 Definitive feral failures.

### New infrastructure

5. **Native Fortran MUMPS 5.8.2 oracle** (`1b5a44e`) — built
   `ref/mumps` with gfortran 15.2 + OpenBLAS; wrote
   `external_benchmarks/mumps_oracle/{Makefile, Makefile.inc.mumps,
   mumps_bench.F, run_mumps.py}`. The Fortran driver reads a manifest
   of matrix paths, reuses one DMUMPS instance, and writes per-matrix
   canonical JSON sidecars. Python wrapper handles JSON I/O.

6. **Native SPRAL/SSIDS oracle** (`3365d35`) — built `ref/spral` with
   meson after installing METIS; wrote
   `external_benchmarks/ssids_oracle/{Makefile, ssids_bench.f90,
   run_ssids.py}`. Requires `OMP_CANCELLATION=true`.

7. **Consensus computation** (`8b4ec3b`) — wrote
   `external_benchmarks/consensus/compute_consensus.py` applying the
   consensus rules from `dev/plans/phase-1b-consensus-exit.md`.
   Classifies each matrix as Definitive / Borderline /
   NumericallyIntractable / Excluded based on inertia + residual
   agreement across feral, rmumps, MUMPS, and SSIDS.

8. **Bench failure analysis** (`3428715`) — added family-grouped
   failure tables and dense ∩ sparse cross-comparison to the bench
   output. Removed the obsolete 153k-line per-row PASS print.

9. **`FERAL_EMIT_SIDECARS` bench flag** — added sidecar emission so
   feral writes `.feral.json` files in the canonical schema alongside
   the MUMPS and SSIDS outputs. Allows the consensus script to read
   all four oracles uniformly.

## Benchmark Results

Final bench run (`cargo run --release --bin bench`):

```
KKT summary: 153151/153151 total
  Inertia match: 152147/153151 (99.3%)
  Residual pass: 152721/153151 (99.7%)
  Worst residual: 3.99e-2 (ACOPP30_0002)

Sparse solver: 153151/153151 total
  Inertia match vs MUMPS: 152043/153151 (99.3%)
  Residual pass:  152788/153151 (99.8%)
  Worst residual: 3.14e-4 (ERRINBAR_0824)
```

(These numbers compare feral to the rmumps sidecar oracle. The
real Phase 1b exit verdict is the consensus analysis below, not
these rmumps-based counts.)

### Phase 1b consensus exit verdict

```
Verdict counts on 169,585 matrices analyzed:
  Definitive               153,117  (90.29%)
  Borderline                   750  ( 0.44%)
  NumericallyIntractable    13,377  ( 7.89%)
  Excluded                   2,341  ( 1.38%)

Feral failures on Definitive matrices:  0
Feral failures on Borderline matrices:  0
```

Pairwise inertia agreement across the four oracles:

| Pair                 | Match    | Total    | %      |
|----------------------|----------|----------|--------|
| feral  vs MUMPS      | 152,094  | 152,145  | 99.97% |
| feral  vs SSIDS      | 152,779  | 153,151  | 99.76% |
| feral  vs rmumps     | 152,147  | 153,151  | 99.34% |
| mumps  vs ssids      | 153,172  | 155,899  | 98.25% |
| rmumps vs mumps      | 152,243  | 155,899  | 97.65% |
| rmumps vs ssids      | 152,709  | 156,927  | 97.31% |

**Feral agrees with canonical Fortran MUMPS on 99.97% of the corpus**
— *higher* than MUMPS vs SSIDS agreement (98.25%) or any pair
involving rmumps.

### Session delta vs pre-session baseline (`8b611f1`)

| Metric                         | Before     | After      | Δ           |
|--------------------------------|------------|------------|-------------|
| Dense inertia match vs rmumps  | 151,930    | 152,147    | +217        |
| Dense residual pass            | 152,489    | 152,721    | +232        |
| Sparse inertia match           | 150,997    | 152,043    | +1,046      |
| Sparse residual pass           | 151,626    | 152,788    | +1,162      |
| Sparse worst residual          | 2.61e21    | 3.14e-4    | 25 orders   |
| Definitive feral failures      | unmeasured | 0          | —           |
| feral vs canonical MUMPS       | unmeasured | 99.97%     | —           |

Tests: 121/121 pass (116 existing + 5 new in `tests/threshold_consistency.rs`).
Clippy: clean.

## Decisions Made

- **Phase 1b exit criterion redefined** — multi-source consensus across
  feral, rmumps, canonical Fortran MUMPS 5.8.2, and canonical Fortran
  SSIDS. Per-matrix verdicts of Definitive / Borderline /
  NumericallyIntractable / Excluded. Phase 1b exits when feral matches
  the consensus on every Definitive matrix. Recorded in
  `dev/decisions.md` and `dev/plans/phase-1b-consensus-exit.md`. The
  original strict 100%-vs-rmumps criterion is superseded because rmumps
  is itself a Rust port with its own bugs that cannot be used as ground
  truth without independent validation.

- **Best-iterate refinement** — dense and sparse `solve_refined` both
  track the smallest residual seen across refinement steps and return
  the corresponding `x`, rather than unconditionally accepting every
  correction. Removes a class of regressions on rank-deficient KKT
  matrices.

- **`zero_tol = f64::EPSILON` default** — reduced from `100 * EPSILON`.
  The 100× safety margin was firing on small SPD matrices with
  legitimate tiny positive pivots. Validated against 3 canonical
  oracles.

- **`external_benchmarks/` top-level directory** — Fortran oracle
  infrastructure lives outside the core Rust crate, is not built by
  cargo, and is not in CI. Feral itself remains pure Rust with zero
  non-Rust dependencies.

## Abandoned Approaches

- **Trace-based 2×2 inertia fix** (deferred) — found a real bug in
  `src/dense/factor.rs::count_2x2_inertia` where the near-singular
  branch uses `a00` instead of the trace to decide the sign of the
  non-zero eigenvalue. The mathematically correct fix caused a
  16-matrix dense regression vs rmumps because rmumps itself uses the
  buggy a00-based logic. Reverted; left a `KNOWN BUG` comment and
  documented in `dev/tried-and-rejected.md`. Re-evaluate now that
  canonical MUMPS is available as a second oracle — the consensus
  data may show the trace-based fix is correct after all.

## Next Session Should

1. **Update FERAL-PROJECT-SPEC.md §1712** with the new consensus exit
   criterion and the final numbers from this session. Done.

2. **Investigate the ACOPP30 residual gap.** On ACOPP30_0000, feral
   and canonical MUMPS agree on inertia `(71, 137, 1)` but feral's
   residual is 3.15e-2 while MUMPS's is 5.0e-14 — 12 orders of
   magnitude worse. Same factorization (per inertia agreement), so
   the difference is almost certainly global equilibration that MUMPS
   applies and feral does not. This is the real Phase 2 target for
   the ACOPP30 family of failures, not delayed pivoting.

3. **Re-attempt the deferred trace-based 2×2 inertia fix.** Now that
   canonical MUMPS is a running oracle, the 16-matrix dense regression
   measured against rmumps can be re-evaluated against the consensus.
   If canonical MUMPS agrees with the trace-based answer, the fix
   should land.

4. **Fix the 1028 MUMPS factor failures** (`INFOG(1) = -9`, workspace
   too small). Bump `ICNTL(14)` in `external_benchmarks/mumps_oracle/
   mumps_bench.F` and re-run. Not blocking Phase 1b exit; affects only
   the coverage of the MUMPS oracle.

5. **Phase 2 planning.** The consensus framework identified 13,377
   NumericallyIntractable matrices (7.89% of the corpus) and 2,341
   Excluded matrices (1.38%). These are not feral bugs, but Phase 2
   delayed pivoting + global equilibration should move many of them
   out of those categories into Definitive.
