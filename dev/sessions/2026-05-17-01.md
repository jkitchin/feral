# Session 2026-05-17-01

## Goal

Localize and remediate the per-iter factor cost gap vs MA57 on the
ipopt-feral Mittelmann sweep. Going in, the suspect was MC64 cost
(98% of warm wall on `probe_rocket_slow.rs` against the pounce
corpus dumps).

## Accomplished

### Investigation pivot: MC64 is rocket-specific, cascade is general

1. **MC64 live trace.** Added `MC64_RECOMPUTE_COUNT` +
   `FERAL_MC64_TRACE=1` to `src/scaling/mc64.rs`. Live ipopt-feral
   run on rocket_12800: 30 MC64 recomputes (one per warm factor —
   confirms #38 `db20166` invalidation), MC64 = **55%** of total
   Ipopt wall (not 98% as the dumped corpus suggested). Per-call
   wall ranges 14–2482 ms.

2. **Per-supernode profile on robot_1600** (`probe_robot_profile.rs`).
   Found MC64 is only **2.6%** of robot_1600 warm wall (350 ms of
   13.7 s). The 32× MA57 gap there lives elsewhere.

3. **Factor trace** (`FERAL_FACTOR_TRACE=1` in `src/capi.rs`).
   Per-factor wall + `sum_delayed` + `max_delayed` exposed
   smoking-gun: robot_1600 late-IPM factors have
   `sum_delayed = 30k–60k` on n=24000 — classic delayed-pivot
   cascade (the same mechanism issue #38 fixed for pinene_3200).

4. **Auto-CB was dead code.** `Solver::with_auto_cascade_break(β)`
   (the warm cascade-break auto-arm from #38) was never wired into
   the capi, so ipopt-feral never benefited. Wired it as the
   default with `FERAL_AUTO_CB_BETA` env (default 0.05).

Spot-check on 10 Mittelmann problems (`benchmarks/mittelmann_ipopt`):

| problem        | CB=off    | auto-CB   | Δ        |
|----------------|-----------|-----------|----------|
| robot_1600     | 13.81 s   |  3.58 s   |  -74 %   |
| marine_1600    | 470.87 s  | 58.13 s   |  -88 %   |
| clnlbeam       | 361.26 s  | 47.73 s   |  -87 %   |
| corkscrw       | 53.89 s   | 15.43 s   |  -71 %   |
| camshape_6400  |  6.67 s   |  2.09 s   |  -69 %   |
| dtoc2          | timeout   | 78.0 s    | rescued  |
| bearing_400    |  6.21 s   |  4.86 s   |  -22 %   |
| rocket_12800   |  8.73 s   | 10.55 s   |  +21 %   |
| arki0003       |  3.10 s   |  3.17 s   |   ~0 %   |
| pinene_3200    | timeout   | timeout   | needs CB=on |

### Scaling routing fix (clnlbeam, #68)

Tightened `pick_scaling_strategy` in `src/scaling/mod.rs` to require
BOTH `diag_only/n >= 0.30` AND `max_col_nnz > 32` before routing to
MC64. clnlbeam was scoring 40% diag_only (HIGHER than VESUVIO's
33%) but has banded structure (max_col_nnz=5) — MC64 hurt it
4.4× iters and 28× wall. New dense-column gate separates banded
KKTs from arrow KKTs with order-of-magnitude margin (ACOPP30 max=29
must miss, MUONSINE min=512 must hit).

End-to-end: clnlbeam 2367 iters / 361s → 506 iters / 57s, matching
the `FERAL_SCALING=infnorm` probe from §14:30. 29 scaling tests
pass including new gate boundary tests + fixture-gated clnlbeam
routing assertion.

### Research note (deferred)

`dev/research/mc64-value-bounded-cache-2026-05-17.md` documents
the value-bounded MC64 cache design — the architecturally correct
fix aligning feral with MA57/SSIDS analysis-only MC64. Defers
implementation because the auto-CB rescue delivered the headline
win and MC64 on rocket_12800 is now ~6.5% of wall, not 98%.

### Tests + lint

- 279 lib tests + all integration tests pass
- `cargo fmt --check` clean
- `cargo clippy --all-targets -- -D warnings` clean
- Pre-commit hooks ran on all three commits without skip

## Decisions Made

None new; this session implemented existing plans:
- §14:30 plan to add max_col_nnz gate (recorded earlier today)
- #38 auto-cascade-break mechanism, already in solver, now wired

## Abandoned Approaches

None abandoned. Value-bounded MC64 cache is *deferred*, not
abandoned — research note documents the design and exit criteria.

## Next Session Should

1. **Validate auto-CB on full 47-problem Mittelmann sweep** (task #12).
   Spot-check was 10 problems with 6 dramatic wins, 1 regression
   (rocket_12800 +21%). Need aggregate impact + any other regressions.

2. **Investigate rocket_12800 +21% regression.** Auto-arm likely
   fires mid-IPM and the cascade-break perturbation adds refinement
   cost on a problem where delayed pivots were not causing failures.
   Either tune β per shape, or gate cascade-break harder (require
   *failed* factor in addition to delayed-pivot count).

3. **Run the scaling routing fix on the full Mittelmann sweep**
   alongside auto-CB to confirm clnlbeam rescue + no regressions
   on VESUVIO/CRESC/MUONSINE/ACOPP30.

4. **(Optional) Implement value-bounded MC64 cache** per the
   research note. Modest direct win (~6.5% on rocket_12800) but
   architecturally correct alignment with MA57.

5. **pinene_3200 still times out** — first factor needs CB=on
   from the start; auto-arm fires too late. Either lift β to 0
   for first-factor-only, or recognise restoration-iter pattern.

## Commits

- `a28cfec` fix(scaling): require dense arrow head, not just diag-only mass, for MC64 routing (#68)
- `f1da854` feat(capi): auto-arm cascade-break by default to rescue delayed-pivot cascades
- `ad48ab2` chore(dev-tools): add Makefile wrapper + pounce KKT replay/time probes
