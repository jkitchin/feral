# Session 2026-04-18-06

## Goal
Originally K3 only; extended in-session per user to K4-K6, plus
Mongoose research via subagent. End state: KaHIP K2-K6 complete
(K1 data reduction still deferred, by plan); Mongoose research
note landed.

## Accomplished
- New module `crates/feral-kahip/src/graph.rs`: `UndirectedGraph`
  CSR (n, xadj, adjncy, eweight), `cut_weight`, `neighbors`,
  `eweights`, `from_csc_unit_weights`. Shared infrastructure for
  K3/K4/K5/K6.
- New module `crates/feral-kahip/src/flow_refine.rs`:
  `flow_refine_bisection(graph, where_, bnd_distance,
  max_imbalance)` implementing one K3 iteration.
  - Boundary detection + BFS band extraction with configurable
    radius.
  - Anti-parallel directed-edge model for undirected graph edges
    (plan audit item 10).
  - `pin_depth = min(max_dist_in_part, bnd_distance)` — pins all
    band vertices at the adjusted depth in each part (audit
    item 2; fallback handles small parts fully contained in the
    BFS ball).
  - INF_CAP computed as `(sum_band_edge_weight / 2) + 1` to keep
    super-edges non-bottlenecks without i64 overflow when many
    vertices pin to super-source.
  - Two-cut MBMC v1: solve max-flow once normally, once with
    s/t swapped on reversed edges; pick the candidate with lower
    cut weight that satisfies the balance tolerance (audit
    item 3; full MBMC deferred).
  - Strict improvement acceptance: `new_cut < old_cut` AND
    balance OK; otherwise `where_` untouched.
- Research note `dev/research/ordering-kahip-k3.md`: formal
  algorithm, band/fixed-node definitions, two-cut MBMC v1 scope,
  API contract, 8-item test-oracle list.
- Journal `dev/journal/2026-04-18-06.org`: orientation, 3-bug
  sequence (oracle, INF_CAP overflow, fallback pinning), oracle
  tuning, done-status.

- **K4 flow-based node separator** (`src/node_separator.rs`, new):
  boundary-bipartite vertex-cover reduction per Sanders-Schulz
  2011 §4.4. Directed s → B_0 → B_1 → t network, source/sink arcs
  carry vertex weights, cross arcs carry `INF = Σ boundary_weight
  + 1`. Separator extracted from saturated source/sink arcs via
  König's theorem. 9/9 new tests pass. Research note
  `dev/research/ordering-kahip-k4.md`.
- **Mongoose research note** via background subagent
  (`dev/research/ordering-mongoose.md`, 330 lines): GPL-3.0-only
  license (blocks source reuse; clean-room-from-paper fine per
  FERAL protocol); published Mongoose is edge-partitioning-only
  — ND+AMD pipeline is the paper's future-work section, NOT
  shipped code. Recommendation (b): cherry-pick HEMSR coarsening
  + 2-phase hybrid refinement into feral-metis/feral-kahip
  behind feature flags rather than build a dedicated
  feral-mongoose crate. tasks.org updated.
- **K5 multilevel bisection** (`src/cycle.rs`, new): reuses
  `feral_metis::internals::{coarsen, fm_refine, graph,
  initial_partition, rng}` (same pattern as feral-scotch) for
  multilevel plumbing; plugs K3 `flow_refine_bisection` at each
  uncoarsening level. Mode tuning (Fast/Eco/Strong) controls
  `n_sep_trials`, `coarsen_floor`, `amd_switch`, `fm_pass_cap`,
  `bnd_distance`, and how many K3 iterations run per level.
  `graph_to_undirected` bridge between i32-indexed `Graph` and
  usize-indexed `UndirectedGraph`.
- **K6 recursive ND driver** (`src/node_nd.rs`, new): connected-
  components walk, per-component recursion, AMD leaf fallback
  (`feral_amd::amd_order`) for subgraphs ≤ `amd_switch`, K5
  bisection + K4 `flow_node_separator` + separator-last
  numbering otherwise. Patterned on
  `feral-scotch/src/node_nd.rs`.
- `kahip_order_full` wired end-to-end (was returning Internal
  error). Module-level docs updated from "pre-implementation
  scaffold" to "K2-K6 complete". Research note
  `dev/research/ordering-kahip-k5-k6.md` with combined K5/K6
  architecture and out-of-scope items.
- `crates/feral-kahip/Cargo.toml`: added `feral-amd` and
  `feral-metis` path deps.

Evidence:
- `cargo test -p feral-kahip` → 61/61 pass (was 40/40 after K3;
  +9 from K4, +12 from K5/K6).
- `cargo clippy -p feral-kahip --all-targets -- -D warnings`
  clean.
- `cargo test --workspace` green.
- Commits: `7651ba7` (K3), `36e400d` (K4), plus this session's
  K5+K6 final commit.

## Benchmark Results
K3 is not wired into the solver; numbers reflect run-to-run
jitter only.
```
--- Dense Phase 2.8.1 exit partition (factor ratio vs MUMPS) ---
bucket                    count      p90     target  verdict
small-frontal (<200)     147982     1.45     <= 2.0     PASS
medium (<500)            152145     1.87     <= 3.0     PASS

--- Sparse Phase 2.8.1 exit partition (factor ratio vs MUMPS) ---
bucket                    count      p90     target  verdict
small-frontal (<200)     153455     1.77     <= 2.0     PASS
medium (<500)            153560     1.78     <= 3.0     PASS
```

## Decisions Made
- **Fixed-node pinning with fallback.** Plan/research say "all
  band vertices at distance EXACTLY `bnd_distance`". For small
  parts fully inside the BFS ball, no such vertex exists.
  Adjusted rule: `pin_depth_p = min(max_dist_in_part, bnd_dist)`
  per side, then pin all part-p vertices at dist == pin_depth_p.
  Preserves the "all deepest" semantics while handling the
  degenerate case.
- **INF_CAP = (sum_band_edge_weight / 2) + 1.** Using
  `i64::MAX / 4` overflowed `excess[source]` when many vertices
  pin with that capacity. The sum-based bound is tight: in-band
  cut is bounded by total in-band edge weight, so INF_CAP just
  exceeds it. Halved because the edges array counts each
  undirected edge as two anti-parallel directed entries.
- **Two-cut MBMC v1 via second solve.** Rather than extract
  sink-closest cut via residual-graph BFS from sink, run a
  second `push_relabel` call with s/t swapped and edges
  reversed. Clumsier but reuses the primitive verbatim; full
  MBMC is K5/K6 follow-up.

## Abandoned Approaches
None. The three bugs (test-oracle count wrong, INF_CAP
overflow, fallback pinning) were implementation bugs found in
testing, not abandoned designs.

## Next Session Should
1. **KaHIP K6 solver-side dispatch + benchmark.** Add
   `OrderingMethod::KahipND` in `src/symbolic/mod.rs`, wire it
   to `feral_kahip::kahip_order`, extend
   `src/bin/bench_orderings.rs` to include it in the large-KKT
   bakeoff. Deferred intentionally from this session per
   `dev/research/ordering-kahip-k5-k6.md`.
2. **KaHIP K1 data reduction integration.** K1 is already
   implemented (`src/data_reduction.rs`) but not wired into the
   driver. Wrap `kahip_order_full` with a reduce → recurse →
   expand pipeline once benchmarking shows whether it's worth
   the added code path.
3. **Adaptive ordering dispatcher** (tasks.org follow-up):
   pick AMD vs METIS vs SCOTCH vs KaHIP based on cheap
   pre-analysis. Now unblocked since all four crates expose the
   contract.
4. **Mongoose HEMSR + 2-phase hybrid refinement follow-up**
   (tasks.org): per Mongoose research note recommendation (b),
   land these two pieces into feral-metis/feral-kahip behind
   feature flags; check SuiteSparse `dev` branch first for any
   post-2020 vertex-separator extension.
