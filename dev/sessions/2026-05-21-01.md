# Session 2026-05-21-01

## Goal

Implement Track B2 of the per-factor cost cluster plan — eliminate the
per-call MC64 Hungarian on `rocket_12800` via a value-bounded MC64
scaling cache. Mid-session the goal pivoted (with human approval) to
**Track A**: investigate the `pinene_3200` iter 6-9 factor-time
explosion, which is 98 % of that problem's wall time.

## Accomplished

### B2 — value-bounded MC64 scaling cache (landed, then pivoted off)

- Wrote `dev/plans/mc64-value-bounded-cache.md`; implemented
  `src/scaling/value_bound.rs` (value-bound pure functions, 10 tests)
  and the Solver-scope `Mc64ScalingCache` in `src/numeric/solver.rs`
  (`with_mc64_cache` builder, cache-hit injection via
  `ScalingStrategy::External`, 5 integration tests).
- The cache is **correct and fully tested** but has **no measured
  corpus payoff**: the gate metric (diagonal dominance of `D·A·D`) is
  confounded by the IPM δ-regularization trajectory, and the MC64
  Hungarian it eliminates is < 2 % of factor cost. Ships as latent
  infrastructure. See `decisions.md` / `tried-and-rejected.md`.

### Fixed — `External` scaling 10× solve bug (pre-existing, latent)

- B2's integration test `mc64_cache_hit_bit_matches_cache_off` caught
  it: `ScalingStrategy::External` paired a real scaling vector with
  `ScalingInfo::NotApplied`. The factor applies `D·A·D`
  unconditionally; the solve keys un-scaling off
  `scaling_info != NotApplied` — so an `External` solve returned
  `D⁻¹A⁻¹D⁻¹b` (exactly 10× on `tridiag(6,10,1)`).
- Fix: the `External` arm now returns `ScalingInfo::Applied`.
  `NotApplied` is now exclusively `Identity` (genuine all-ones).
  Verified bit-identical across repeated calls; 302 lib tests + full
  suite green. Committed `c990def`.

### Track A — pinene_3200 iter 6-9 blowup characterized (A1)

- Established this is **issue #8**, and that the warm-state hypothesis
  was already disproved (2026-05-17): the cascade is structural to the
  iter-N matrix's *numeric content*, standalone ≈ warm.
- Ran `diag_pinene_pivot_cliff` (per-supernode 2×2 / delayed-pivot
  stats) on iterates 0008 and 0009 (n=127995, nnz=733k). **Direct
  evidence — the root front is a fully dense block, ~14 % of n:**

  | metric            |   0008 |   0009 |
  |-------------------|-------:|-------:|
  | root front nelim  |  15446 |  17538 |
  | root n_2x2        |   7064 |   8221 |
  | root n_delayed_in |  15341 |  17433 |
  | nnz_L             | 128.5M | 165.7M |
  | total_delayed     | 116541 | 133648 |

  The last supernode (node 10486) is a dense ~17.5k×17.5k root front
  (`nelim = nrow`, no trailing block); ~91 % of its columns are in
  2×2 pivots. Cost ≈ a dense 17.5k LDLᵀ ≈ 1.8e12 flops — that is the
  208 s. The three supernodes below it (10483/10484/10485, each
  ~3.8k–18k columns wide) eliminate only 4 / 11 / 493 columns — pure
  delay conduits feeding everything into the root.
- This is the **zero-(2,2)-block KKT cascade** (issue #46 family): as
  the IPM drives δ_c→0, every (2,2) row must be 2×2-paired and the
  pairing cannot be made locally, so columns delay all the way up the
  tree. The cascade worsens monotonically across IPM iters (root
  15446→17538, nnz_L +29 %). The probe's stale header ("0008 fast:
  44ms") is wrong — standalone 0008 now factors in ~3 min (≈ its warm
  135.7 s), confirming the cascade is structural to the matrix.

## Benchmark Results

PASS on all four exit-partition buckets — no regression from B2.

```
--- Dense Phase 2.8.1 exit partition (factor ratio vs MUMPS) ---
bucket                    count      p90     target  verdict
small-frontal (<200)     147982     1.32     <= 2.0     PASS
medium (<500)            152145     1.74     <= 3.0     PASS

--- Sparse Phase 2.8.1 exit partition (factor ratio vs MUMPS) ---
bucket                    count      p90     target  verdict
small-frontal (<200)     153455     1.57     <= 2.0     PASS
medium (<500)            153560     1.57     <= 3.0     PASS
```

## Decisions Made

- **External reports `Applied`** — fixes the latent 10× solve bug
  (appended to `decisions.md`).
- **Track B2 ships as latent infrastructure, payoff unproven** —
  pivot off B2 (appended to `decisions.md`).

## Abandoned Approaches

- **B2 value-bounded MC64 scaling cache** as a payoff push — the gate
  metric is confounded by the IPM δ trajectory; it rejects every warm
  iterate on `pinene` and cannot demonstrate a hit on `rocket`
  (pattern changes between its 2 dumped iters). Recorded in
  `tried-and-rejected.md`.

## Next Session Should

1. **Track A2 — make cascade-break proactive for pinene.** A1 has
   localized the cascade: a dense ~15.4k×15.4k root front built from
   delayed pivots, zero-(2,2)-block 2×2-dominated. The open question
   is *why default auto-CB (`AUTO_CB=0.05`) does not already rescue
   pinene* — the root delays 15341 columns, far above `0.05·n=6400`,
   so the reactive arm should fire on iter N+1. Either the pattern
   fingerprint changes between pinene iters (resetting auto-CB state,
   as it does on rocket), or bounded-Δ `PerturbToEps` does not break
   *this* cascade. Measure `pinene` under explicit `CB=on` and trace
   the auto-CB arm state before choosing the A2 lever.
2. Cross-check against the issue #46 fix (session 2026-05-20-03) —
   that was the *same* zero-(2,2)-block delayed-pivot cascade on a
   smaller KKT; confirm whether its fix path applies to pinene.
3. Track A3 validation; then Track C (#48 marine WrongInertia).
