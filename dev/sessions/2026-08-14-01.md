# Session 2026-08-14-01

## Goal

Fix issue #168 — the claim recorded at `895ef65` that `peel + dense_bump_max_dim
4096` passes the downstream `bchoco06_illcond_scaled_path_recovers_bound_649`,
reported from the discopt side as not reproducing.

## Accomplished

**The issue is right. The claim does not reproduce, and I reproduced the
non-reproduction independently before touching the record.**

Harness: discopt worktree at `bce881ff` unmodified, feral worktrees at `e00aa70`
and `895ef65`, `[patch.crates-io] feral = { path = … }`, one arm per run,
`cargo test -p discopt-core --lib bchoco06`. Both feral revs are version
`0.15.1`, so the patch applies with no version override.

| feral rev | ordering | cap | result | `PROBE_DENSE_BUMP` firings |
|---|---|---|---|---|
| `e00aa70` | whole-basis AMD | 0 | **ok** | 0 |
| `e00aa70` | peel | 0 | **FAILED** — `Numerical` | 0 |
| `e00aa70` | peel | 4096 | **FAILED** — `Numerical` | **26** |
| `895ef65` | peel | 4096 | **FAILED** — `Numerical` | **26** |

Same assertion in every failure, and it is the test's ground truth rather than
its subject:

```
assertion `left == right` failed: unscaled cold solve of the bchoco06 root LP must be Optimal
  left: Numerical
 right: Optimal
```

Firing counts come from `eprintln!("PROBE_DENSE_BUMP bump_dim={}", bump_dim)`
inserted immediately after `want_dense_bump` in `sparse_factor.rs`: 26 in both
cap-4096 arms, 0 in both cap-0 arms. The failing configuration is therefore one
where the dense route provably ran, and a silently-unapplied patch is ruled out —
it would have made the cap-4096 arm identical to peel-no-cap, which it is not.
My numbers match #168's exactly, including the 26. Row 4 shows the behaviour is
identical at the commit the claim was authored on, so nothing that landed
afterwards caused it.

Record corrected in four places:

- `dev/research/lu-ordering-and-kernel-2026-08-13.md` — § "Does the fix strand
  the 1.71x?" rewritten; the answer is now **yes**.
- `src/lu/mod.rs` — `LuParams::dense_bump_max_dim` doc now states the cap does
  not recover the bound and why there is no cap-without-peel fallback.
- `CHANGELOG.md` — recorded under 0.16.0.
- `dev/tried-and-rejected.md` — logged as rejected on measurement.
- `dev/research/hyper-sparse-solves-2026-08-13.md` — cross-reference on the
  1.71x table, which is where a reader would go to justify taking the cap.

**The wider retraction.** The paragraph that followed the table argued that
because the failing configuration was the *middle* of three, "no ordering-quality
story survives that", and the result was coin-flip sensitivity to any
perturbation of the bits the ratio test reads. That rested entirely on the false
row. With the corrected table there is no middle: both peel arms fail, only
whole-basis AMD passes, and the failure tracks the ordering. This is **not**
evidence that whole-basis AMD is the better trajectory in general — #166 has
QPLIB_3225 apparently going the other way — but "reordering the bump again lands
back on a certifying path" is withdrawn.

**Release status unchanged and nothing downstream exposed.** `dense_bump_max_dim`
defaults to `0` and `analyze` is whole-basis AMD, so both halves of the lever are
off unless a caller asks. #163's coupling argument stands for anyone who does
ask: `want_dense_bump` is gated on `symbolic.triangularized`, so the 1.71x and
the lost dual bound are one lever. `895ef65` was the only evidence against that.

## Benchmark Results

Not re-run. This session changed one doc comment in `src/lu/mod.rs` and
otherwise only `dev/` and `CHANGELOG.md`; no executable code path was touched, so
the LDLᵀ bench measures the same binary behaviour as session 2026-08-13-04.
Session 04's numbers stand, including its dense-KKT p90 regression — small-frontal
1.57 → **1.61** (target ≤ 2.0, PASS) and medium 1.96 → **2.09** (target ≤ 3.0,
PASS) — which is reported here rather than omitted because it is the most recent
unfavourable comparison on record.

## Decisions Made

None architectural. One practice rule, recorded in `dev/tried-and-rejected.md`:
a pass/fail result on a **silent-fallback** code path is not evidence unless the
arm also demonstrates the path fired. The retracted measurement was taken on
exactly such a path with no such proof — and #162 had already argued this
standard for its own tests via `used_dense_bump()`, so the standard existed and
the measurement did not meet it.

## Abandoned Approaches

Appended to `dev/tried-and-rejected.md`: "the dense-bump route escapes the #163
bound loss". Rejected on re-measurement, with the symptoms and the failing
configuration stated rather than reframed. Why the original run reported `PASSED`
is undetermined — vacuity is ruled out; stale build artifacts and an arm mix-up
cannot be separated after the fact.

## Next Session Should

1. **#166 — QPLIB_3225.** Now a live regression on `main`, not on a branch: #162
   merged as `51f87ec`, so the issue's title is stale. The decisive test is the
   one #163 used — hold discopt fixed, flip only `analyze`'s body. If it tracks
   the ordering, then the revert bought bchoco06 and cost QPLIB_3225, and that
   pair is the real characterization of the trajectory sensitivity.
2. **Close #161 and #163.** Both are addressed by merged work but neither
   auto-closed — the PR body referenced them without a closing keyword. #163's
   fix is `83dd752` (verified an ancestor of `main`); #161's part B is `06fd5ba` /
   `71468d6`. #161 needs a judgment call first: its headline is "1.6–3.9x slower
   than SuperLU at equal fill", and #167 shows the fill premise itself was wrong,
   so re-measure its reproducer against merged `main` before closing.
3. **#167 — threshold-Markowitz for the bump.** Scoping only; it is a new
   factorization path, not a new ordering.
4. **Release 0.16.0.** The blocker this session was checking is now settled
   rather than removed: the coupling is real, and the CHANGELOG says so.
