# Session 2026-05-16-11 — Issue #11 SmallLeafBatch default-flip re-eval

## Goal
Re-run the Phase 2.11 `SmallLeafBatch::Off → On` default-flip
experiment under the protocol documented in issue #11, now that
prerequisites #9 (SIMD trailing-update kernel) and #10 (APP
pivoting) are closed. Decision criterion: median speedup ≥ +10%
on tiny + small bucket AND worst per-matrix slowdown ≤ +5%.

## Accomplished
- Wrote a one-off `FERAL_SMALL_LEAF=on|off` env-var override
  inside `src/bin/bench_one_matrix.rs` so the gate could be A/Bd
  without recompiling. **Reverted** at session end per the
  failure-path workflow.
- Wrote `external_benchmarks/comparison/issue_11_ab.py`, a
  5-runs-per-variant interleaved harness over the full
  comparison sample (66 matrices with on-disk RHS). **Removed**
  at session end per the failure-path workflow; reproducible
  from this checkpoint and `dev/journal/2026-05-16-11.org`.
- Ran 5 × Off, 5 × On, interleaved (~55 min wall total).
- Computed per-matrix median, per-bucket median speedup, and
  per-bucket worst-case slowdown. Result: REJECT.
- Appended a detailed rejection entry to
  `dev/tried-and-rejected.md` with the full measurement table,
  worst-offenders, best-wins, and reasoning.
- `cargo test --release --lib` 259 passed; `--tests` 5 passed;
  `cargo clippy --bin bench_one_matrix -- -D warnings` clean;
  `cargo fmt --check` clean.

## Benchmark Results

Skipping `cargo run --bin bench --release` for this session —
the A/B harness is the relevant measurement and is reported in
the tried-and-rejected entry. Summary inline:

```
bucket    n_mats  med_speedup_%  worst_slowdown_%  best_speedup_%
------------------------------------------------------------------------------
tiny          20          0.00            50.00           40.00
small         19          1.90             9.09            8.97
medium         8          0.30             2.01            6.37
large         10          7.70            12.53           21.46
xlarge         7         -4.35             6.55            8.57

Decision (tiny + small combined, n=39):
  median speedup      = +1.44%  (criterion ≥ +10%)  FAIL
  worst per-matrix    = +50.00% (criterion ≤ +5%)   FAIL
OVERALL DECISION: REJECT
```

Median across 5 runs per matrix per variant; `Off` is the
current `SmallLeafBatch::default()`. The +50% outlier is a
1 µs timer-resolution artifact (n=5 matrices factor in 2–3 µs);
the next two worst (n=12 +25%, n=31 +10%) are similarly close
to timer resolution. Real per-matrix wins on tiny+small top out
at +9.8% (HAIFAM_0370, n=249).

## Decisions Made

None — explicitly NOT flipping the default. The compiled-in
`SmallLeafBatch::Off` at `src/numeric/factorize.rs:212-213` is
unchanged. The doc-comment on the enum already records the
Phase 2.11 rejection; the new tried-and-rejected entry extends
the rejection to the post-SIMD + post-APP kernel regime.

## Abandoned Approaches

- **Issue #11 flip** — appended to `dev/tried-and-rejected.md`
  under heading "2026-05-16 — Issue #11 `SmallLeafBatch::On`
  default flip, post-SIMD+APP re-eval". Full measurement table
  and reasoning recorded there.

## Next Session Should

1. **Close issue #11** with a comment linking to the new
   tried-and-rejected entry. The investigation is complete and
   the answer is "stays opt-in, structural tail gap remains".
2. The large-bucket +7.7% median speedup (with a +12.5% worst
   slowdown) seen in passing is interesting but out of scope
   for #11. If anyone wants to chase it, open a separate issue
   — the criterion in that bucket would be different from the
   small-front amortization target #11 was designed around.
3. The structural tail gap on bushy IPM KKTs is still owned by
   Phase 2.12's column-renumbering amalgamation (already
   merged); no follow-up needed here.

## Protocol Deviations

- Original Phase 2.11 referenced `diag_small_leaf_gate.rs` as
  the harness. This session used the cross-solver comparison
  sample (66 matrices) instead, matching the issue's "full
  corpus via `external_benchmarks/comparison`" wording. Sample
  is the same one used by the `report.py` head-to-head table.
- `bench_one_matrix` was modified in-session to read
  `FERAL_SMALL_LEAF` so a single binary could A/B both
  variants. The library default and all other binaries were
  untouched. Modification reverted at session end.
- All 66 matrices completed under the 600 s per-variant timeout,
  so no reduced-corpus deviation was needed.
- The agent worktree (`agent-a0fd99cd1319a3cd0`) was removed
  by an external process mid-session; recovery branch was
  created as `issue-11-small-leaf-reeval` off the new main HEAD
  (`ea98f98`). The bench data itself was unaffected (it ran in
  the main worktree's `target/release/`).
