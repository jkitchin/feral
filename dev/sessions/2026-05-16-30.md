# Session 2026-05-16-30 — Issue #37 reframe via #38: stale MC64 cache root-cause

## Goal

User assertion: #37 (pinene_3200 CB=off regression) "relies on a workaround
that should not be needed". Investigate whether the workaround
(`Solver::with_cascade_break(0.5)` builder, or pounce-side
`feral_cascade_break=yes` ipopt option) can be eliminated by a feral-side
default fix.

Mid-session redirect: user pointed at issue #38 (sister case to #37 on
`rocket_12800` where BOTH CB modes fail), which reframes the question from
"flip the right default" to "fix the underlying mechanism". Pivoted to
chasing #38's Failure A (warm-Solver-state slowdown).

## Accomplished

- **AutoRace ordering hypothesis killed by diag data.** Ran
  `diag_pinene_amd` on `pinene_3200_{0008,0009}` + `robot_1600_0003` to
  test whether switching `Solver::new()`'s default from
  `OrderingMethod::Auto` to `OrderingMethod::AutoRace` would close #37 by
  preferring AMD on pinene. Result: under CB=off, AMD on pinene_3200 is
  **~10× WORSE** than MetisND (917s and 1055s vs MetisND's 88s, with
  ~13.5M delayed pivots), not 4.5× better as the c92cafe commit message
  claimed. c92cafe's benchmark predates 585d739 (cascade-break became
  opt-in), so it was measuring AMD-with-CB-armed. Robot_1600_0003 was
  clean (both AMD and MetisND produce neg=9601 matching the MUMPS oracle,
  AMD 1.4× faster), so the regression guard would not have fired.
  Conclusion: ordering choice cannot fix #37; the mechanism is the issue.

- **Reproduced #38 Failure A** (warm-Solver-state slowdown) on the 18
  `/tmp/rkt_*.bin` rocket dumps. Default `Solver::new()` (CB=off,
  parallel=on, scaling=Auto):

  | call | factor   | neg   | comment            |
  |-----:|---------:|------:|--------------------|
  | #000 |  0.320s  | 38400 | cold (incl. symbolic) |
  | #009 |  0.023s  | 38400 | warm, stable        |
  | #010 |  0.022s  | 38395 | inertia drift starts |
  | #014 |  0.024s  | 38145 | drift continuing    |
  | #015 |  0.056s  | 37513 | cost begins to climb |
  | #016 |  2.093s  | 35900 | cost explodes       |
  | #017 | 43.216s  | 31720 | runaway             |

  Matches issue body's qualitative description.

- **NEW FINDING (bigger than #38's framing): warm Solver under default
  `ScalingStrategy::Auto` produces SILENTLY WRONG INERTIA on the same
  matrix that a fresh Solver factors correctly.** Side-by-side fresh
  vs warm on calls #014..#017:

  | call | warm factor | fresh factor | warm neg | fresh neg |
  |-----:|------------:|-------------:|---------:|----------:|
  | #014 |    0.024s   |   1.206s     | 38145    | **38400** |
  | #015 |    0.056s   |   1.222s     | 37513    | **38400** |
  | #016 |    2.093s   |   1.194s     | 35900    | **38400** |
  | #017 |   43.216s   |   1.640s     | 31720    | **38400** |

  Fresh produces stable, correct 38400 throughout (matches MA57 oracle
  per #38's CB=on table). Warm produces drifting wrong inertia starting
  at call #010 — well before the cost explosion. This is a silent
  correctness bug in feral, not a perf regression.

- **Localized to sequential path** (not parallel-pool warm state). PAR=0
  re-runs reproduce the inertia drift and cost runaway identically.

- **Root cause identified: stale MC64 cache in
  `SymbolicFactorization::cached_mc64`.**
  - `ScalingStrategy::default() == Auto`
  - `Auto` on arrow-KKT routes to `Mc64Symmetric` (rocket KKT has 38400
    degree-≤2 slack columns out of n=89601 → triggers
    `pick_ordering_preprocess` → `LdltCompress`)
  - `LdltCompress` at symbolic time populates `cached_mc64` from iter 0
    values via `mc64::compute_matching(matrix)`
  - `compute_scaling_with_cache` consumes `Some(cache)` via
    `scaling_from_cache(cache)` — applies iter-0 scaling to iter-N matrix
  - MC64 matching, dual vars (u, v), column maxes (cmax) are
    value-dependent; cache becomes increasingly mismatched as iter values
    drift
  - Result: silently wrong scaling → wrong factor → wrong inertia →
    eventually cascade of delayed pivots → cost runaway

- **Confirmed by forcing `ScalingStrategy::InfNorm`** (no MC64 cache
  path). Same warm Solver, same 18 rocket KKTs, but scaling=InfNorm
  recomputes each factor() call:

  | call | factor   | neg   |
  |-----:|---------:|------:|
  | #000 |  0.409s  | 38400 |
  | #014 |  0.028s  | 38400 |
  | #015 |  0.042s  | 38400 |
  | #016 |  0.028s  | 38400 |
  | #017 |  0.027s  | 38400 |

  Stable inertia, no cost runaway, all calls ~27ms warm. Bypassing the
  MC64 cache fixes both symptoms.

  (Cross-check: `SCALING=identity` does NOT fix the bug. Identity is a
  confounded probe — it uses no cache, but also no scaling, so the
  raw ill-conditioned matrix cascades for matrix reasons. InfNorm is the
  clean control: no cache + good scaling → both symptoms gone.)

## Benchmark Results

No `cargo run --bin bench --release` this session — pure investigation,
no code changes to the solver. The numbers above (rocket replay sweeps)
are the relevant measurements. No regression run because no change
landed.

## Decisions Made

None new on main. Decision to commit a fix is deferred to the next
session, pending choice between the four options in the journal §17:35.

## Abandoned Approaches

- `OrderingMethod::AutoRace` as the default for `Solver::new()` —
  proposed as a fix for #37 (AMD allegedly 4.5× faster than MetisND
  on pinene_3200_0009 per c92cafe commit msg). DEAD on diag data:
  under CB=off (the current default), AMD on pinene_3200 is
  ~10× WORSE than MetisND (917s vs 88s with ~13.5M delayed
  pivots). The c92cafe benchmark predates 585d739 making CB opt-in.
  Recorded in `dev/tried-and-rejected.md`.

## Next Session Should

- **Write research note `dev/research/mc64-cache-staleness-2026-05-16.md`**
  covering the failure mechanism and the four fix options analyzed
  against the IPM use case.

- **Land the fix.** Likely option 2: recompute MC64 cache (or skip the
  cache path entirely) on every `factor()` call when scaling resolves to
  `Mc64Symmetric`. Lose the ~70% symbolic-overhead amortization the
  cache provides, but that amortization assumed re-factor on the SAME
  matrix, which never happens in IPM. Need a regression test that
  locks warm-vs-fresh inertia agreement on a small arrow-KKT
  reproducer (rocket dumps are 12 MB each — too big to ship; need a
  smaller fixture).

- **Re-test #37 (pinene_3200) under the fix.** Pinene is also
  arrow-KKT (KKT order 127995 with optimal-control slack structure)
  and likely hits the same cache-staleness path. If the MC64-cache
  fix rescues pinene_3200 under CB=off defaults, #37 closes
  without the `with_cascade_break` workaround — which is exactly
  what the user originally asked for.

- **Post a finding-comment on #38** reframing the issue: not "warm
  slowdown" but "stale MC64 cache produces wrong inertia (silent
  corruption) and eventually explodes cost." Failure B (iter-4
  numerical disagreement vs MA57 under CB=on) is a separate
  investigation.

- Outstanding from prior sessions (not touched here):
  - +7.7% large-bucket median speedup signal from the #11 re-eval
    (next-session item flagged in `2026-05-16-29.md`)
  - #18 issue close pending user-side ipopt-feral rebuild + NARX_CFy
    re-run
  - Stale agent worktree cleanup under `.claude/worktrees/`

## Reproducer notes

The findings rest on three uncommitted helpers in `src/bin/`:
- `feral_replay.rs` — extended this session with `FRESH=1`,
  `START`/`STOP`, `SCALING={identity,infnorm,mc64}` env-var knobs
- `feral_time.rs` — single-factor timer (unchanged)
- `Makefile` (uncommitted) — local convenience

Per the failure-path workflow used in #11, these stay out of tree;
the reproducer recipe is fully captured in
`dev/journal/2026-05-16-30.org`. KKT dumps `/tmp/rkt_{000..017}.bin`
were produced by `POUNCE_DBG_KKT_DUMP=/tmp/rkt
POUNCE_DBG_KKT_DUMP_ALL=1 pounce nl/rocket_12800.nl` (see #38).
