# Session 2026-05-16-06

## Goal

Close out the final pre-floor lever for issue #10 (forced supernode
amalgamation), merge agent-27's M4 synthetic generators, and ship
the cumulative 5-lever verdict for the 1D-banded Mittelmann panel.

## Accomplished

### Issue #10 closed — 5/5 architectural levers exhausted

| # | Lever                            | Verdict on 1D-banded panel  |
|---|----------------------------------|-----------------------------|
| 1 | SmallLeafBatch driver removal    | within noise                |
| 2 | MAXFROMM AMAX-scan cache         | within noise                |
| 3 | Manual axpy SIMD tightening      | pulp ties scalar within 1ns |
| 4 | Ordering swap (Metis/Scotch ND)  | 1.3–2.3× slower             |
| 5 | Forced amalgamation (nemin)      | shape widens 2×; time flat  |

This session covered lever #5. Built
`src/bin/diag_nemin_amalgamation_panel.rs` sweeping
`SupernodeParams::nemin ∈ {16, 32, 64, 128}` on the 4-family ×
20-matrix Mittelmann panel. Pilot run with `nemin ∈ {256, MAX}`
hung 30+ min on `clnlbeam_0000` (collapsed front of order >n/2);
capped at 128.

Per-family geomean factor_us ratios vs nemin=16 baseline:

| family        | n=32  | n=64  | n=128 |
|---------------|-------|-------|-------|
| clnlbeam      | 1.032 | 1.356 | 1.989 |
| henon120      | 0.970 | 0.960 | 1.029 |
| lane_emden120 | 0.953 | 0.903 | 0.909 |
| dirichlet120  | 0.951 | 0.943 | 0.958 |

Acceptance gate `factor_us/nemin16 < 0.9` met only on
`lane_emden120@nemin=64` (0.903), and only barely. ncol_mean
doubled at nemin=64 across three of four families (the shape
lever did engage), but factor_nnz inflated 1.23-1.33× and factor
time stayed flat or regressed (clnlbeam −36%).

Closed GH #10 with cumulative 5-lever summary comment pointing at
the three research notes. Defaults unchanged: `nemin=16`,
`OrderingMethod::Amd`. The opt-in knobs `Solver::with_ordering`
and `SupernodeParams::nemin` stay shipped for non-1D-banded
workloads where elimination trees have genuine fusion
opportunities.

### Agent-27 merge (M4 synthetic generators)

Merged `worktree-agent-a2da3d0db2f4a5beb` into main (commit
`05aef71`). Conflicts on `CHANGELOG.md` and
`external_benchmarks/stress/manifest.tsv` resolved by concatenating
the M4 section after the #29 section. Test suite green (259
passed). Pushed and closed GH #27 with the three substantive
corrections vs the issue text documented in the close comment
(saddle inertia uses Gould 1985 formula, Q1-P0 has 2 spurious
pressure modes, mc64_resistant required Q D Q^T form).

## Benchmark Results

```
--- Dense Phase 2.8.1 exit partition (factor ratio vs MUMPS) ---
bucket                    count      p90     target  verdict
small-frontal (<200)     147982     1.34     <= 2.0     PASS
medium (<500)            152145     1.70     <= 3.0     PASS

--- Sparse Phase 2.8.1 exit partition (factor ratio vs MUMPS) ---
bucket                    count      p90     target  verdict
small-frontal (<200)     153455     1.68     <= 2.0     PASS
medium (<500)            153560     1.68     <= 3.0     PASS

Top 10 worst factor-ratio vs MUMPS:
name                             n    feral(μs)    mumps(μs)      ratio
MUONSINE_0000                 1537        10600          376      28.19
KIRBY2_0007                    458          894          119       7.51
KIRBY2_0006                    458          839          127       6.61
KIRBY2_0008                    458          733          122       6.01
CRESC132_0000                 5314        64748        12266       5.28
KIRBY2_0009                    458          653          128       5.10
KIRBY2_0010                    458          667          133       5.02
KIRBY2_0011                    458          567          120       4.72
KIRBY2_0012                    458          463          118       3.92
ACOPR30_0000                   400          782          211       3.71
```

No regression versus session 02 baseline. Bench gates PASS.

## Decisions Made

- **Issue #10 closes as "hardware floor reached on the 1D-banded
  panel"** — no APP path shipped. Cumulative 5-lever verdict
  documented in three research notes:
  - `dev/research/issue-10-maxfromm-phase2-corpus.md` (#1, #2)
  - `dev/research/issue-10-ordering-supernode-shape.md` (#4)
  - `dev/research/issue-10-amalgamation-floor.md` (#5, this session)
- Defaults retained: `SupernodeParams::nemin = 16`,
  `OrderingMethod::Amd`. Opt-in knobs `Solver::with_ordering` and
  `SupernodeParams::nemin` ship as-is for workloads with genuine
  fusion opportunities.
- Recorded in `dev/decisions.md` (2026-05-16 entry).

## Abandoned Approaches

- **Forced supernode amalgamation (`nemin > 16`) on the 1D-banded
  Mittelmann panel.** The shape lever engages (ncol_mean doubles at
  nemin=64) but factor time stays flat or regresses 36% on
  clnlbeam. Recorded in `dev/tried-and-rejected.md` with the per-
  family ratio table and the joint 5-lever conclusion.

## Next Session Should

1. **Pounce-side disposition of #10/#33 closure.** Both issues
   closed with hardware-floor verdicts on the 1D-banded
   Mittelmann panel. pounce-feral's clnlbeam benchmark sees no
   improvement from this work. Decision needed: file a fresh
   issue for "children-of-children amalgamation as the next lever"
   (the `Open question for follow-up` in
   `issue-10-amalgamation-floor.md`), or leave it as documented
   future-work pending a wider workload where it might pay off.
2. **Continue the SQD fast-path build-out (#34).** Session 05 by
   another agent landed commit (a) — research note + bib + decisions
   + GH issue. Commit phasing (b)-(h) is queued in the user-approved
   plan at `/Users/jkitchin/.claude/plans/let-s-work-on-a-reflective-anchor.md`:
   (b) `NumericParams::sqd_mode` builder, (c) dense
   `factor_diagonal`, (d) `factor_one_supernode` dispatch,
   (e) `FeralError::SqdContractViolated`, (f) tests, (g) bench,
   (h) checkpoint + CHANGELOG.
3. **Open follow-ups from the recently-merged agent work** (low
   priority — flagged in agent reports but not blocking):
   - Should `rankdef_exact_*` generators use strict `zero == k`
     oracle vs loose `1 ≤ zero ≤ k`? (#27 follow-up)
   - `wide_frontal_616` dense kernel dispatch question. (#27)
   - FBRAIN3LS_0788 verdict.json scoreboard refresh. (#29)
   - `rankdef_100_1` (true exact-zero) corpus addition. (#31)
4. **Remaining open GH issues for parallel agent work**:
   `#26` (M3: triple the stress-suite SuiteSparse corpus),
   `#13` (per-supernode fixed-overhead reduction), `#14` (wide /
   near-square supernode kernel throughput vs MA57 on 27-point
   stencils), `#18` (NARX_CFy convergence stall), `#11` (re-eval
   SmallLeafBatch default flip), `#28` (M5 CI gate, blocked on #26).
   `#19` (parallel assembly heuristic) is in the recent-commits
   stream and may already be resolved.
