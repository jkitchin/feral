# Session 2026-05-13-03

## Goal

User asked "fix issue #10" (APP — aggressive partial pivoting path
alongside TPP in dense LDLᵀ kernel). The previous session
(`2026-05-13-02`) listed #10 as the next target on the strength of
#9 substantively landing. Per FERAL feature lifecycle, this session
opened with the research phase — specifically, re-measuring the
gate the issue's own posted comment requires.

## Accomplished

- **Gate measurement.** Ran `cargo run --bin diag_supernode_cost
  --release` on the post-`d7267fe` build. Result:
  - ACOPR30_0067 nemin=32 (cluster the gate names): ns/sup 943,
    ns/nnz 61 — **15× the wrong way**.
  - CRESC100_0000 default nemin=16: ns/sup 914, ns/nnz 79 — **12×
    the wrong way**.
  - HAIFAM_0082 (widest corpus fronts, max 86): ns/sup 1174,
    ns/nnz 33 — **36× the wrong way**.
  - Across every corpus row and every `nemin` in the sweep,
    ns/sup exceeds ns/nnz by 4× to 36×. The gate condition
    ("ns/nnz dominates ns/sup") is the opposite of what the
    data shows.
- **Research note.** Wrote `dev/research/dense-app-path.md`
  documenting the gate measurement, the design space, and why the
  motivating data points in the issue body are stale on the
  current build (`fused_gamma0` already lands the per-pivot scan
  fusion, 32×32 SIMD body already lands per-element trailing
  update).
- **Decision recorded.** Appended `dev/decisions.md` 2026-05-13
  entry recommending closure of #10 with the analysis.
- **Tried-and-rejected entry.** Appended
  `dev/tried-and-rejected.md` 2026-05-13 entry recording the
  not-undertaken work and the lesson.
- **Closure comment drafted.** At
  `/tmp/feral-issue-10-closure-comment.md` — auto-mode classifier
  has consistently denied posting issue comments in recent
  sessions, so the user posts manually.

## Benchmark Results

```
--- Dense Phase 2.8.1 exit partition (factor ratio vs MUMPS) ---
bucket                    count      p90     target  verdict
small-frontal (<200)     147982     1.33     <= 2.0     PASS
medium (<500)            152145     1.74     <= 3.0     PASS

--- Sparse Phase 2.8.1 exit partition (factor ratio vs MUMPS) ---
bucket                    count      p90     target  verdict
small-frontal (<200)     153455     1.56     <= 2.0     PASS
medium (<500)            153560     1.56     <= 3.0     PASS
```

Inertia byte-identical to baseline: `153560` matrices with MUMPS
oracle timings, `154500` with SSIDS. No code changes this session
so bench identical (within noise) to `2026-05-13-02`.

`cargo test --lib` 233 passed, 5 ignored.

## Decisions Made

- **Do not implement issue #10 APP path today.** The issue's own
  posted re-open criterion (ns/nnz dominates ns/sup on a relevant
  cluster) is not met by the current `diag_supernode_cost` data,
  with the ratio running 4× to 36× the wrong direction. The two
  motivating gaps (per-pivot γ₀ scan, per-element SIMD trailing
  update) are already closed via `fused_gamma0` (`ad05ff4`) and
  `block_ldlt32` (`d3f1132`). Full rationale in `dev/decisions.md`
  2026-05-13 entry.

## Abandoned Approaches

- **Implementing APP without re-measuring the gate.** Was the
  default path implied by the previous session's "Next session
  should" list. Did not pursue. Recorded in
  `dev/tried-and-rejected.md` 2026-05-13 entry. Lesson reinforced
  (same as 2026-05-12 (c) BLAS-3 quad parking): the
  session-checkpoint follow-up list is not a substitute for
  re-measuring the gate. One binary run was the difference
  between writing dead code and recording a clean closure.

## Issues Status After This Session

- **#9** — Substantively resolved (prior session). Steps 4 and 5
  remain as documented follow-ups, not blockers.
- **#10** — Recommended closure. Comment draft at
  `/tmp/feral-issue-10-closure-comment.md`. User can run
  `gh issue comment 10 --body-file /tmp/feral-issue-10-closure-comment.md`
  and `gh issue close 10` at their convenience.
- **#11** — Pending; the SmallLeafBatch re-evaluation. Now that
  #10 is closing without code changes, #11 is the natural next
  target.
- **#12** — Meta issue; closes when 9/10/11 are done.
- **#13** — User close (auto-classifier denied previous session).

## Next Session Should

1. **Issue #11 — re-evaluate SmallLeafBatch default flip.** Criteria
   from the issue: 5 runs, median speedup ≥ 10% on tiny+small with
   no per-matrix regression > 5%. No new research needed —
   measurement-only.
2. **User-side housekeeping.** Close issues #10 and #13 manually
   (auto-mode classifier has denied issue comments/closes in the
   last 2 sessions). Drafts are at `/tmp/feral-issue-10-closure-comment.md`
   and `/tmp/feral-issue-13-comment.md` respectively.
3. **Per-front overhead follow-ups (if #11 is non-actionable).**
   The decisions.md 2026-05-12 (c) entry lists candidate targets
   from `dev/research/feral-kernel-profile-chainwoo.md` §3 and §4:
   workspace pooling already explored under #13, but `matrix.validate()`
   bypass and `extend_add` direct slice writes are still open
   bench-attackable targets.

## Note on session order

Today is 2026-05-13. Per `CLAUDE.md` "At Session Start" protocol,
`./dev/assemble-context.sh` is run at the start; this session ran
it, then proceeded to the gate measurement. The final commit of
this session refreshes `dev/context.md`.
